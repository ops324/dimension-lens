{
  "name": "dimension-lens",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "teeth": "node scripts/teeth.mjs",
    "bench": "vitest bench --run",
    "ladder": "node scripts/ladder.mjs",
    "ladder:teeth": "node scripts/ladder.mjs --teeth",
    "gecko": "node scripts/gecko.mjs",
    "ladder:budgets": "node scripts/ladder.mjs --teeth --budgets"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/three": "0.185.1",
    "playwright-core": "^1.62.1",
    "typescript": "~5.9.3",
    "vite": "^7.3.6",
    "vitest": "^3.2.7"
  }
}
